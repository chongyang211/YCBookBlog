---
title: 数据与 IO 处理
date: 2025-06-07 10:00:00
permalink: /pages/sh-data-io/
categories:
  - ScriptHub
  - Shell
  - 基础语法
tags:
  - Shell
  - 基础
author:
  name: 杨充
  link: https://github.com/yangchong211
---
# 数据与 IO 处理

## 一、数组关联

索引数组定义/访问/遍历、关联数组(字典)、数组长度 `@`、数组切片、数组增删改。

```bash
#!/bin/bash
echo 'Hello Shell!'
```

## 二、字符串处理

字符串长度/截取/替换/删除、大小写转换、模式匹配、here document/here string、printf 格式化。

```bash
#!/bin/bash
echo 'Hello Shell!'
```

## 三、重定向管道

stdin/stdout/stderr、`>/>>/</<<` 重定向、管道 `|`、tee 分流、/dev/null 黑洞、文件描述符操作。

```bash
#!/bin/bash
echo 'Hello Shell!'
```
