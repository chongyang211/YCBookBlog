---
title: 网络调度与部署
date: 2025-06-07 10:00:00
permalink: /pages/sh-net-deploy/
categories:
  - ScriptHub
  - Shell
  - 运维脚本
tags:
  - Shell
  - 运维
author:
  name: 杨充
  link: https://github.com/yangchong211
---
# 网络调度与部署

## 一、网络诊断

ping/traceroute 连通性、netstat/ss 端口、curl/wget 请求、DNS 解析 dig/nslookup、防火墙 iptables 检查、抓包 tcpdump。

```bash
#!/bin/bash
set -e
echo '运维脚本模板'
```

## 二、定时任务

crontab 语法(分时日周月)、特殊字符(`*/, -`)、环境变量问题、日志重定向、anacron 补充、at 一次性任务。

```bash
#!/bin/bash
set -e
echo '运维脚本模板'
```

## 三、批量部署

SSH 免密登录、ansible 轻量集成、批量执行命令(ssh+for)、文件分发、版本回滚、灰度策略、部署检查清单。

```bash
#!/bin/bash
set -e
echo '运维脚本模板'
```
