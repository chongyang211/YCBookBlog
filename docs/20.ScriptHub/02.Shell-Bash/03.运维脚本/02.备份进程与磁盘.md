---
title: 备份进程与磁盘
date: 2025-06-07 10:00:00
permalink: /pages/sh-backup-disk/
categories:
  - ScriptHub
  - Shell
  - 运维脚本
tags:
  - Shell
  - 运维
author:
  name: 杨充
  link: https://github.com/yangchong211
---
# 备份进程与磁盘

## 一、备份恢复

全量/增量备份、rsync 同步、tar 压缩归档、数据库(mysql/pg)备份、远程备份(scp/sftp)、定时备份 cron、自动清理。

```bash
#!/bin/bash
set -e
echo '运维脚本模板'
```

## 二、进程管理

ps/top/htop 查看、kill/pkill 信号、nohup/disown 后台运行、进程守护(while+sleep)、supervisor 集成。

```bash
#!/bin/bash
set -e
echo '运维脚本模板'
```

## 三、磁盘清理

df/du 磁盘使用、大文件查找、日志轮转(logrotate)、临时文件清理、docker 镜像清理、inode 耗尽排查。

```bash
#!/bin/bash
set -e
echo '运维脚本模板'
```
