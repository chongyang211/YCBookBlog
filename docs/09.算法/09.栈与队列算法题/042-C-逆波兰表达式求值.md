---
title: C007-逆波兰表达式求值
date: 2025-06-07 10:00:00
permalink: /pages/algo200-c007/
categories:
  - book;通用技术的提升;数据结构算法;算法考核;栈与队列
tags:
  - 算法;栈;LeetCode
author: 
  name: 杨充
  link: https://github.com/yangchong211
---
# C007 逆波兰表达式求值

> LeetCode 150 · ⭐⭐ · 后缀表达式
>
> 栈的天然应用场景——后缀表达式无需括号即可唯一确定运算顺序。

## 01. 题目

`["2","1","+","3","*"]` → `9` ((2+1)×3)

## 02. 代码

**Java**：
```java
public int evalRPN(String[] tokens) {
    Deque<Integer> stack = new ArrayDeque<>();
    for (String t : tokens) {
        switch (t) {
            case "+": stack.push(stack.pop() + stack.pop()); break;
            case "-": int b = stack.pop(), a = stack.pop(); stack.push(a - b); break;
            case "*": stack.push(stack.pop() * stack.pop()); break;
            case "/": int d = stack.pop(), c = stack.pop(); stack.push(c / d); break;
            default: stack.push(Integer.parseInt(t));
        }
    }
    return stack.pop();
}
```

**Python**：
```python
def evalRPN(self, tokens):
    stack = []
    ops = {'+': lambda a,b: a+b, '-': lambda a,b: a-b, '*': lambda a,b: a*b, '/': lambda a,b: int(a/b)}
    for t in tokens:
        if t in ops:
            b, a = stack.pop(), stack.pop()
            stack.append(ops[t](a, b))
        else: stack.append(int(t))
    return stack[0]
```

**复杂度**：时间 O(N)，空间 O(N)。
