---
title: G007-课程表II
date: 2025-06-07 10:00:00
permalink: /pages/algo200-g007/
categories: [book, 通用技术的提升, 数据结构算法, 算法考核, 图]
tags: [算法, 拓扑排序, LeetCode]
author: {name: 杨充, link: https://github.com/yangchong211}
---
# G007 课程表 II

> LeetCode 210 · ⭐⭐ · 拓扑排序

## 01. 题目

返回课程的学习顺序（拓扑排序结果）。若存在环返回空数组。

## 02. 代码

**Java**：
```java
public int[] findOrder(int n, int[][] pre) {
    List<Integer>[] adj=new List[n];
    for(int i=0;i<n;i++) adj[i]=new ArrayList<>();
    int[] indeg=new int[n];
    for(int[] p:pre){ adj[p[1]].add(p[0]); indeg[p[0]]++; }
    Queue<Integer> q=new LinkedList<>();
    for(int i=0;i<n;i++) if(indeg[i]==0) q.offer(i);
    int[] res=new int[n]; int idx=0;
    while(!q.isEmpty()){
        int u=q.poll(); res[idx++]=u;
        for(int v:adj[u]) if(--indeg[v]==0) q.offer(v);
    }
    return idx==n?res:new int[0];
}
```

**Python**：
```python
def findOrder(self, n, pre):
    adj=[[] for _ in range(n)]; indeg=[0]*n
    for a,b in pre: adj[b].append(a); indeg[a]+=1
    q=deque([i for i in range(n) if indeg[i]==0]); res=[]
    while q:
        u=q.popleft(); res.append(u)
        for v in adj[u]: indeg[v]-=1; [q.append(v) for _ in [None] if indeg[v]==0]
    return res if len(res)==n else []
```

**复杂度**：O(V+E)/O(V+E)。**相关**：[G006 课程表](G006-课程表.md)
