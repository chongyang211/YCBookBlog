---
title: L001/L002-跳跃游戏
date: 2025-06-07 10:00:00
permalink: /pages/algo200-l001/
categories: [book, 通用技术的提升, 数据结构算法, 算法考核, 贪心算法]
tags: [算法, 贪心, LeetCode]
author: {name: 杨充, link: https://github.com/xugaoyi}
---
# L001 跳跃游戏 / L002 跳跃游戏 II

> LeetCode 55/45 · ⭐⭐ · 贪心

## L001 能否到达

```java
public boolean canJump(int[] nums) {
    int reach=0;
    for(int i=0;i<nums.length;i++) {
        if(i>reach) return false;
        reach=Math.max(reach,i+nums[i]);
    }
    return true;
}
```

## L002 最小跳跃次数

```java
public int jump(int[] nums) {
    int jumps=0, curEnd=0, farthest=0;
    for(int i=0;i<nums.length-1;i++) {
        farthest=Math.max(farthest,i+nums[i]);
        if(i==curEnd) { jumps++; curEnd=farthest; }
    }
    return jumps;
}
```

**Python**：
```python
def canJump(self, nums):
    reach=0
    for i,n in enumerate(nums):
        if i>reach: return False
        reach=max(reach,i+n)
    return True

def jump(self, nums):
    jumps=cur_end=farthest=0
    for i in range(len(nums)-1):
        farthest=max(farthest,i+nums[i])
        if i==cur_end: jumps+=1; cur_end=farthest
    return jumps
```

**复杂度**：O(N)/O(1)。

**相关**：[L004 无重叠区间](L004-无重叠区间.md)、[L006 合并区间](L006-合并区间.md)
