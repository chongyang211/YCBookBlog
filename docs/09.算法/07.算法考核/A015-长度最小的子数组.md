---
title: A015-长度最小的子数组
date: 2025-06-07 10:00:00
permalink: /pages/algo200-a015/
categories:
  - book
  - 通用技术的提升
  - 数据结构算法
  - 算法考核
  - 数组与字符串
tags:
  - 算法
  - 滑动窗口
  - LeetCode
author: 
  name: 杨充
  link: https://github.com/yangchong211
---
# A015 长度最小的子数组（Minimum Size Subarray Sum）

> LeetCode 209 · ⭐⭐ · 滑动窗口
>
> 滑动窗口的入门题。"右扩累加，满足条件左缩"——这道模板题写熟后，所有滑动窗口题都是它的变体。

---

## 01. 题目描述

找出和 ≥ target 的长度最小的连续子数组，返回其长度。若不存在返回 0。

**示例**：

```
输入：target = 7, nums = [2,3,1,2,4,3]
输出：2  ([4,3])
```

---

## 02. 题目分析

### 滑动窗口模板

```
窗口初始化 l=0, sum=0
for r in 0..n-1:
    sum += nums[r]          // 右扩
    while sum >= target:    // 满足条件
        更新 minLen
        sum -= nums[l++]    // 左缩
```

---

## 03. 代码

**Java**：

```java
public int minSubArrayLen(int target, int[] nums) {
    int l = 0, sum = 0, min = Integer.MAX_VALUE;
    for (int r = 0; r < nums.length; r++) {
        sum += nums[r];
        while (sum >= target) {
            min = Math.min(min, r - l + 1);
            sum -= nums[l++];
        }
    }
    return min == Integer.MAX_VALUE ? 0 : min;
}
```

**Python**：

```python
def minSubArrayLen(self, target: int, nums: List[int]) -> int:
    l = s = 0
    ans = len(nums) + 1
    for r, x in enumerate(nums):
        s += x
        while s >= target:
            ans = min(ans, r - l + 1)
            s -= nums[l]
            l += 1
    return 0 if ans > len(nums) else ans
```

**C++**：

```cpp
int minSubArrayLen(int target, vector<int>& nums) {
    int l = 0, sum = 0, ans = INT_MAX;
    for (int r = 0; r < nums.size(); r++) {
        sum += nums[r];
        while (sum >= target) {
            ans = min(ans, r - l + 1);
            sum -= nums[l++];
        }
    }
    return ans == INT_MAX ? 0 : ans;
}
```

**复杂度**：时间 O(N)，空间 O(1)。

**相关题目**：[A016 无重复字符最长子串](A016-无重复字符的最长子串.md)
