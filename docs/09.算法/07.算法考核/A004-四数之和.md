---
title: A004-四数之和
date: 2025-06-07 10:00:00
permalink: /pages/algo200-a004/
categories:
  - book
  - 通用技术的提升
  - 数据结构算法
  - 算法考核
  - 数组与字符串
tags:
  - 算法
  - 双指针
  - 排序
  - 剪枝
  - LeetCode
author: 
  name: 杨充
  link: https://github.com/xugaoyi
---
# A004 四数之和（4Sum）

> LeetCode 18 · ⭐⭐ · 排序+双指针+剪枝
>
> 三数之和的推广——多一层循环 + 关键剪枝。学会这道题，N 数之和触类旁通。

---

## 01. 题目描述

给定包含 n 个整数的数组 `nums` 和目标值 `target`，找出所有不重复的四元组 `[nums[a], nums[b], nums[c], nums[d]]` 满足和为 target。a、b、c、d 互不相同。

**示例**：

```
输入：nums = [1,0,-1,0,-2,2], target = 0
输出：[[-2,-1,1,2],[-2,0,0,2],[-1,0,0,1]]
```

---

## 02. 题目分析

### 2.1 框架

从三数之和扩展：固定前两个数 (i, j)，双指针找后两个 (l, r)。

```
排序 → for i → for j → while(l<r) 夹逼
```

### 2.2 关键剪枝

N 越大，剪枝越重要：

| 剪枝 | 条件 | 作用 |
|------|------|------|
| 最小和 > target | `nums[i]+nums[i+1]+nums[i+2]+nums[i+3] > target` | 后面更大，直接 break |
| 最大和 < target | `nums[i]+nums[n-3]+nums[n-2]+nums[n-1] < target` | i 不够大，continue |
| 重复跳过 | `i>0 && nums[i]==nums[i-1]` | 去重 |

### 2.3 溢出注意

四数之和可能超过 int 范围，用 `long` 计算和。

---

## 03. 代码

**Java**：

```java
public List<List<Integer>> fourSum(int[] nums, int target) {
    Arrays.sort(nums);
    List<List<Integer>> res = new ArrayList<>();
    int n = nums.length;
    if (n < 4) return res;

    for (int i = 0; i < n - 3; i++) {
        // 剪枝1：最小和 > target
        if ((long)nums[i]+nums[i+1]+nums[i+2]+nums[i+3] > target) break;
        // 剪枝2：最大和 < target
        if ((long)nums[i]+nums[n-3]+nums[n-2]+nums[n-1] < target) continue;
        if (i > 0 && nums[i] == nums[i - 1]) continue;

        for (int j = i + 1; j < n - 2; j++) {
            if ((long)nums[i]+nums[j]+nums[j+1]+nums[j+2] > target) break;
            if ((long)nums[i]+nums[j]+nums[n-2]+nums[n-1] < target) continue;
            if (j > i + 1 && nums[j] == nums[j - 1]) continue;

            int l = j + 1, r = n - 1;
            while (l < r) {
                long sum = (long)nums[i]+nums[j]+nums[l]+nums[r];
                if (sum == target) {
                    res.add(Arrays.asList(nums[i],nums[j],nums[l],nums[r]));
                    while (l < r && nums[l] == nums[l+1]) l++;
                    while (l < r && nums[r] == nums[r-1]) r--;
                    l++; r--;
                } else if (sum < target) l++;
                else r--;
            }
        }
    }
    return res;
}
```

**Python**：

```python
def fourSum(self, nums: List[int], target: int) -> List[List[int]]:
    nums.sort()
    res, n = [], len(nums)
    if n < 4: return res
    for i in range(n - 3):
        if nums[i] + nums[i+1] + nums[i+2] + nums[i+3] > target: break
        if nums[i] + nums[n-3] + nums[n-2] + nums[n-1] < target: continue
        if i > 0 and nums[i] == nums[i-1]: continue
        for j in range(i+1, n-2):
            if nums[i]+nums[j]+nums[j+1]+nums[j+2] > target: break
            if nums[i]+nums[j]+nums[n-2]+nums[n-1] < target: continue
            if j > i+1 and nums[j] == nums[j-1]: continue
            l, r = j+1, n-1
            while l < r:
                s = nums[i]+nums[j]+nums[l]+nums[r]
                if s == target:
                    res.append([nums[i],nums[j],nums[l],nums[r]])
                    while l < r and nums[l] == nums[l+1]: l += 1
                    while l < r and nums[r] == nums[r-1]: r -= 1
                    l += 1; r -= 1
                elif s < target: l += 1
                else: r -= 1
    return res
```

**C++**：

```cpp
vector<vector<int>> fourSum(vector<int>& nums, int target) {
    sort(nums.begin(), nums.end());
    vector<vector<int>> res;
    int n = nums.size();
    if (n < 4) return res;
    for (int i = 0; i < n - 3; i++) {
        if ((long)nums[i]+nums[i+1]+nums[i+2]+nums[i+3] > target) break;
        if ((long)nums[i]+nums[n-3]+nums[n-2]+nums[n-1] < target) continue;
        if (i > 0 && nums[i] == nums[i-1]) continue;
        for (int j = i+1; j < n-2; j++) {
            if ((long)nums[i]+nums[j]+nums[j+1]+nums[j+2] > target) break;
            if ((long)nums[i]+nums[j]+nums[n-2]+nums[n-1] < target) continue;
            if (j > i+1 && nums[j] == nums[j-1]) continue;
            int l = j+1, r = n-1;
            while (l < r) {
                long s = (long)nums[i]+nums[j]+nums[l]+nums[r];
                if (s == target) {
                    res.push_back({nums[i],nums[j],nums[l],nums[r]});
                    while (l<r && nums[l]==nums[l+1]) l++;
                    while (l<r && nums[r]==nums[r-1]) r--;
                    l++; r--;
                } else if (s < target) l++;
                else r--;
            }
        }
    }
    return res;
}
```

**复杂度**：时间 O(N³)，空间 O(logN)。

---

## 04. 总结

| 关键点 | 说明 |
|--------|------|
| 框架 | N 数之和 = 排序 + (N-2) 层循环 + 双指针 |
| 剪枝 | 提前判断最小和/最大和 |
| 溢出 | 四数相加用 `long` |

**相关题目**：[A002 三数之和](A002-三数之和.md)、[A003 最接近的三数之和](A003-最接近的三数之和.md)
