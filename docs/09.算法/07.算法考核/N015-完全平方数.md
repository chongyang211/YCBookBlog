---
title: N015-完全平方数
date: 2025-06-07 10:00:00
permalink: /pages/algo200-n015/
categories: [book, 通用技术的提升, 数据结构算法, 算法考核, 动态规划]
tags: [算法, DP, 完全背包, LeetCode]
author: {name: 杨充, link: https://github.com/yangchong211}
---
# N015 完全平方数（Perfect Squares）

> LeetCode 279 · ⭐⭐ · 完全背包
>
> 硬币就是完全平方数——N011 零钱兑换的特例。

## 01. 题目

`n = 12` → 3（4+4+4）；`n = 13` → 2（4+9）

## 02. 分析

与 N011 完全相同的完全背包框架。硬币集合 = {1, 4, 9, 16, ...}。

## 03. 代码

**Java**：
```java
public int numSquares(int n) {
    int[] dp = new int[n + 1];
    Arrays.fill(dp, n + 1);
    dp[0] = 0;
    for (int i = 1; i * i <= n; i++)
        for (int j = i * i; j <= n; j++)
            dp[j] = Math.min(dp[j], dp[j - i * i] + 1);
    return dp[n];
}
```

**Python**：
```python
def numSquares(self, n: int) -> int:
    dp = [n + 1] * (n + 1)
    dp[0] = 0
    for i in range(1, int(n**0.5) + 1):
        sq = i * i
        for j in range(sq, n + 1):
            dp[j] = min(dp[j], dp[j - sq] + 1)
    return dp[n]
```

**C++**：
```cpp
int numSquares(int n) {
    vector<int> dp(n + 1, n + 1);
    dp[0] = 0;
    for (int i = 1; i * i <= n; i++)
        for (int j = i * i; j <= n; j++)
            dp[j] = min(dp[j], dp[j - i*i] + 1);
    return dp[n];
}
```

**复杂度**：O(N·√N) / O(N)。

---

## 04. 四平方和定理（选读）

任何正整数都可以表示为最多 4 个完全平方数之和（Lagrange定理）。因此答案只能是 1/2/3/4。

**相关题目**：[N011 零钱兑换](N011-零钱兑换.md)、[N012 零钱兑换II](N012-零钱兑换II.md)
