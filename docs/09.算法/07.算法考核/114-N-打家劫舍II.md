---
title: N003-打家劫舍II
date: 2025-06-07 10:00:00
permalink: /pages/algo200-n003/
categories: [book, 通用技术的提升, 数据结构算法, 算法考核, 动态规划]
tags: [算法, DP, 环形DP, LeetCode]
author: {name: 杨充, link: https://github.com/yangchong211}
---
# N003 打家劫舍 II（环形）

> LeetCode 213 · ⭐⭐ · 环形DP

## 01. 题目

房屋围成环形（首尾相邻），不能偷相邻两家。`[2,3,2]` → 3。

## 02. 题目分析

环形 → 首尾不能同时偷。做两次线性 DP：
- `rob(0, n-2)`：不偷最后一家
- `rob(1, n-1)`：不偷第一家
取 max。

## 03. 代码

**Java**：
```java
public int rob(int[] nums) {
    int n = nums.length;
    if (n == 1) return nums[0];
    return Math.max(robRange(nums, 0, n - 2), robRange(nums, 1, n - 1));
}
int robRange(int[] nums, int l, int r) {
    int prev2 = 0, prev = 0;
    for (int i = l; i <= r; i++) {
        int cur = Math.max(prev, prev2 + nums[i]);
        prev2 = prev; prev = cur;
    }
    return prev;
}
```

**Python**：
```python
def rob(self, nums):
    def rob_range(l, r):
        prev2 = prev = 0
        for i in range(l, r + 1):
            cur = max(prev, prev2 + nums[i])
            prev2 = prev; prev = cur
        return prev
    n = len(nums)
    if n == 1: return nums[0]
    return max(rob_range(0, n-2), rob_range(1, n-1))
```

**复杂度**：O(N)/O(1)。

**相关**：[N002 打家劫舍](N002-打家劫舍.md)、[N004 打家劫舍III](N004-打家劫舍III.md)
