---
title: B007-合并K个升序链表
date: 2025-06-07 10:00:00
permalink: /pages/algo200-b007/
categories:
  - book
  - 通用技术的提升
  - 数据结构算法
  - 算法考核
  - 链表
tags:
  - 算法
  - 链表
  - 优先队列
  - 分治
  - LeetCode
author: 
  name: 杨充
  link: https://github.com/yangchong211
---
# B007 合并 K 个升序链表

> LeetCode 23 · ⭐⭐⭐ · 优先队列 / 分治
>
> 两种解法覆盖两个重要数据结构。堆解最直观，分治解展示了如何化 K 为 2。

---

## 01. 题目描述

合并 K 个有序链表。

---

## 02. 题目分析

| 解法 | 时间 | 空间 | 思路 |
|------|:---:|:---:|------|
| 优先队列 | O(NlogK) | O(K) | K个头的堆 |
| 分治合并 | O(NlogK) | O(logK) | 两两合并 |

---

## 03. 解法一：优先队列

**Java**：

```java
public ListNode mergeKLists(ListNode[] lists) {
    PriorityQueue<ListNode> pq = new PriorityQueue<>((a,b) -> a.val - b.val);
    for (ListNode node : lists) if (node != null) pq.offer(node);
    ListNode dummy = new ListNode(0), cur = dummy;
    while (!pq.isEmpty()) {
        ListNode min = pq.poll();
        cur.next = min; cur = cur.next;
        if (min.next != null) pq.offer(min.next);
    }
    return dummy.next;
}
```

**Python**：

```python
def mergeKLists(self, lists):
    from heapq import heappush, heappop
    # 需要wrapper因为heap比较tuple时，若val相等会比较ListNode（不支持比较）
    heap = []
    ListNode.__lt__ = lambda self, other: self.val < other.val
    for node in lists:
        if node: heappush(heap, node)
    dummy = cur = ListNode(0)
    while heap:
        node = heappop(heap)
        cur.next = node; cur = cur.next
        if node.next: heappush(heap, node.next)
    return dummy.next
```

## 04. 解法二：分治合并

```java
public ListNode mergeKLists(ListNode[] lists) {
    if (lists.length == 0) return null;
    return merge(lists, 0, lists.length - 1);
}
ListNode merge(ListNode[] lists, int l, int r) {
    if (l == r) return lists[l];
    int m = (l + r) / 2;
    return mergeTwo(merge(lists, l, m), merge(lists, m + 1, r));
}
```

**相关题目**：[B006 合并两个](B006-合并两个有序链表.md)
