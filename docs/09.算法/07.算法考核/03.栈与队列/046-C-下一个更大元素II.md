---
title: C011-下一个更大元素II
date: 2025-06-07 10:00:00
permalink: /pages/algo200-c011/
categories:
  - book;通用技术的提升;数据结构算法;算法考核;栈与队列
tags:
  - 算法;单调栈;LeetCode
author: {name: 杨充, link: https://github.com/yangchong211}
---
# C011 下一个更大元素 II

> LeetCode 503 · ⭐⭐ · 单调栈+循环数组
>
> 循环数组技巧：拼接或 `i%n` 遍历两遍。

## 01. 代码

**Java**：
```java
public int[] nextGreaterElements(int[] nums) {
    int n = nums.length;
    int[] ans = new int[n]; Arrays.fill(ans, -1);
    Deque<Integer> stack = new ArrayDeque<>();
    for (int i = 0; i < 2 * n; i++) {
        while (!stack.isEmpty() && nums[i % n] > nums[stack.peek()]) {
            ans[stack.pop()] = nums[i % n];
        }
        if (i < n) stack.push(i);
    }
    return ans;
}
```

**Python**：
```python
def nextGreaterElements(self, nums):
    n = len(nums)
    ans, stack = [-1] * n, []
    for i in range(2 * n):
        while stack and nums[i % n] > nums[stack[-1]]:
            ans[stack.pop()] = nums[i % n]
        if i < n: stack.append(i)
    return ans
```

**复杂度**：时间 O(N)，空间 O(N)。
