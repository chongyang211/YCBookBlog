---
title: A008-删除有序数组中的重复项
date: 2025-06-07 10:00:00
permalink: /pages/algo200-a008/
categories:
  - book
  - 通用技术的提升
  - 数据结构算法
  - 算法考核
  - 数组与字符串
tags:
  - 算法
  - 双指针
  - LeetCode
author: 
  name: 杨充
  link: https://github.com/yangchong211
---
# A008 删除有序数组中的重复项（Remove Duplicates）

> LeetCode 26 · ⭐ · 双指针
>
> 移动零的姊妹题——快慢指针，"遇到不同的就写入"。

---

## 01. 题目描述

原地删除有序数组中的重复项，使每个元素只出现一次，返回新长度。

**示例**：

```
输入：nums = [0,0,1,1,1,2,2,3,3,4]
输出：5, nums = [0,1,2,3,4,_,_,_,_,_]
```

---

## 02. 代码

**Java**：

```java
public int removeDuplicates(int[] nums) {
    int slow = 0;
    for (int fast = 1; fast < nums.length; fast++)
        if (nums[fast] != nums[slow])
            nums[++slow] = nums[fast];
    return slow + 1;
}
```

**Python**：

```python
def removeDuplicates(self, nums: List[int]) -> int:
    slow = 0
    for fast in range(1, len(nums)):
        if nums[fast] != nums[slow]:
            slow += 1
            nums[slow] = nums[fast]
    return slow + 1
```

**C++**：

```cpp
int removeDuplicates(vector<int>& nums) {
    int slow = 0;
    for (int fast = 1; fast < nums.size(); fast++)
        if (nums[fast] != nums[slow])
            nums[++slow] = nums[fast];
    return slow + 1;
}
```

**复杂度**：时间 O(N)，空间 O(1)。

**相关题目**：[A009 删除重复项 II](A009-删除有序数组中的重复项II.md)
