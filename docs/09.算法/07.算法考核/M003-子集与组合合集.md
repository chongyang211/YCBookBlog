---
title: M003-M007-子集与组合合集
date: 2025-06-07 10:00:00
permalink: /pages/algo200-m003/
categories: [book, 通用技术的提升, 数据结构算法, 算法考核, 回溯算法]
tags: [算法, 回溯, 组合, 子集, LeetCode]
author: {name: 杨充, link: https://github.com/xugaoyi}
---
# M003-M007 子集与组合合集

> LeetCode 78/90/39/40/77/17 · ⭐⭐ · 回溯

## 回溯通用模板

```java
void backtrack(参数) {
    if(终止条件) { 收集结果; return; }
    for(选择 in 选择列表) {
        做选择; backtrack(...); 撤销选择;
    }
}
```

## M003 子集 (78)

```java
List<List<Integer>> res = new ArrayList<>();
public List<List<Integer>> subsets(int[] nums) {
    backtrack(nums, 0, new ArrayList<>()); return res;
}
void backtrack(int[] nums, int start, List<Integer> path) {
    res.add(new ArrayList<>(path));
    for (int i = start; i < nums.length; i++) {
        path.add(nums[i]); backtrack(nums, i+1, path); path.remove(path.size()-1);
    }
}
```

## M004 子集 II (90) — 有重复元素

```java
public List<List<Integer>> subsetsWithDup(int[] nums) {
    Arrays.sort(nums); backtrack(nums,0,new ArrayList<>()); return res;
}
void backtrack(int[] nums, int start, List<Integer> path) {
    res.add(new ArrayList<>(path));
    for (int i=start; i<nums.length; i++) {
        if (i>start && nums[i]==nums[i-1]) continue; // 去重
        path.add(nums[i]); backtrack(nums,i+1,path); path.remove(path.size()-1);
    }
}
```

## M005 组合总和 (39) — 可无限重复使用

```java
public List<List<Integer>> combinationSum(int[] cand, int target) {
    backtrack(cand,target,0,new ArrayList<>()); return res;
}
void backtrack(int[] cand, int remain, int start, List<Integer> path) {
    if(remain==0) { res.add(new ArrayList<>(path)); return; }
    if(remain<0) return;
    for(int i=start; i<cand.length; i++) {
        path.add(cand[i]); backtrack(cand,remain-cand[i],i,path); path.remove(path.size()-1);
    }
}
```

## M006 组合总和 II (40) — 每个只能用一次+去重

```java
public List<List<Integer>> combinationSum2(int[] cand, int target) {
    Arrays.sort(cand); backtrack(cand,target,0,new ArrayList<>()); return res;
}
void backtrack(int[] cand, int remain, int start, List<Integer> path) {
    if(remain==0) { res.add(new ArrayList<>(path)); return; }
    for(int i=start; i<cand.length; i++) {
        if(cand[i]>remain) break;
        if(i>start && cand[i]==cand[i-1]) continue;
        path.add(cand[i]); backtrack(cand,remain-cand[i],i+1,path); path.remove(path.size()-1);
    }
}
```

## M007 组合 (77) — C(n,k)

```java
public List<List<Integer>> combine(int n, int k) {
    backtrack(n,k,1,new ArrayList<>()); return res;
}
void backtrack(int n, int k, int start, List<Integer> path) {
    if(path.size()==k) { res.add(new ArrayList<>(path)); return; }
    for(int i=start; i<=n; i++) {
        path.add(i); backtrack(n,k,i+1,path); path.remove(path.size()-1);
    }
}
```

## M008 电话号码的字母组合 (17)

```java
String[] map = {"","","abc","def","ghi","jkl","mno","pqrs","tuv","wxyz"};
public List<String> letterCombinations(String digits) {
    List<String> res = new ArrayList<>();
    if(digits.isEmpty()) return res;
    backtrack(digits,0,new StringBuilder(),res);
    return res;
}
void backtrack(String digits, int idx, StringBuilder sb, List<String> res) {
    if(idx==digits.length()) { res.add(sb.toString()); return; }
    for(char c : map[digits.charAt(idx)-'0'].toCharArray()) {
        sb.append(c); backtrack(digits,idx+1,sb,res); sb.deleteCharAt(sb.length()-1);
    }
}
```

## 回溯去重诀窍

| 问题类型 | 去重方式 |
|---------|---------|
| 全排列 | `used[i]` 或 `i>0 && nums[i]==nums[i-1] && !used[i-1]` |
| 子集/组合 | 排序 + `i>start && nums[i]==nums[i-1]` |

**复杂度**：子集 O(N·2^N)，组合 C(n,k)，全排列 O(N·N!)。

**相关**：[M011 N皇后](M011-N皇后.md)
