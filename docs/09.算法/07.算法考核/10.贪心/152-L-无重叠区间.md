---
title: L004-无重叠区间
date: 2025-06-07 10:00:00
permalink: /pages/algo200-l004/
categories: [book, 通用技术的提升, 数据结构算法, 算法考核, 贪心算法]
tags: [算法, 贪心, 区间调度, LeetCode]
author: {name: 杨充, link: https://github.com/yangchong211}
---
# L004 无重叠区间

> LeetCode 435 · ⭐⭐ · 区间调度贪心

## 01. 题目

`intervals = [[1,2],[2,3],[3,4],[1,3]]` → 1（移除 [1,3] 即可不重叠）。

## 02. 题目分析

经典区间调度问题：按**结束时间**排序，贪心选择最早结束的区间，跳过与之重叠的。

```
排序后：[[1,2],[2,3],[1,3],[3,4]]
选[1,2], end=2
[2,3] start>=end → 选, end=3
[1,3] start<end → 跳过, cnt++
[3,4] start>=end → 选, end=4
cnt=1
```

## 03. 代码

**Java**：
```java
public int eraseOverlapIntervals(int[][] intervals) {
    Arrays.sort(intervals, (a, b) -> a[1] - b[1]);
    int end = intervals[0][1], remove = 0;
    for (int i = 1; i < intervals.length; i++) {
        if (intervals[i][0] < end) remove++;
        else end = intervals[i][1];
    }
    return remove;
}
```

**Python**：
```python
def eraseOverlapIntervals(self, intervals):
    intervals.sort(key=lambda x: x[1])
    end, cnt = intervals[0][1], 0
    for s, e in intervals[1:]:
        if s < end: cnt += 1
        else: end = e
    return cnt
```

**复杂度**：O(NlogN)/O(1)。**核心**：按 end 排序，贪心选最早结束的。

**相关**：[L005 引爆气球](L005-用最少数量的箭引爆气球.md)、[L006 合并区间](L006-合并区间.md)
