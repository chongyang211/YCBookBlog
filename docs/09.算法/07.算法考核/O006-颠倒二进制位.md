---
title: O006-颠倒二进制位
date: 2025-06-07 10:00:00
permalink: /pages/algo200-o006/
categories: [book, 通用技术的提升, 数据结构算法, 算法考核, 位运算]
tags: [算法, 位运算, LeetCode]
author: {name: 杨充, link: https://github.com/yangchong211}
---
# O006 颠倒二进制位

> LeetCode 190 · ⭐ · 逐位反转

## 01. 题目

颠倒32位无符号整数的二进制位。

## 02. 代码

**Java**：
```java
public int reverseBits(int n) {
    int r=0; for(int i=0;i<32;i++){ r=(r<<1)|(n&1); n>>>=1; } return r;
}
```

**Python**：
```python
def reverseBits(self, n: int) -> int:
    return int(bin(n)[2:].zfill(32)[::-1], 2)
```

**C++**：
```cpp
uint32_t reverseBits(uint32_t n) {
    uint32_t r=0; for(int i=0;i<32;i++){ r=(r<<1)|(n&1); n>>=1; } return r;
}
```

**复杂度**：O(1)。

**相关**：[O008 两整数之和](O008-两整数之和.md)
