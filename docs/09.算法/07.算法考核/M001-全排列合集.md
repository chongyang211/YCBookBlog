---
title: M001/M002-全排列
date: 2025-06-07 10:00:00
permalink: /pages/algo200-m001/
categories: [book, 通用技术的提升, 数据结构算法, 算法考核, 回溯算法]
tags: [算法, 回溯, LeetCode]
author: {name: 杨充, link: https://github.com/xugaoyi}
---
# M001 全排列 / M002 全排列 II

> LeetCode 46/47 · ⭐⭐ · 回溯+去重

## M001 无重复全排列

**Java**：
```java
List<List<Integer>> res = new ArrayList<>();
public List<List<Integer>> permute(int[] nums) {
    backtrack(nums, new ArrayList<>(), new boolean[nums.length]);
    return res;
}
void backtrack(int[] nums, List<Integer> path, boolean[] used) {
    if(path.size()==nums.length) { res.add(new ArrayList<>(path)); return; }
    for(int i=0;i<nums.length;i++) {
        if(used[i]) continue;
        path.add(nums[i]); used[i]=true;
        backtrack(nums,path,used);
        path.remove(path.size()-1); used[i]=false;
    }
}
```

## M002 含重复全排列（排序+剪枝去重）

```java
public List<List<Integer>> permuteUnique(int[] nums) {
    Arrays.sort(nums); // 必须先排序
    backtrack(nums, new ArrayList<>(), new boolean[nums.length]);
    return res;
}
void backtrack(int[] nums, List<Integer> path, boolean[] used) {
    if(path.size()==nums.length) { res.add(new ArrayList<>(path)); return; }
    for(int i=0;i<nums.length;i++) {
        if(used[i]||(i>0&&nums[i]==nums[i-1]&&!used[i-1])) continue;
        path.add(nums[i]); used[i]=true;
        backtrack(nums,path,used);
        path.remove(path.size()-1); used[i]=false;
    }
}
```

**Python**：
```python
def permute(self, nums):
    res=[]
    def dfs(path, used):
        if len(path)==len(nums): res.append(path[:]); return
        for i in range(len(nums)):
            if not used[i]:
                used[i]=True; path.append(nums[i])
                dfs(path,used)
                path.pop(); used[i]=False
    dfs([],[False]*len(nums))
    return res

def permuteUnique(self, nums):
    nums.sort(); res=[]
    def dfs(path,used):
        if len(path)==len(nums): res.append(path[:]); return
        for i in range(len(nums)):
            if used[i] or (i>0 and nums[i]==nums[i-1] and not used[i-1]): continue
            used[i]=True; path.append(nums[i]); dfs(path,used); path.pop(); used[i]=False
    dfs([],[False]*len(nums))
    return res
```

**复杂度**：O(N·N!)/O(N)。**去重关键**：`!used[i-1]` 保证同层不重复。

**相关**：[M003 子集](M003-子集.md)、[M005 组合总和](M005-组合总和.md)
