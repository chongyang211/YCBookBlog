---
title: L004/L005/L006-区间问题合集
date: 2025-06-07 10:00:00
permalink: /pages/algo200-l004/
categories: [book, 通用技术的提升, 数据结构算法, 算法考核, 贪心算法]
tags: [算法, 贪心, 区间调度, LeetCode]
author: {name: 杨充, link: https://github.com/xugaoyi}
---
# L004-L006 区间问题合集

> LeetCode 435/452/56 · ⭐⭐ · 贪心区间调度

## L004 无重叠区间 (435)
最少移除几个区间使剩余不重叠。

```java
public int eraseOverlapIntervals(int[][] intervals) {
    Arrays.sort(intervals,(a,b)->a[1]-b[1]);
    int end=intervals[0][1], cnt=0;
    for(int i=1;i<intervals.length;i++)
        if(intervals[i][0]<end) cnt++; else end=intervals[i][1];
    return cnt;
}
```

## L005 用最少数量的箭引爆气球 (452)
```java
public int findMinArrowShots(int[][] points) {
    Arrays.sort(points,(a,b)->Integer.compare(a[1],b[1]));
    int end=points[0][1], arrows=1;
    for(int i=1;i<points.length;i++)
        if(points[i][0]>end) { arrows++; end=points[i][1]; }
    return arrows;
}
```

## L006 合并区间 (56)
```java
public int[][] merge(int[][] intervals) {
    Arrays.sort(intervals,(a,b)->a[0]-b[0]);
    List<int[]> res=new ArrayList<>();
    for(int[] in:intervals) {
        if(res.isEmpty()||res.get(res.size()-1)[1]<in[0]) res.add(in);
        else res.get(res.size()-1)[1]=Math.max(res.get(res.size()-1)[1],in[1]);
    }
    return res.toArray(new int[res.size()][]);
}
```

**Python**：
```python
def merge(self, intervals):
    intervals.sort(key=lambda x:x[0])
    res=[intervals[0]]
    for s,e in intervals[1:]:
        if s>res[-1][1]: res.append([s,e])
        else: res[-1][1]=max(res[-1][1],e)
    return res
```

**区间调度统一公式**：按 `end` 排序（合并区间例外）。
