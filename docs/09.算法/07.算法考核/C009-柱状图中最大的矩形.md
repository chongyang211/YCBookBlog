---
title: C009-柱状图中最大的矩形
date: 2025-06-07 10:00:00
permalink: /pages/algo200-c009/
categories:
  - book
  - 通用技术的提升
  - 数据结构算法
  - 算法考核
  - 栈与队列
tags:
  - 算法
  - 单调栈
  - LeetCode
author: 
  name: 杨充
  link: https://github.com/yangchong211
---
# C009 柱状图中最大的矩形

> LeetCode 84 · ⭐⭐⭐ · 单调栈
>
> 单调栈第二经典题（第一是接雨水）。"找到左右第一个比自己矮的柱子"是单调栈的范式思维。

---

## 01. 题目描述

给定 heights，求其中能勾勒出的最大矩形面积。

**示例**：`[2,1,5,6,2,3]` → 10 (5+6 的矩形)

---

## 02. 题目分析

对每个柱子 i，以其高度能延伸的最大矩形 = `heights[i] × (rightSmaller - leftSmaller - 1)`。

单调递增栈：遇到矮柱子时弹栈计算面积。

---

## 03. 代码

**Java**：

```java
public int largestRectangleArea(int[] heights) {
    int n = heights.length, max = 0;
    Deque<Integer> stack = new ArrayDeque<>();
    for (int i = 0; i <= n; i++) {
        int h = (i == n) ? 0 : heights[i];
        while (!stack.isEmpty() && h < heights[stack.peek()]) {
            int height = heights[stack.pop()];
            int width = stack.isEmpty() ? i : i - stack.peek() - 1;
            max = Math.max(max, height * width);
        }
        stack.push(i);
    }
    return max;
}
```

**Python**：

```python
def largestRectangleArea(self, heights):
    stack, ans = [], 0
    heights.append(0)  # 哨兵
    for i, h in enumerate(heights):
        while stack and h < heights[stack[-1]]:
            height = heights[stack.pop()]
            width = i if not stack else i - stack[-1] - 1
            ans = max(ans, height * width)
        stack.append(i)
    return ans
```

**C++**：

```cpp
int largestRectangleArea(vector<int>& heights) {
    heights.push_back(0);
    stack<int> st;
    int ans = 0;
    for (int i = 0; i < heights.size(); i++) {
        while (!st.empty() && heights[i] < heights[st.top()]) {
            int h = heights[st.top()]; st.pop();
            int w = st.empty() ? i : i - st.top() - 1;
            ans = max(ans, h * w);
        }
        st.push(i);
    }
    return ans;
}
```

**复杂度**：时间 O(N)，空间 O(N)。

**相关题目**：[A006 接雨水](A006-接雨水.md)、[C010 每日温度](C010-每日温度.md)
