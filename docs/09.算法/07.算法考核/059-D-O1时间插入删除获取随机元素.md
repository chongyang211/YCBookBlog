---
title: D009-前K个高频元素
date: 2025-06-07 10:00:00
permalink: /pages/algo200-d009/
categories:
  - book;通用技术的提升;数据结构算法;算法考核;哈希表与查找
tags:
  - 算法;哈希表;堆;桶排序;LeetCode
author: {name: 杨充, link: https://github.com/yangchong211}
---
# D009 前 K 个高频元素

> LeetCode 347 · ⭐⭐ · 哈希+堆/桶排序

## 01. 题目

`nums = [1,1,1,2,2,3], k = 2` → `[1,2]`

## 02. 解法对比

| 解法 | 时间 | 空间 |
|------|:---:|:---:|
| 最小堆(k大小) | O(NlogK) | O(N+K) |
| 桶排序 | O(N) | O(N) |
| 快速选择 | O(N)平均 | O(N) |

## 03. 桶排序（最优）

**Java**：
```java
public int[] topKFrequent(int[] nums, int k) {
    Map<Integer, Integer> freq = new HashMap<>();
    for (int n : nums) freq.merge(n, 1, Integer::sum);
    List<Integer>[] buckets = new List[nums.length + 1];
    for (int key : freq.keySet()) {
        int f = freq.get(key);
        if (buckets[f] == null) buckets[f] = new ArrayList<>();
        buckets[f].add(key);
    }
    int[] res = new int[k];
    for (int i = buckets.length - 1, j = 0; j < k; i--)
        if (buckets[i] != null) for (int v : buckets[i]) res[j++] = v;
    return res;
}
```

**Python**：
```python
def topKFrequent(self, nums, k):
    from collections import Counter
    freq = Counter(nums)
    buckets = [[] for _ in range(len(nums) + 1)]
    for num, f in freq.items(): buckets[f].append(num)
    res = []
    for i in range(len(buckets)-1, -1, -1):
        for num in buckets[i]:
            res.append(num)
            if len(res) == k: return res
```

**复杂度**：O(N)/O(N)。
