---
title: A005-盛最多水的容器
date: 2025-06-07 10:00:00
permalink: /pages/algo200-a005/
categories:
  - book
  - 通用技术的提升
  - 数据结构算法
  - 算法考核
  - 数组与字符串
tags:
  - 算法
  - 双指针
  - 贪心
  - LeetCode
author: 
  name: 杨充
  link: https://github.com/xugaoyi
---
# A005 盛最多水的容器（Container With Most Water）

> LeetCode 11 · ⭐⭐ · 双指针
>
> 一道让大多数人第一反应是 O(N²) 的题，双指针只需 O(N)。理解"为什么移动矮的"是这道题的精髓。

---

## 01. 题目描述

给定长度为 n 的数组 `height`，n 条垂直线，第 i 条线的高度为 `height[i]`。找出两条线与 x 轴共同构成的容器可以容纳最多的水。

**示例**：

```
输入：[1,8,6,2,5,4,8,3,7]
输出：49
解释：选取 height[1]=8 和 height[8]=7，宽度=7，面积=7×7=49
```

---

## 02. 题目分析

### 2.1 面积公式

$$area = \min(height[l], height[r]) \times (r - l)$$

面积由**较短的边**和**宽度**共同决定。

### 2.2 为什么移动较短的指针？

反证法：假设 `height[l] < height[r]`。
- 固定 l，移动 r → 宽度减小，高度 ≤ height[l] → 面积一定变小 ❌
- 固定 r，移动 l → 宽度减小，但高度可能变大 → 面积可能增大 ✅

所以：**哪边矮就移动哪边**。

```
[1, 8, 6, 2, 5, 4, 8, 3, 7]

l=0(1), r=8(7): min=1, w=8, area=8, 左矮 → l++
l=1(8), r=8(7): min=7, w=7, area=49, 右矮 → r--
l=1(8), r=7(3): min=3, w=6, area=18, 右矮 → r--
l=1(8), r=6(8): min=8, w=5, area=40, 左矮 → l++
l=2(6), r=6(8): min=6, w=4, area=24, 左矮 → l++
l=3(2), r=6(8): min=2, w=3, area=6,  左矮 → l++
l=4(5), r=6(8): min=5, w=2, area=10, 左矮 → l++
l=5(4), r=6(8): min=4, w=1, area=4,  左矮 → l++ → l==r 结束

最大值：49
```

---

## 03. 代码

**Java**：

```java
public int maxArea(int[] height) {
    int l = 0, r = height.length - 1, max = 0;
    while (l < r) {
        int h = Math.min(height[l], height[r]);
        max = Math.max(max, h * (r - l));
        if (height[l] < height[r]) l++;
        else r--;
    }
    return max;
}
```

**Python**：

```python
def maxArea(self, height: List[int]) -> int:
    l, r, ans = 0, len(height) - 1, 0
    while l < r:
        h = min(height[l], height[r])
        ans = max(ans, h * (r - l))
        if height[l] < height[r]: l += 1
        else: r -= 1
    return ans
```

**C++**：

```cpp
int maxArea(vector<int>& height) {
    int l = 0, r = height.size() - 1, ans = 0;
    while (l < r) {
        int h = min(height[l], height[r]);
        ans = max(ans, h * (r - l));
        height[l] < height[r] ? l++ : r--;
    }
    return ans;
}
```

**复杂度**：时间 O(N)，空间 O(1)。

---

## 04. 接雨水 vs 盛水容器

| | 盛最多水的容器 | 接雨水 |
|------|:---:|:---:|
| 求什么 | 最大面积（选两条线） | 总水量（所有凹槽） |
| 双指针移动 | 移动矮的 | 哪边矮处理哪边 |
| 困住水的条件 | 只需两条线 | 需要凹槽形状 |
| 难度 | ⭐⭐ | ⭐⭐⭐ |

---

## 05. 总结

**核心定理**：移动较短指针不会错过最优解。(数学可严格证明)

**相关题目**：[A006 接雨水](A006-接雨水.md)
