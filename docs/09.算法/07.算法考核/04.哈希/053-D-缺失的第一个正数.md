---
title: D003-缺失的第一个正数
date: 2025-06-07 10:00:00
permalink: /pages/algo200-d003/
categories:
  - book;通用技术的提升;数据结构算法;算法考核;哈希表与查找
tags:
  - 算法;原地哈希;LeetCode
author: {name: 杨充, link: https://github.com/yangchong211}
---
# D003 缺失的第一个正数

> LeetCode 41 · ⭐⭐⭐ · 原地哈希

## 01. 题目

`[3,4,-1,1]` → 2。O(N)时间 O(1)空间。

## 02. 分析

答案范围 `[1, n+1]`。将每个数 x 放到下标 x-1 处，然后扫描第一个 `nums[i] != i+1`。

## 03. 代码

**Java**：
```java
public int firstMissingPositive(int[] nums) {
    int n = nums.length;
    for (int i = 0; i < n; i++)
        while (nums[i] > 0 && nums[i] <= n && nums[nums[i]-1] != nums[i])
            swap(nums, i, nums[i]-1);
    for (int i = 0; i < n; i++) if (nums[i] != i+1) return i+1;
    return n+1;
}
void swap(int[] a, int i, int j) { int t = a[i]; a[i] = a[j]; a[j] = t; }
```

**Python**：
```python
def firstMissingPositive(self, nums):
    n = len(nums)
    for i in range(n):
        while 1 <= nums[i] <= n and nums[nums[i]-1] != nums[i]:
            nums[nums[i]-1], nums[i] = nums[i], nums[nums[i]-1]
    for i in range(n):
        if nums[i] != i + 1: return i + 1
    return n + 1
```

**复杂度**：O(N)/O(1)。
