---
title: C012-滑动窗口最大值
date: 2025-06-07 10:00:00
permalink: /pages/algo200-c012/
categories:
  - book;通用技术的提升;数据结构算法;算法考核;栈与队列
tags:
  - 算法;单调队列;LeetCode
author: {name: 杨充, link: https://github.com/yangchong211}
---
# C012 滑动窗口最大值

> LeetCode 239 · ⭐⭐⭐ · 单调队列
>
> 单调递减队列：队首始终是当前窗口最大值。入队时从队尾弹出小于当前值的元素。

## 01. 代码

**Java**：
```java
public int[] maxSlidingWindow(int[] nums, int k) {
    int n = nums.length;
    int[] ans = new int[n - k + 1];
    Deque<Integer> dq = new ArrayDeque<>(); // 存下标，值递减
    for (int i = 0; i < n; i++) {
        while (!dq.isEmpty() && dq.peekFirst() <= i - k) dq.pollFirst();
        while (!dq.isEmpty() && nums[i] >= nums[dq.peekLast()]) dq.pollLast();
        dq.offerLast(i);
        if (i >= k - 1) ans[i - k + 1] = nums[dq.peekFirst()];
    }
    return ans;
}
```

**Python**：
```python
def maxSlidingWindow(self, nums, k):
    dq = deque()
    ans = []
    for i, x in enumerate(nums):
        if dq and dq[0] <= i - k: dq.popleft()
        while dq and nums[dq[-1]] <= x: dq.pop()
        dq.append(i)
        if i >= k - 1: ans.append(nums[dq[0]])
    return ans
```

**复杂度**：时间 O(N)，空间 O(K)。
