---
title: M004-子集II
date: 2025-06-07 10:00:00
permalink: /pages/algo200-m004/
categories: [book, 通用技术的提升, 数据结构算法, 算法考核, 回溯算法]
tags: [算法, 回溯, 去重, LeetCode]
author: {name: 杨充, link: https://github.com/yangchong211}
---
# M004 子集 II（含重复）

> LeetCode 90 · ⭐⭐ · 回溯+排序去重

## 01. 题目

`nums = [1,2,2]` → `[[],[1],[1,2],[1,2,2],[2],[2,2]]`

## 02. 题目分析

在 M003 基础上加：**排序 + 跳过同层重复元素**。

去重条件：`i > start && nums[i] == nums[i-1]`（同层跳过）。

## 03. 代码

**Java**：
```java
public List<List<Integer>> subsetsWithDup(int[] nums) {
    Arrays.sort(nums);
    List<List<Integer>> res = new ArrayList<>();
    backtrack(nums, 0, new ArrayList<>(), res);
    return res;
}
void backtrack(int[] nums, int start, List<Integer> path, List<List<Integer>> res) {
    res.add(new ArrayList<>(path));
    for (int i = start; i < nums.length; i++) {
        if (i > start && nums[i] == nums[i-1]) continue; // 同层去重
        path.add(nums[i]); backtrack(nums, i+1, path, res); path.remove(path.size()-1);
    }
}
```

**Python**：
```python
def subsetsWithDup(self, nums):
    nums.sort()
    res = []
    def dfs(start, path):
        res.append(path[:])
        for i in range(start, len(nums)):
            if i > start and nums[i] == nums[i-1]: continue
            path.append(nums[i]); dfs(i+1, path); path.pop()
    dfs(0, [])
    return res
```

**复杂度**：O(N·2^N)/O(N)。

**相关**：[M003 子集](M003-子集.md)、[M002 全排列II](M002-全排列II.md)
