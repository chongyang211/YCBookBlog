---
title: O004-2的幂
date: 2025-06-07 10:00:00
permalink: /pages/algo200-o004/
categories: [book, 通用技术的提升, 数据结构算法, 算法考核, 位运算]
tags: [算法, 位运算, LeetCode]
author: {name: 杨充, link: https://github.com/yangchong211}
---
# O004 2的幂

> LeetCode 231 · ⭐ · `n & (n-1)`

## 01. 题目

判断一个整数是否是2的幂。`16`→true，`3`→false。

## 02. 分析

2的幂的二进制只有一个1：`1000 & 0111 = 0`。

## 03. 代码

**Java**：
```java
public boolean isPowerOfTwo(int n) { return n > 0 && (n & (n - 1)) == 0; }
```

**Python**：
```python
def isPowerOfTwo(self, n: int) -> bool: return n > 0 and n & (n - 1) == 0
```

**C++**：
```cpp
bool isPowerOfTwo(int n) { return n > 0 && (n & (n - 1)) == 0; }
```

**复杂度**：O(1)/O(1)。

**相关**：[O005 位1的个数](O005-位1的个数.md)
