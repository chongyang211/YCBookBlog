---
title: E009-二叉树的直径
date: 2025-06-07 10:00:00
permalink: /pages/algo200-e009/
categories:
  - book;通用技术的提升;数据结构算法;算法考核;树与二叉树
tags: [算法, 二叉树, DFS, LeetCode]
author: {name: 杨充, link: https://github.com/yangchong211}
---
# E009 二叉树的直径

> LeetCode 543 · ⭐ · DFS+后序

## 01. 分析

直径 = 左子树深度 + 右子树深度。后序遍历时更新全局最大直径。

## 02. 代码

**Java**：
```java
int max = 0;
public int diameterOfBinaryTree(TreeNode root) {
    depth(root); return max;
}
int depth(TreeNode root) {
    if (root == null) return 0;
    int L = depth(root.left), R = depth(root.right);
    max = Math.max(max, L + R);
    return 1 + Math.max(L, R);
}
```

**Python**：
```python
def diameterOfBinaryTree(self, root):
    self.ans = 0
    def depth(node):
        if not node: return 0
        L, R = depth(node.left), depth(node.right)
        self.ans = max(self.ans, L + R)
        return 1 + max(L, R)
    depth(root)
    return self.ans
```

**复杂度**：O(N)/O(H)。
