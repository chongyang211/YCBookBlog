---
title: A016-无重复字符的最长子串
date: 2025-06-07 10:00:00
permalink: /pages/algo200-a016/
categories:
  - book
  - 通用技术的提升
  - 数据结构算法
  - 算法考核
  - 数组与字符串
tags:
  - 算法
  - 滑动窗口
  - 哈希表
  - LeetCode
author: 
  name: 杨充
  link: https://github.com/xugaoyi
---
# A016 无重复字符的最长子串（Longest Substring Without Repeating Characters）

> LeetCode 3 · ⭐⭐ · 滑动窗口+哈希集合
>
> 面试出镜率 Top 5 的滑动窗口经典题。不仅是模板，更考察对"窗口何时缩小"的理解。

---

## 01. 题目描述

找出不含有重复字符的最长子串的长度。

**示例**：

```
输入：s = "abcabcbb"
输出：3  ("abc")

输入：s = "pwwkew"
输出：3  ("wke")
```

---

## 02. 题目分析

### 滑动窗口+集合

右扩时检查是否重复。重复则不断左缩直到不重复。

```java
public int lengthOfLongestSubstring(String s) {
    Set<Character> set = new HashSet<>();
    int l = 0, max = 0;
    for (int r = 0; r < s.length(); r++) {
        while (set.contains(s.charAt(r)))
            set.remove(s.charAt(l++));
        set.add(s.charAt(r));
        max = Math.max(max, r - l + 1);
    }
    return max;
}
```

### 优化：字符数组代替 HashSet

由于字符集是 ASCII（128 或 256），用 `int[128]` 代替 `Set`，速度更快：

```java
public int lengthOfLongestSubstring(String s) {
    int[] last = new int[128];
    Arrays.fill(last, -1);
    int l = 0, max = 0;
    for (int r = 0; r < s.length(); r++) {
        char c = s.charAt(r);
        if (last[c] >= l) l = last[c] + 1;
        last[c] = r;
        max = Math.max(max, r - l + 1);
    }
    return max;
}
```

---

## 03. 代码

**Java**：

```java
public int lengthOfLongestSubstring(String s) {
    int[] last = new int[128];
    Arrays.fill(last, -1);
    int l = 0, max = 0;
    for (int r = 0; r < s.length(); r++) {
        char c = s.charAt(r);
        if (last[c] >= l) l = last[c] + 1;
        last[c] = r;
        max = Math.max(max, r - l + 1);
    }
    return max;
}
```

**Python**：

```python
def lengthOfLongestSubstring(self, s: str) -> int:
    last = {}
    l = ans = 0
    for r, c in enumerate(s):
        if c in last and last[c] >= l:
            l = last[c] + 1
        last[c] = r
        ans = max(ans, r - l + 1)
    return ans
```

**C++**：

```cpp
int lengthOfLongestSubstring(string s) {
    vector<int> last(128, -1);
    int l = 0, ans = 0;
    for (int r = 0; r < s.size(); r++) {
        if (last[s[r]] >= l) l = last[s[r]] + 1;
        last[s[r]] = r;
        ans = max(ans, r - l + 1);
    }
    return ans;
}
```

**复杂度**：时间 O(N)，空间 O(128)=O(1)。

**相关题目**：[A017 字母异位词](A017-找到字符串中所有字母异位词.md)、[A018 最小覆盖子串](A018-最小覆盖子串.md)
