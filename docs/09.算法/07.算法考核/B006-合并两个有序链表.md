---
title: B006-合并两个有序链表
date: 2025-06-07 10:00:00
permalink: /pages/algo200-b006/
categories:
  - book
  - 通用技术的提升
  - 数据结构算法
  - 算法考核
  - 链表
tags:
  - 算法
  - 链表
  - 归并
  - LeetCode
author: 
  name: 杨充
  link: https://github.com/xugaoyi
---
# B006 合并两个有序链表

> LeetCode 21 · ⭐ · 归并
>
> 归并排序的链表版。dummy 节点的经典用法。

---

## 01. 题目描述

合并两个升序链表，返回升序链表。

**示例**：

```
输入：1→2→4, 1→3→4
输出：1→1→2→3→4→4
```

---

## 02. 代码

**Java**：

```java
public ListNode mergeTwoLists(ListNode l1, ListNode l2) {
    ListNode dummy = new ListNode(0), cur = dummy;
    while (l1 != null && l2 != null) {
        if (l1.val < l2.val) { cur.next = l1; l1 = l1.next; }
        else { cur.next = l2; l2 = l2.next; }
        cur = cur.next;
    }
    cur.next = l1 != null ? l1 : l2;
    return dummy.next;
}
```

**Python**：

```python
def mergeTwoLists(self, l1, l2):
    dummy = cur = ListNode(0)
    while l1 and l2:
        if l1.val < l2.val: cur.next = l1; l1 = l1.next
        else: cur.next = l2; l2 = l2.next
        cur = cur.next
    cur.next = l1 or l2
    return dummy.next
```

**C++**：

```cpp
ListNode* mergeTwoLists(ListNode* l1, ListNode* l2) {
    ListNode dummy(0), *cur = &dummy;
    while (l1 && l2) {
        if (l1->val < l2->val) { cur->next = l1; l1 = l1->next; }
        else { cur->next = l2; l2 = l2->next; }
        cur = cur->next;
    }
    cur->next = l1 ? l1 : l2;
    return dummy.next;
}
```

**复杂度**：时间 O(m+n)，空间 O(1)。

**相关题目**：[B007 合并K个链表](B007-合并K个升序链表.md)
