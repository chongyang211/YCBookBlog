---
title: A001-两数之和
date: 2025-06-07 10:00:00
permalink: /pages/algo200-a001/
categories:
  - book
  - 通用技术的提升
  - 数据结构算法
  - 算法考核
  - 数组与字符串
tags:
  - 算法
  - 哈希表
  - LeetCode
author: 
  name: 杨充
  link: https://github.com/xugaoyi
---
# A001 两数之和（Two Sum）

> LeetCode 1 · ⭐ · 哈希映射
>
> LeetCode 第一题，面试出场率最高的题目。看似简单，却能考察哈希表的理解深度。

---

## 01. 题目描述

给定整数数组 `nums` 和整数目标值 `target`，在该数组中找出和为目标值的两个整数，返回它们的数组下标。

假设每种输入只会对应一个答案，且同一元素不能使用两次。可按任意顺序返回答案。

**示例**：

```
输入：nums = [2,7,11,15], target = 9
输出：[0,1]
```

```
输入：nums = [3,2,4], target = 6
输出：[1,2]
```

---

## 02. 题目分析

### 2.1 核心问题

对每个元素 `x`，需要找另一个元素 `y = target - x`。

### 2.2 解法演进

```
解法一：暴力双重循环 O(N²)
   ↓ 空间换时间
解法二：哈希表 O(N) / O(N)
   ↓ 一次遍历优化
解法二（优化）：边遍历边查哈希 O(N) / O(N)
```

---

## 03. 解法一：暴力枚举

```java
public int[] twoSum(int[] nums, int target) {
    for (int i = 0; i < nums.length; i++)
        for (int j = i + 1; j < nums.length; j++)
            if (nums[i] + nums[j] == target)
                return new int[]{i, j};
    return new int[0];
}
```

**复杂度**：时间 O(N²)，空间 O(1)。

---

## 04. 解法二：哈希表（最优）

### 思路

遍历时把元素的值和下标存入哈希表。对当前元素 `x`，查 `target - x` 是否已在表中。

```java
public int[] twoSum(int[] nums, int target) {
    Map<Integer, Integer> map = new HashMap<>();
    for (int i = 0; i < nums.length; i++) {
        int y = target - nums[i];
        if (map.containsKey(y))
            return new int[]{map.get(y), i};
        map.put(nums[i], i);
    }
    return new int[0];
}
```

**Python**：

```python
def twoSum(self, nums: List[int], target: int) -> List[int]:
    seen = {}
    for i, x in enumerate(nums):
        y = target - x
        if y in seen:
            return [seen[y], i]
        seen[x] = i
```

**C++**：

```cpp
vector<int> twoSum(vector<int>& nums, int target) {
    unordered_map<int, int> mp;
    for (int i = 0; i < nums.size(); i++) {
        int y = target - nums[i];
        if (mp.count(y)) return {mp[y], i};
        mp[nums[i]] = i;
    }
    return {};
}
```

**复杂度**：时间 O(N)，空间 O(N)。

---

## 05. 总结

| 解法 | 时间 | 空间 | 适用场景 |
|------|:---:|:---:|------|
| 暴力 | O(N²) | O(1) | 数据量小 |
| 哈希表 | O(N) | O(N) | **标准答案** |

**关键启示**：哈希表查找 O(1) 是空间换时间的经典案例。此题是三数之和、四数之和的基础。

**相关题目**：[A002 三数之和](A002-三数之和.md)、[I005 两数之和 II](I005-两数之和II.md)
