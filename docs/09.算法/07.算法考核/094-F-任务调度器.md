---
title: F009-任务调度器
date: 2025-06-07 10:00:00
permalink: /pages/algo200-f009/
categories: [book, 通用技术的提升, 数据结构算法, 算法考核, 堆与优先队列]
tags: [算法, 桶思想, LeetCode]
author: {name: 杨充, link: https://github.com/yangchong211}
---
# F009 任务调度器

> LeetCode 621 · ⭐⭐ · 桶思想

## 01. 题目

相同任务间隔 n 个时间单位。求最短执行时间。`tasks=["A","A","A","B","B","B"], n=2` → 8。

## 02. 分析

公式：`max(len(tasks), (maxFreq-1)*(n+1)+maxCount)`

## 03. 代码

**Java**：
```java
public int leastInterval(char[] tasks, int n) {
    int[] f=new int[26]; for(char c:tasks) f[c-'A']++;
    int maxF=0, maxCnt=0;
    for(int x:f){ if(x>maxF){ maxF=x; maxCnt=1; } else if(x==maxF) maxCnt++; }
    return Math.max(tasks.length, (maxF-1)*(n+1)+maxCnt);
}
```

**Python**：
```python
def leastInterval(self, tasks, n):
    from collections import Counter
    f=list(Counter(tasks).values())
    m=max(f); cnt=f.count(m)
    return max(len(tasks), (m-1)*(n+1)+cnt)
```

**复杂度**：O(N)/O(1)。
