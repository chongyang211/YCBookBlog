---
title: M011-N皇后
date: 2025-06-07 10:00:00
permalink: /pages/algo200-m011/
categories: [book, 通用技术的提升, 数据结构算法, 算法考核, 回溯算法]
tags: [算法, 回溯, LeetCode]
author: {name: 杨充, link: https://github.com/xugaoyi}
---
# M011 N 皇后

> LeetCode 51 · ⭐⭐⭐ · 回溯+约束

**Java**：
```java
public List<List<String>> solveNQueens(int n) {
    List<List<String>> res = new ArrayList<>();
    char[][] board = new char[n][n];
    for (char[] row : board) Arrays.fill(row, '.');
    backtrack(res, board, 0);
    return res;
}
void backtrack(List<List<String>> res, char[][] board, int row) {
    if (row == board.length) { List<String> list = new ArrayList<>(); for (char[] r:board) list.add(new String(r)); res.add(list); return; }
    for (int col = 0; col < board.length; col++) {
        if (!isValid(board, row, col)) continue;
        board[row][col] = 'Q';
        backtrack(res, board, row + 1);
        board[row][col] = '.';
    }
}
boolean isValid(char[][] board, int row, int col) {
    for (int i = 0; i < row; i++) if (board[i][col] == 'Q') return false;
    for (int i=row-1,j=col-1; i>=0&&j>=0; i--,j--) if(board[i][j]=='Q') return false;
    for (int i=row-1,j=col+1; i>=0&&j<board.length; i--,j++) if(board[i][j]=='Q') return false;
    return true;
}
```

**Python**：
```python
def solveNQueens(self, n):
    res = []
    board = [['.']*n for _ in range(n)]
    def valid(row, col):
        for r in range(row):
            if board[r][col] == 'Q': return False
            c1, c2 = col-(row-r), col+(row-r)
            if c1>=0 and board[r][c1]=='Q': return False
            if c2<n and board[r][c2]=='Q': return False
        return True
    def dfs(row):
        if row == n: res.append([''.join(r) for r in board]); return
        for col in range(n):
            if valid(row, col):
                board[row][col] = 'Q'
                dfs(row+1)
                board[row][col] = '.'
    dfs(0)
    return res
```

**复杂度**：O(N!)/O(N²)。
