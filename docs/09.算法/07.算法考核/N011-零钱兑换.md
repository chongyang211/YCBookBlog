---
title: N011-零钱兑换
date: 2025-06-07 10:00:00
permalink: /pages/algo200-n011/
categories: [book, 通用技术的提升, 数据结构算法, 算法考核, 动态规划]
tags: [算法, DP, 完全背包, LeetCode]
author: {name: 杨充, link: https://github.com/yangchong211}
---
# N011 零钱兑换（Coin Change）

> LeetCode 322 · ⭐⭐ · 完全背包
>
> 经典完全背包问题。正序遍历容量 = 物品可无限使用。

## 01. 题目

给定硬币面额 `coins` 和总金额 `amount`，最少需要几个硬币能凑出总金额。若凑不出返回 -1。

`coins = [1,2,5], amount = 11` → 3（5+5+1）

## 02. 题目分析

### 完全背包 vs 0-1背包

| | 0-1背包 | 完全背包 |
|------|:---:|:---:|
| 遍历顺序 | **倒序** j | **正序** j |
| 物品使用 | 最多一次 | 不限次数 |
| 代表题 | N013 分割等和子集 | N011 零钱兑换 |

### 状态转移

$$dp[i] = \min(dp[i],\; dp[i - coin] + 1)$$

`dp[0] = 0`，其余初始化为 `amount+1`（表示无法凑成）。

---

## 03. 代码

**Java**：
```java
public int coinChange(int[] coins, int amount) {
    int[] dp = new int[amount + 1];
    Arrays.fill(dp, amount + 1);
    dp[0] = 0;
    for (int coin : coins)
        for (int i = coin; i <= amount; i++)
            dp[i] = Math.min(dp[i], dp[i - coin] + 1);
    return dp[amount] > amount ? -1 : dp[amount];
}
```

**Python**：
```python
def coinChange(self, coins: List[int], amount: int) -> int:
    dp = [amount + 1] * (amount + 1)
    dp[0] = 0
    for c in coins:
        for i in range(c, amount + 1):
            dp[i] = min(dp[i], dp[i - c] + 1)
    return -1 if dp[amount] > amount else dp[amount]
```

**C++**：
```cpp
int coinChange(vector<int>& coins, int amount) {
    vector<int> dp(amount + 1, amount + 1);
    dp[0] = 0;
    for (int c : coins)
        for (int i = c; i <= amount; i++)
            dp[i] = min(dp[i], dp[i - c] + 1);
    return dp[amount] > amount ? -1 : dp[amount];
}
```

**复杂度**：O(N·amount) / O(amount)。

---

## 04. 总结

```mermaid
flowchart LR
    A["完全背包"] --> B["正序遍历容量"]
    B --> C["dp[i]=min(dp[i], dp[i-c]+1)"]
    C --> D["最少硬币数"]
```

**核心口诀**：**0-1背包倒着走，完全背包正着走。**

**相关题目**：[N012 零钱兑换II](N012-零钱兑换II.md)、[N013 分割等和子集](N013-分割等和子集.md)、[N015 完全平方数](N015-完全平方数.md)
