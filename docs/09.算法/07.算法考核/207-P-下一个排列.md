---
title: P007-下一个排列
date: 2025-06-07 10:00:00
permalink: /pages/algo200-p007/
categories: [book, 通用技术的提升, 数据结构算法, 算法考核, 数学与思维题]
tags: [算法, 字典序, LeetCode]
author: {name: 杨充, link: https://github.com/yangchong211}
---
# P007 下一个排列

> LeetCode 31 · ⭐⭐ · 字典序

## 01. 题目

`[1,2,3]` → `[1,3,2]`；`[3,2,1]` → `[1,2,3]`。求字典序中的下一个排列。

## 02. 分析

①找到第一个降序位置 i：`a[i]<a[i+1]` ②找到 i 右侧比 a[i] 大的最小数 j ③交换 i,j ④反转 i+1 到末尾。

## 03. 代码

**Java**：
```java
public void nextPermutation(int[] nums) {
    int n=nums.length, i=n-2;
    while(i>=0&&nums[i]>=nums[i+1]) i--;
    if(i>=0){ int j=n-1; while(nums[j]<=nums[i]) j--; swap(nums,i,j); }
    reverse(nums,i+1,n-1);
}
void reverse(int[] a,int l,int r){ while(l<r) swap(a,l++,r--); }
void swap(int[] a,int i,int j){ int t=a[i]; a[i]=a[j]; a[j]=t; }
```

**Python**：
```python
def nextPermutation(self, nums):
    i=len(nums)-2
    while i>=0 and nums[i]>=nums[i+1]: i-=1
    if i>=0:
        j=len(nums)-1
        while nums[j]<=nums[i]: j-=1
        nums[i],nums[j]=nums[j],nums[i]
    nums[i+1:]=reversed(nums[i+1:])
```

**复杂度**：O(N)/O(1)。

**相关**：[M001 全排列](M001-全排列.md)
