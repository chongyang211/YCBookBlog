---
title: J002-归并排序
date: 2025-06-07 10:00:00
permalink: /pages/algo200-j002/
categories: [book, 通用技术的提升, 数据结构算法, 算法考核, 排序算法]
tags: [算法, 排序, 归并排序]
author: {name: 杨充, link: https://github.com/yangchong211}
---
# J002 归并排序（Merge Sort）

> 手写必考 · ⭐⭐ · 分治+合并
>
> 稳定排序，O(NlogN) 时间，O(N) 空间。适合链表排序和外排序。

## 01. 题目

手写归并排序。

## 02. 题目分析

```
归并排序 = 分治
  分：递归将数组对半拆分
  治：合并两个有序子数组
  合：用临时数组归并，写回原数组
```

## 03. 代码

**Java**：
```java
void mergeSort(int[] nums, int l, int r) {
    if (l >= r) return;
    int m = l + (r - l) / 2;
    mergeSort(nums, l, m);
    mergeSort(nums, m + 1, r);
    merge(nums, l, m, r);
}
void merge(int[] nums, int l, int m, int r) {
    int[] tmp = new int[r - l + 1];
    int i = l, j = m + 1, k = 0;
    while (i <= m && j <= r)
        tmp[k++] = nums[i] <= nums[j] ? nums[i++] : nums[j++];
    while (i <= m) tmp[k++] = nums[i++];
    while (j <= r) tmp[k++] = nums[j++];
    System.arraycopy(tmp, 0, nums, l, tmp.length);
}
```

**Python**：
```python
def merge_sort(self, nums, l, r):
    if l >= r: return
    m = (l + r) // 2
    self.merge_sort(nums, l, m)
    self.merge_sort(nums, m + 1, r)
    tmp, i, j = [], l, m + 1
    while i <= m and j <= r:
        if nums[i] <= nums[j]: tmp.append(nums[i]); i += 1
        else: tmp.append(nums[j]); j += 1
    tmp.extend(nums[i:m+1]); tmp.extend(nums[j:r+1])
    nums[l:r+1] = tmp
```

**C++**：
```cpp
void merge(vector<int>& a, int l, int m, int r) {
    vector<int> tmp(r-l+1);
    int i=l, j=m+1, k=0;
    while(i<=m && j<=r) tmp[k++] = a[i]<=a[j] ? a[i++] : a[j++];
    while(i<=m) tmp[k++]=a[i++]; while(j<=r) tmp[k++]=a[j++];
    copy(tmp.begin(), tmp.end(), a.begin()+l);
}
void mergeSort(vector<int>& a, int l, int r) {
    if(l>=r) return; int m=l+(r-l)/2;
    mergeSort(a,l,m); mergeSort(a,m+1,r); merge(a,l,m,r);
}
```

**复杂度**：O(NlogN)/O(N)。稳定。

**相关**：[J001 快速排序](J001-快速排序.md)、[J004 逆序对](J004-数组中的逆序对.md)
