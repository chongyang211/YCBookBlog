---
title: O005-位1的个数
date: 2025-06-07 10:00:00
permalink: /pages/algo200-o005/
categories: [book, 通用技术的提升, 数据结构算法, 算法考核, 位运算]
tags: [算法, 位运算, LeetCode]
author: {name: 杨充, link: https://github.com/yangchong211}
---
# O005 位1的个数

> LeetCode 191 · ⭐ · `n & (n-1)`

## 01. 题目

统计二进制中1的个数。`11(1011)` → 3。

## 02. 分析

`n & (n-1)` 消除最低位1。循环直到n=0。

## 03. 代码

**Java**：
```java
public int hammingWeight(int n) { int c=0; while(n!=0){ n&=n-1; c++; } return c; }
```

**Python**：
```python
def hammingWeight(self, n: int) -> int: return bin(n).count('1')
```

**C++**：
```cpp
int hammingWeight(uint32_t n) { return __builtin_popcount(n); }
```

**复杂度**：O(k)/O(1)，k为1的个数。

**相关**：[O004 2的幂](O004-2的幂.md)、[O009 汉明距离](O009-汉明距离.md)
