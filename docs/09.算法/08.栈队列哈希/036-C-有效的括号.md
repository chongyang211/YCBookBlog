---
title: C001-有效的括号
date: 2025-06-07 10:00:00
permalink: /pages/algo200-c001/
categories:
  - book
  - 通用技术的提升
  - 数据结构算法
  - 算法考核
  - 栈与队列
tags:
  - 算法
  - 栈
  - LeetCode
author: 
  name: 杨充
  link: https://github.com/yangchong211
---
# C001 有效的括号

> LeetCode 20 · ⭐ · 栈匹配
>
> 栈的第一道题。LIFO 天然适合"最近未匹配"场景。

---

## 01. 题目描述

判断字符串括号是否有效：①左括号必须用同类型右括号闭合 ②正确顺序闭合。

**示例**：`"()[]{}"` → true, `"(]"` → false

---

## 02. 代码

**Java**：

```java
public boolean isValid(String s) {
    Deque<Character> stack = new ArrayDeque<>();
    for (char c : s.toCharArray()) {
        if (c == '(') stack.push(')');
        else if (c == '[') stack.push(']');
        else if (c == '{') stack.push('}');
        else if (stack.isEmpty() || stack.pop() != c) return false;
    }
    return stack.isEmpty();
}
```

**Python**：

```python
def isValid(self, s: str) -> bool:
    stack = []
    pair = {')': '(', ']': '[', '}': '{'}
    for c in s:
        if c in '([{': stack.append(c)
        elif not stack or stack.pop() != pair[c]: return False
    return not stack
```

**C++**：

```cpp
bool isValid(string s) {
    stack<char> st;
    for (char c : s) {
        if (c == '(') st.push(')');
        else if (c == '[') st.push(']');
        else if (c == '{') st.push('}');
        else if (st.empty() || st.top() != c) return false;
        else st.pop();
    }
    return st.empty();
}
```

**复杂度**：时间 O(N)，空间 O(N)。

**相关题目**：[C002 最长有效括号](C002-最长有效括号.md)
