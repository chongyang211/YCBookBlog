---
title: E006-二叉树的最大深度
date: 2025-06-07 10:00:00
permalink: /pages/algo200-e006/
categories:
  - book;通用技术的提升;数据结构算法;算法考核;树与二叉树
tags: [算法, 二叉树, DFS, LeetCode]
author: {name: 杨充, link: https://github.com/yangchong211}
---
# E006 二叉树的最大深度

> LeetCode 104 · ⭐ · DFS/BFS

## 01. 代码

**Java**：
```java
public int maxDepth(TreeNode root) {
    if (root == null) return 0;
    return 1 + Math.max(maxDepth(root.left), maxDepth(root.right));
}
```

**Python**：
```python
def maxDepth(self, root) -> int:
    if not root: return 0
    return 1 + max(self.maxDepth(root.left), self.maxDepth(root.right))
```

**BFS 写法**（按层计数，O(N)/O(N)）：每遍历一层 depth++。

**复杂度**：O(N)/O(H)。**相关**：[E009 直径](E009-二叉树的直径.md)
