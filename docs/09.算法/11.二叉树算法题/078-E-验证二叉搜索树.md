---
title: E013-验证二叉搜索树
date: 2025-06-07 10:00:00
permalink: /pages/algo200-e013/
categories: [book, 通用技术的提升, 数据结构算法, 算法考核, 树与二叉树]
tags: [算法, BST, 递归, LeetCode]
author: {name: 杨充, link: https://github.com/yangchong211}
---
# E013 验证二叉搜索树

> LeetCode 98 · ⭐⭐ · 范围约束/中序

## 01. 分析

方法一（范围约束）：递归传 (min, max) 范围。左子树 max=root.val，右子树 min=root.val。
方法二（中序）：BST 中序递增，记录前驱比较。

## 02. 代码

**Java**：
```java
public boolean isValidBST(TreeNode root) {
    return validate(root, Long.MIN_VALUE, Long.MAX_VALUE);
}
boolean validate(TreeNode node, long min, long max) {
    if (node == null) return true;
    if (node.val <= min || node.val >= max) return false;
    return validate(node.left, min, node.val) && validate(node.right, node.val, max);
}
```

**Python**：
```python
def isValidBST(self, root):
    def valid(node, lo, hi):
        if not node: return True
        if node.val <= lo or node.val >= hi: return False
        return valid(node.left, lo, node.val) and valid(node.right, node.val, hi)
    return valid(root, float('-inf'), float('inf'))
```

**复杂度**：O(N)/O(H)。
