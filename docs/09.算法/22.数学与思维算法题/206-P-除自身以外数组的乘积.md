---
title: P006-除自身以外数组的乘积
date: 2025-06-07 10:00:00
permalink: /pages/algo200-p006/
categories: [book, 通用技术的提升, 数据结构算法, 算法考核, 数学与思维题]
tags: [算法, 前缀积, LeetCode]
author: {name: 杨充, link: https://github.com/yangchong211}
---
# P006 除自身以外数组的乘积

> LeetCode 238 · ⭐⭐ · 前缀积+后缀积

## 01. 题目

`[1,2,3,4]` → `[24,12,8,6]`。不能使用除法，O(N)。

## 02. 分析

第一遍遍历存前缀积，第二遍从右往左乘后缀积。

## 03. 代码

**Java**：
```java
public int[] productExceptSelf(int[] nums) {
    int n=nums.length; int[] res=new int[n]; res[0]=1;
    for(int i=1;i<n;i++) res[i]=res[i-1]*nums[i-1];
    int right=1;
    for(int i=n-1;i>=0;i--){ res[i]*=right; right*=nums[i]; }
    return res;
}
```

**Python**：
```python
def productExceptSelf(self, nums):
    n=len(nums); res=[1]*n
    for i in range(1,n): res[i]=res[i-1]*nums[i-1]
    right=1
    for i in range(n-1,-1,-1): res[i]*=right; right*=nums[i]
    return res
```

**复杂度**：O(N)/O(1)（输出数组不计）。

**相关**：[P005 多数元素](P005-多数元素.md)
