---
title: Python 代码规范
date: 2020-06-20 14:00:00
permalink: /pages/python-style-guide/
categories:
  - 技术
  - 代码规范
tags:
  - Python
  - 代码规范
  - PEP8
author:
  name: 杨充
  link: https://github.com/yangchong211
description: Python 编码规范，基于 PEP8，涵盖命名、格式、类型注解、异常处理等
---
# Python 代码规范

基于 [PEP 8](https://peps.python.org/pep-0008/) 和 [Google Python Style Guide](https://google.github.io/styleguide/pyguide.html)。

## 目录

- [01.命名规范](#01命名规范)
  - [1.1 命名总表](#11-命名总表)
  - [1.2 命名反模式](#12-命名反模式)
- [02.代码格式](#02代码格式)
  - [2.1 缩进与空格](#21-缩进与空格)
  - [2.2 import 顺序](#22-import-顺序)
  - [2.3 空行与换行](#23-空行与换行)
- [03.类型注解](#03类型注解)
- [04.字符串与文档](#04字符串与文档)
- [05.比较与条件](#05比较与条件)
- [06.异常处理](#06异常处理)
- [07.列表与字典推导](#07列表与字典推导)
- [08.上下文管理器](#08上下文管理器)
- [09.函数规范](#09函数规范)
- [10.类设计规范](#10类设计规范)
- [11.并发与异步](#11并发与异步)
- [12.常见反模式](#12常见反模式)
- [13.工具链与自动化](#13工具链与自动化)
- [14.代码审查清单](#14代码审查清单)

## 01.命名规范

### 1.1 命名总表

| 类型 | 规范 | 示例 |
|:-----|:-----|:-----|
| 模块/包 | 小写 + 下划线 | `my_module`, `data_utils` |
| 类 | 大驼峰 | `MyClass`, `DataProcessor` |
| 异常类 | 大驼峰 + `Error` 后缀 | `ValidationError` |
| 函数/方法 | 小写 + 下划线 | `get_user()`, `calculate_total` |
| 变量 | 小写 + 下划线 | `user_count`, `max_retries` |
| 常量 | 全大写 + 下划线 | `MAX_SIZE`, `DEFAULT_TIMEOUT` |
| 私有成员 | 单下划线前缀 | `_internal_method`, `_private_var` |
| 内部私有 | 双下划线前缀 | `__very_private`（name mangling） |
| 魔术方法 | 双下划线前后 | `__init__`, `__str__` |

```python
# ✅ 正确
class UserProfile:
    MAX_NAME_LENGTH = 50

    def __init__(self, user_id: int):
        self._id = user_id
        self.name: str = ""

    def get_display_name(self) -> str:
        return self.name or f"User_{self._id}"
```

### 1.2 命名反模式

| 反模式 | 示例 | 改进 |
|:-------|:-----|:-----|
| 单字母 | `u`, `f`, `x` | `user`, `file_path` |
| 拼音 | `dian_hua` | `phone_number` |
| 类型冗余 | `name_str`, `age_int` | `name`, `age` |
| 否定命名 | `is_not_empty` | `not items`（直接用 `if not items`） |

## 02.代码格式

### 2.1 缩进与空格

```python
# ✅ 4 个空格缩进（不要用 Tab）
def long_function_name(
    var_one: int,
    var_two: str,
    var_three: float,
) -> dict:
    result = {
        "one": var_one,
        "two": var_two,
    }
    return result

# ✅ 运算符两侧加空格
count = a + b * c
is_valid = (age >= 18) and (name is not None)

# ✅ 逗号后加空格
items = [1, 2, 3]
config = {"host": "localhost", "port": 8080}

# ❌ 括号内不加空格
items = [ 1, 2, 3 ]    # 错误
data = {"a" : 1}        # 错误
```

### 2.2 import 顺序

```python
# ✅ 分组顺序：标准库 → 第三方 → 本地
import os
import sys
from typing import Optional

import requests
from flask import Flask

from myapp.utils import helper
```

### 2.3 空行与换行

```python
# ✅ 类之间 2 空行，方法之间 1 空行
class FirstClass:
    def method_one(self):
        pass


class SecondClass:
    def method_one(self):
        pass

    # ✅ 方法内逻辑分组用 1 空行
    def process(self, data):
        cleaned = self._clean(data)

        validated = self._validate(cleaned)
        return self._execute(validated)
```

## 03.类型注解

```python
from typing import Optional, List, Dict, Union, Any

# ✅ 函数注解
def find_user(user_id: int) -> Optional[Dict[str, Any]]:
    """查找用户，找不到返回 None"""
    ...

# ✅ 变量注解
users: List[str] = []
config: Dict[str, Union[str, int]] = {}

# ✅ Python 3.10+ 内置泛型语法
def process(items: list[str]) -> dict[str, int]:
    ...

# ✅ 类属性注解
class DataStore:
    cache: Dict[str, bytes]
    max_size: int = 1024 * 1024

    def __init__(self) -> None:
        self.cache = {}

# ❌ 不要忽略类型注解
def find_user(user_id):  # 缺少注解
    ...
```

## 04.字符串与文档

```python
# ✅ 优先使用 f-string（Python 3.6+）
name = "World"
msg = f"Hello, {name}! Count: {count + 1}"

# ❌ 旧的 % 格式化
msg = "Hello, %s! Count: %d" % (name, count)

# ✅ 多行字符串
query = """
    SELECT id, name, email
    FROM users
    WHERE active = 1
    ORDER BY name
"""

# ✅ 长字符串拼接
msg = (
    f"User {user.name} has {len(orders)} orders "
    f"with total amount {total:.2f}"
)

# ✅ Docstring（Google 风格）
def calculate(price: float, tax: float = 0.1) -> float:
    """计算含税价格。

    Args:
        price: 原始价格
        tax: 税率，默认 0.1

    Returns:
        含税总价

    Raises:
        ValueError: price 为负数时
    """
    if price < 0:
        raise ValueError("price must be non-negative")
    return price * (1 + tax)
```

## 05.比较与条件

```python
# ✅ 与 None 比较用 is
if user is None:
    return
if data is not None:
    process(data)

# ✅ 空序列判断（Python 的"假值"）
if not items:            # 而非 len(items) == 0
    return []
if not user_name:        # 空字符串
    user_name = "Unknown"

# ✅ 布尔值直接判断
if is_active:            # 而非 is_active == True
    ...

# ✅ 三目运算符
status = "active" if user.is_active else "inactive"

# ✅ 多条件用 in
if status in ("active", "pending"):
    ...

# ❌ 避免
if status == "active" or status == "pending":
    ...

# ✅ 链式比较
if 0 <= score <= 100:
    grade = "valid"
```

## 06.异常处理

```python
# ✅ 捕获具体异常
try:
    result = api.call()
except ConnectionError as e:
    logger.error(f"API 连接失败: {e}")
    raise
except ValueError:
    return default_value
finally:
    cleanup()

# ❌ 不要捕获所有异常（除非确实需要）
try:
    ...
except Exception:          # 太宽泛
    pass                   # 吞掉异常更危险

# ✅ 自定义异常
class ValidationError(Exception):
    """数据校验异常。"""
    def __init__(self, field: str, msg: str) -> None:
        self.field = field
        super().__init__(f"{field}: {msg}")

# ✅ raise from 保留异常链
try:
    process_data()
except ValueError as e:
    raise AppError("处理失败") from e
```

## 07.列表与字典推导

```python
# ✅ 列表推导（简单场景）
squares = [x * x for x in range(10)]
active_users = [u for u in users if u.is_active]

# ✅ 字典推导
name_map = {u.id: u.name for u in users}

# ✅ 集合推导
unique_tags = {tag for article in articles for tag in article.tags}

# ✅ 生成器表达式（大数据量时用）
total = sum(x * x for x in big_dataset)  # 不创建中间列表

# ❌ 推导嵌套太深（> 2 层）
# 拆分为多步更清晰
```

## 08.上下文管理器

```python
# ✅ with 语句自动管理资源
with open("data.json", "r") as f:
    content = f.read()

# ✅ 嵌套上下文管理器
with open("src.txt") as src, open("dst.txt", "w") as dst:
    dst.write(src.read())

# ✅ 自定义上下文管理器
from contextlib import contextmanager

@contextmanager
def timer(name: str):
    start = time.time()
    yield
    elapsed = time.time() - start
    logger.info(f"{name}: {elapsed:.2f}s")

with timer("计算阶段"):
    heavy_computation()
```

## 09.函数规范

```python
# ✅ 参数默认值用 None 而非可变对象
def add_item(item: str, items: list[str] | None = None) -> list[str]:
    if items is None:
        items = []
    items.append(item)
    return items

# ❌ 可变默认参数（经典陷阱）
def add_item(item, items=[]):  # 多次调用共享同一个 list！
    items.append(item)
    return items

# ✅ 仅关键字参数（*, 之后的参数必须用关键字传递）
def connect(host: str, port: int, *, timeout: int = 30, ssl: bool = True):
    ...

connect("localhost", 8080, timeout=60)      # ✅
connect("localhost", 8080, 60, True)         # ❌ 不清晰

# ✅ 函数体不超过 50 行
# ✅ 参数不超过 5 个，超过则封装为 dataclass
```

## 10.类设计规范

```python
from dataclasses import dataclass

# ✅ 数据容器优先用 dataclass
@dataclass
class User:
    id: int
    name: str
    email: str | None = None

# ✅ 属性用 @property（不暴露内部实现）
class Circle:
    def __init__(self, radius: float) -> None:
        self._radius = radius

    @property
    def radius(self) -> float:
        return self._radius

    @radius.setter
    def radius(self, value: float) -> None:
        if value < 0:
            raise ValueError("radius must be >= 0")
        self._radius = value

# ✅ 静态方法 vs 类方法
class DateUtils:
    @staticmethod
    def is_weekend(date): ...          # 不依赖类

    @classmethod
    def from_string(cls, s: str): ...  # 依赖类（常用作工厂）
```

## 11.并发与异步

```python
import asyncio

# ✅ async/await 异步 IO
async def fetch_all(urls: list[str]) -> list[dict]:
    async with aiohttp.ClientSession() as session:
        tasks = [fetch(session, url) for url in urls]
        return await asyncio.gather(*tasks)

# ✅ 线程池执行 CPU 密集型任务
def cpu_intensive(data):
    ...

result = await asyncio.to_thread(cpu_intensive, large_data)

# ✅ 超时控制
async def fetch_with_timeout(url: str, timeout: float = 10):
    async with asyncio.timeout(timeout):
        return await fetch(url)
```

## 12.常见反模式

| 反模式 | 问题 | 改进 |
|:-------|:-----|:-----|
| `from module import *` | 污染命名空间 | 显式导入 |
| 裸 `except:` | 吞掉 KeyboardInterrupt 等 | `except Exception:` 或具体异常 |
| 循环中拼接字符串 | O(n²) 性能 | `"".join(list)` |
| 函数参数可变默认值 | 多次调用共享对象 | 默认值用 None |
| `if x == True` | 冗余 | `if x` |
| `type(obj) == type(...)` | 不认子类 | `isinstance(obj, ...)` |

## 13.工具链与自动化

```ini
# pyproject.toml
[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B"]

[tool.mypy]
python_version = "3.11"
strict = true
```

| 工具 | 用途 |
|:-----|:-----|
| `ruff` | 代码格式 + lint（替代 flake8 + isort） |
| `mypy` | 类型检查 |
| `black` | 代码格式化（与 ruff 选一） |
| `pytest` | 单元测试 |
| `pre-commit` | Git 提交前自动检查 |

```bash
# 安装 pre-commit
pip install pre-commit
# 配置 .pre-commit-config.yaml 后在每次 git commit 时自动运行
pre-commit install
```

## 14.代码审查清单

```markdown
## 命名
- [ ] 变量/函数小写下划线，类大驼峰，常量全大写
- [ ] 不存在单字母、拼音、含义模糊的命名

## 格式
- [ ] 4 空格缩进，无 Tab
- [ ] import 顺序正确（标准库→第三方→本地）
- [ ] 单行不超过 120 字符

## 类型
- [ ] 公开函数有类型注解
- [ ] 复杂类型用 typing 模块（Optional/List/Dict/Union）

## 逻辑
- [ ] 与 None 比较用 is
- [ ] 空序列用 `if not items` 而非 `len() == 0`
- [ ] 异常捕获具体类型，无裸 except

## 健壮性
- [ ] 函数默认参数不用可变对象
- [ ] 文件操作用 with 语句
- [ ] 敏感信息不硬编码

## 性能
- [ ] 循环外无数据库/网络调用
- [ ] 大数据量用生成器而非列表
- [ ] 字符串拼接用 join 而非 +
```
