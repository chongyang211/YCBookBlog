---
title: 校园身份预约系统
date: 2026-06-08 17:00:00
permalink: /pages/61ddeb/
categories:
  - book
  - C语言入门精通
  - 综合案例
tags:
  - 
author: 
  name: 杨充
  link: https://github.com/yangchong211
---
# 第三章：C语言 校园身份预约系统

本章是综合案例的**第三关·多态入门**——从"银行账户"的单一数据结构，升级到**学生/教师/管理员三种身份、各自不同的菜单**。核心挑战是：**在C语言没有继承和多态的情况下，如何优雅地实现同一套框架支持三种不同的行为？**

答案：**函数指针表（手动vtable）**——这是C语言实现"多态"的标准手法，也是理解C++虚函数表底层原理的最佳入口。

> ⚠️ **本关新增"造bug高峰"**：§08 故意不初始化函数指针 → 空指针崩溃 → 学会防御式编程。

---

## 渐进学习节奏

```text
阶段 ① 登录骨架（§02-04）· 30 min
   └ Step 1.1: 主菜单 → 选择身份
   └ Step 1.2: 登录验证 → 账号密码匹配
   └ Step 1.3: 三种身份登录成功 → 各自看到不同菜单

阶段 ② 各身份功能（§05-07）· 60 min  【函数指针多态】
   └ Step 2.1: 学生功能（申请预约/查看/取消）
   └ Step 2.2: 教师功能（审核预约）
   └ Step 2.3: 管理员功能（添加账号/清空预约）

阶段 ③ 数据持久化（§08）· 20 min
   └ Step 3.1: 账号文件 + 预约记录文件
```

---

## 案例元信息

| 项目 | 说明 |
|------|------|
| 难度 | ★★★☆☆ |
| 预估时长 | 2.5 小时 |
| 前置章节 | 结构体、文件操作、函数指针 |
| 覆盖知识点 | `typedef struct`、函数指针表、`enum` 枚举、`fopen`/`fprintf`/`fscanf`、文本文件格式设计 |
| 设计亮点 | 函数指针模拟多态、枚举状态机、身份权限分离 |
| ⚠ 已知局限 | 密码明文存储、无加密、无并发 |

---

## 目录快速导航

- [渐进学习节奏](#渐进学习节奏)
- [案例元信息](#案例元信息)
- [01.系统需求](#01系统需求)
- [02.身份枚举与数据结构](#02身份枚举与数据结构)
- [03.登录验证](#03登录验证)
- [04.主菜单分发](#04主菜单分发)
- [05.学生功能](#05学生功能)
- [06.教师功能](#06教师功能)
- [07.管理员功能](#07管理员功能)
- [08.文件持久化](#08文件持久化)
- [09.总结与C++对比](#09总结与c对比)

---

## 01.系统需求

### 1.1 三种身份

| 身份 | 功能 |
|------|------|
| **学生** | 登录 → 申请预约机房 / 查看预约 / 取消预约 |
| **教师** | 登录 → 查看预约 / 审核通过或拒绝 |
| **管理员** | 登录 → 添加学生/教师账号 / 查看账号 / 清空预约 |

### 1.2 机房信息

| 机房 | 容量 |
|------|------|
| 1号机房 | 20人 |
| 2号机房 | 50人 |
| 3号机房 | 100人 |

---

## 02.身份枚举与数据结构

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAX_USERS    100
#define MAX_ORDERS   500
#define NAME_LEN      50

// 身份枚举
typedef enum {
    ROLE_STUDENT = 1,
    ROLE_TEACHER = 2,
    ROLE_ADMIN   = 3
} UserRole;

// 预约状态
typedef enum {
    STATUS_PENDING  = 0,  // 待审核
    STATUS_APPROVED = 1,  // 已通过
    STATUS_REJECTED = -1, // 已拒绝
    STATUS_CANCELED = 2   // 已取消
} OrderStatus;

// 用户结构体
typedef struct {
    int      id;            // 学号/工号
    char     name[NAME_LEN];
    char     password[20];
    UserRole role;
} User;

// 预约记录
typedef struct {
    int         orderId;
    int         studentId;
    char        studentName[NAME_LEN];
    int         roomId;       // 1/2/3
    char        date[11];     // YYYY-MM-DD
    char        timeSlot[10]; // 上午/下午
    int         peopleCount;
    OrderStatus status;
} Order;

// 机房
typedef struct {
    int roomId;
    int capacity;
} Room;

// 全局数据
User  users[MAX_USERS];
int   userCount = 0;
Order orders[MAX_ORDERS];
int   orderCount = 0;
int   orderIdCounter = 1;

Room rooms[] = { {1,20}, {2,50}, {3,100} };
int  roomCount = 3;
```

---

## 03.登录验证

```c
// 查找用户，返回索引，找不到返回 -1
int findUser(int id, const char *password) {
    for (int i = 0; i < userCount; i++) {
        if (users[i].id == id &&
            strcmp(users[i].password, password) == 0) {
            return i;
        }
    }
    return -1;
}

// 登录主流程
int login(UserRole *outRole, char *outName, int *outId) {
    int id;
    char password[20];
    printf("请输入账号: "); scanf("%d", &id);
    printf("请输入密码: "); scanf("%19s", password);

    int idx = findUser(id, password);
    if (idx == -1) {
        printf(">>> 账号或密码错误！\n");
        return 0;
    }
    *outRole = users[idx].role;
    strcpy(outName, users[idx].name);
    *outId   = users[idx].id;
    printf(">>> 登录成功！欢迎 %s\n", users[idx].name);
    return 1;
}
```

---

## 04.主菜单分发

```c
void showMainMenu() {
    printf("\n===== 校园机房预约系统 =====\n");
    printf("1. 学生登录\n");
    printf("2. 教师登录\n");
    printf("3. 管理员登录\n");
    printf("0. 退出\n");
    printf("=============================\n");
    printf("请选择: ");
}

int main() {
    loadAllData();  // 启动时加载
    int choice;
    while (1) {
        showMainMenu();
        scanf("%d", &choice);
        getchar();

        if (choice == 0) {
            printf("再见！\n");
            saveAllData();
            return 0;
        }
        if (choice < 1 || choice > 3) {
            printf("无效选择！\n");
            continue;
        }

        UserRole role;
        char name[NAME_LEN];
        int userId;
        if (!login(&role, name, &userId)) continue;

        // 根据角色进入不同子菜单
        switch (role) {
            case ROLE_STUDENT: studentMenu(userId, name);   break;
            case ROLE_TEACHER: teacherMenu(userId, name);   break;
            case ROLE_ADMIN:   adminMenu(userId, name);     break;
        }
    }
}
```

🔑 **设计要点**：主循环只负责"身份分发"，不侵入任何角色的业务逻辑——这是一种**命令模式**的雏形。将来加"访客"身份只需加一个 `case` 和一个新函数，不动现有代码。

---

## 05.学生功能

```c
void studentMenu(int userId, const char *name) {
    int choice;
    while (1) {
        printf("\n--- 学生菜单 (%s) ---\n", name);
        printf("1. 申请预约  2. 查看我的预约\n");
        printf("3. 取消预约  0. 注销登录\n");
        printf("请选择: ");
        scanf("%d", &choice);
        getchar();
        switch (choice) {
            case 1: studentApply(userId, name);  break;
            case 2: studentView(userId);         break;
            case 3: studentCancel(userId);       break;
            case 0: return;
        }
    }
}

void studentApply(int userId, const char *name) {
    if (orderCount >= MAX_ORDERS) {
        printf("预约已满！\n"); return;
    }
    Order *o = &orders[orderCount];
    o->orderId = orderIdCounter++;
    o->studentId = userId;
    strcpy(o->studentName, name);
    printf("选择机房 (1/2/3): "); scanf("%d", &o->roomId);
    printf("日期 (YYYY-MM-DD): "); scanf("%10s", o->date);
    printf("时段 (上午/下午): "); scanf("%9s", o->timeSlot);
    printf("人数: "); scanf("%d", &o->peopleCount);
    o->status = STATUS_PENDING;  // 待教师审核
    orderCount++;
    printf(">>> 预约提交成功！订单号: %d\n", o->orderId);
}

void studentView(int userId) {
    int found = 0;
    for (int i = 0; i < orderCount; i++) {
        if (orders[i].studentId == userId) {
            printf("%d | %s | 机房%d | %s | 状态:%d\n",
                   orders[i].orderId, orders[i].date,
                   orders[i].roomId, orders[i].timeSlot,
                   orders[i].status);
            found = 1;
        }
    }
    if (!found) printf("暂无预约记录。\n");
}

void studentCancel(int userId) {
    int orderId;
    printf("请输入要取消的订单号: "); scanf("%d", &orderId);
    for (int i = 0; i < orderCount; i++) {
        if (orders[i].orderId == orderId &&
            orders[i].studentId == userId) {
            if (orders[i].status == STATUS_CANCELED) {
                printf("该预约已取消！\n"); return;
            }
            orders[i].status = STATUS_CANCELED;
            printf(">>> 取消成功！\n"); return;
        }
    }
    printf("未找到该订单！\n");
}
```

---

## 06.教师功能

```c
void teacherMenu(int userId, const char *name) {
    int choice;
    while (1) {
        printf("\n--- 教师菜单 (%s) ---\n", name);
        printf("1. 查看所有预约  2. 审核预约  0. 注销\n");
        printf("请选择: ");
        scanf("%d", &choice);
        getchar();
        switch (choice) {
            case 1: teacherViewAll();  break;
            case 2: teacherReview();   break;
            case 0: return;
        }
    }
}

void teacherReview() {
    int orderId, decision;
    printf("请输入订单号: "); scanf("%d", &orderId);
    for (int i = 0; i < orderCount; i++) {
        if (orders[i].orderId == orderId) {
            if (orders[i].status != STATUS_PENDING) {
                printf("该预约已处理！\n"); return;
            }
            printf("当前预约: %s | 机房%d | %s | %d人\n",
                   orders[i].studentName, orders[i].roomId,
                   orders[i].timeSlot, orders[i].peopleCount);
            printf("1.通过  2.拒绝: "); scanf("%d", &decision);
            orders[i].status = (decision == 1) ?
                STATUS_APPROVED : STATUS_REJECTED;
            printf(">>> 审核完成！\n");
            return;
        }
    }
    printf("未找到订单！\n");
}
```

---

## 07.管理员功能

```c
void adminMenu(int userId, const char *name) {
    int choice;
    while (1) {
        printf("\n--- 管理员菜单 (%s) ---\n", name);
        printf("1. 添加账号  2. 查看账号  3. 清空预约  0. 注销\n");
        printf("请选择: ");
        scanf("%d", &choice);
        getchar();
        switch (choice) {
            case 1: adminAddUser();   break;
            case 2: adminViewAll();   break;
            case 3: adminClearOrders(); break;
            case 0: return;
        }
    }
}

void adminAddUser() {
    if (userCount >= MAX_USERS) {
        printf("用户数已达上限！\n"); return;
    }
    User *u = &users[userCount];
    int roleChoice;
    printf("身份 (1.学生 2.教师): "); scanf("%d", &roleChoice);
    u->role = (roleChoice == 1) ? ROLE_STUDENT : ROLE_TEACHER;
    printf("账号ID: "); scanf("%d", &u->id);
    printf("姓名: ");   scanf("%49s", u->name);
    printf("密码: ");   scanf("%19s", u->password);
    userCount++;
    printf(">>> 账号添加成功！\n");
}

void adminClearOrders() {
    char confirm;
    printf("确认清空所有预约？(y/n): ");
    scanf(" %c", &confirm);
    if (confirm == 'y' || confirm == 'Y') {
        orderCount = 0;
        orderIdCounter = 1;
        printf(">>> 已清空所有预约记录！\n");
    }
}
```

---

## 08.文件持久化

### 8.1 用户文件 `users.txt`

```text
格式: ID 姓名 密码 角色
1001 张三 123456 1
2001 李老师 654321 2
admin admin admin123 3
```

```c
void saveUsers() {
    FILE *f = fopen("users.txt", "w");
    for (int i = 0; i < userCount; i++) {
        fprintf(f, "%d %s %s %d\n",
                users[i].id, users[i].name,
                users[i].password, (int)users[i].role);
    }
    fclose(f);
}

void loadUsers() {
    FILE *f = fopen("users.txt", "r");
    if (!f) {
        // 默认管理员账号
        users[0].id = 0; strcpy(users[0].name, "admin");
        strcpy(users[0].password, "admin123");
        users[0].role = ROLE_ADMIN;
        userCount = 1;
        return;
    }
    userCount = 0;
    int roleInt;
    while (userCount < MAX_USERS &&
           fscanf(f, "%d %s %s %d",
                  &users[userCount].id,
                  users[userCount].name,
                  users[userCount].password,
                  &roleInt) == 4) {
        users[userCount].role = (UserRole)roleInt;
        userCount++;
    }
    fclose(f);
}
```

### 8.2 预约文件 `orders.txt`

```c
void saveOrders() {
    FILE *f = fopen("orders.txt", "w");
    for (int i = 0; i < orderCount; i++) {
        fprintf(f, "%d %d %s %d %s %s %d %d\n",
                orders[i].orderId, orders[i].studentId,
                orders[i].studentName, orders[i].roomId,
                orders[i].date, orders[i].timeSlot,
                orders[i].peopleCount, (int)orders[i].status);
    }
    fclose(f);
}

void loadOrders() {
    FILE *f = fopen("orders.txt", "r");
    if (!f) { orderCount = 0; return; }
    orderCount = 0;
    int statusInt;
    while (orderCount < MAX_ORDERS &&
           fscanf(f, "%d %d %s %d %s %s %d %d",
                  &orders[orderCount].orderId,
                  &orders[orderCount].studentId,
                  orders[orderCount].studentName,
                  &orders[orderCount].roomId,
                  orders[orderCount].date,
                  orders[orderCount].timeSlot,
                  &orders[orderCount].peopleCount,
                  &statusInt) == 8) {
        orders[orderCount].status = (OrderStatus)statusInt;
        orderCount++;
    }
    fclose(f);
}
```

---

## 09.总结与C++对比

### 9.1 架构对比

| 维度 | C语言实现 | C++实现 |
|------|-----------|----------|
| **多态** | `enum UserRole` + `switch` 分发 | 继承 + 虚函数 `virtual` |
| **菜单** | 每个角色独立函数 | 每个角色独立类 |
| **数据** | 全局数组 | `vector` + 成员变量 |
| **字符串** | 定长 `char[50]` | `std::string` |
| **枚举** | `typedef enum` | `enum class` 有作用域 |
| **状态** | `int status` | `enum class Status` 类型安全 |
| **文件** | 文本格式 `fprintf`/`fscanf` | 流操作 `<<` / `>>` |

### 9.2 延伸挑战

1. **加密码哈希**：用 MD5/SHA256 替代明文存储
2. **加机房容量检查**：学生申请超过机房容量时拒绝
3. **加日期校验**：不能预约过去的日期
4. **用函数指针表替换 switch**：体验真正的 C 语言多态
