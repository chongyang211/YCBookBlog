# 1.2 整型与位运算原理

> 上一篇我们看到，"数据编码"本质上是给现实信息找到一种二进制表达。但当我们真正落到机器字长（32 位、64 位）时，会发现一个绕不过去的核心问题：**为什么计算机选择"补码"而不是"原码"来表示整数？为什么 `int` 溢出后会"绕回"？为什么 `-1` 的二进制看起来全是 1？**
>
> 本章从一个真实工作中遇到的"诡异 BUG"出发，深入挖掘整型设计背后的硬件约束、数学美感与工程权衡，并把"位运算"这种看似底层的技巧，还原成一种通用的"集合论 + 状态压缩"思想。

## 一、问题引入：一个由整数引发的线上事故

### 1.1 真实案例：跨天统计为什么会"跳过一秒"
（占位：以 `(start + end) / 2` 求中点导致溢出的经典 BUG 引入；或线上 `int * 1000` 毫秒时间戳溢出案例）

### 1.2 矛盾点浮现：人脑的"数"与机器的"数"
（占位：揭示无符号、有符号、补码、溢出绕回这些与"数学直觉"相悖的现象）

## 二、整型表示的三次范式跃迁

### 2.1 第一性原理：固定位宽的代价
### 2.2 原码方案：直觉但有"两个零"
### 2.3 反码方案：解决了一部分，却带来加法的不对称
### 2.4 补码方案：让"减法"变成"加法"的伟大发明
### 2.5 从硬件电路视角理解补码：为什么它最省门电路

## 三、整数溢出与边界陷阱

### 3.1 有符号溢出 = 未定义行为？
### 3.2 无符号溢出的"模 2^n"语义
### 3.3 编译器优化是如何利用 UB 的
### 3.4 安全整数（Saturating / Checked / Wrapping）

## 四、位运算的本质：信息的最小操作单元

### 4.1 与或非异或：从布尔代数到集合论
### 4.2 位运算的"状态压缩"思想
### 4.3 经典技巧背后的原理（清零最低位 1、判断 2 的幂、bitmap）
### 4.4 位运算在工程中的真实威力（权限位、Bloom Filter、压缩索引）

## 五、字节序与对齐：跨越机器边界的难题

### 5.1 大小端：为什么会有两种字节序
### 5.2 网络字节序的统一约定
### 5.3 内存对齐：硬件给软件的"无形税"

## 六、总结与思考

