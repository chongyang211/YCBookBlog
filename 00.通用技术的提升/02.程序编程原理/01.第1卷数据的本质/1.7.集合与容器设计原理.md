# 1.7 集合与容器设计原理

> 前几章我们讨论了"单个数据"的表示——整型、浮点、字符串、引用。但工程世界几乎没有"孤立的数据"，更多的是**一组数据的集合**：用户列表、订单字典、关注者集合、消息队列……
>
> 集合容器看似只是 `List`、`Map`、`Set`，但它们背后藏着计算机科学最深刻的一组权衡：**时间 vs 空间、连续 vs 离散、有序 vs 无序、读多 vs 写多**。本章从一次真实的"性能踩坑"出发，回到第一性原理，重新审视容器的本质。

## 一、问题引入：一个 ArrayList 把服务打挂了

### 1.1 真实案例：百万级数据 contains 调用的雪崩
### 1.2 矛盾点浮现：为什么"看起来一样的容器"性能差 1000 倍？

## 二、容器的第一性原理：内存布局决定一切

### 2.1 连续内存：数组的伟大与局限
### 2.2 离散内存：链表如何用"指针"换"灵活"
### 2.3 缓存友好性：为什么数组在现代 CPU 上几乎总是赢家
### 2.4 顺序访问 vs 随机访问的硬件代价差异

## 三、动态数组的设计哲学

### 3.1 为什么扩容因子是 1.5 或 2 而不是 1.1
### 3.2 摊还分析：O(1) 平均插入是如何被证明的
### 3.3 缩容策略：为什么大多数容器"只扩不缩"

## 四、哈希表：用空间换时间的极致艺术

### 4.1 哈希函数的设计目标：均匀、快速、抗碰撞
### 4.2 开放寻址 vs 链地址法的取舍
### 4.3 负载因子与 rehash：为什么是 0.75
### 4.4 现代哈希表的进化（Robin Hood / Swiss Table / Cuckoo）

## 五、有序容器：树结构的精妙平衡

### 5.1 为什么需要"有序"——区间查询与范围操作
### 5.2 红黑树 vs 跳表：两种"近似平衡"思想的对照
### 5.3 B+ 树为什么是磁盘世界的王者

## 六、并发容器：当多个线程同时访问

### 6.1 写时复制：读多写少的最优解
### 6.2 分段锁 vs CAS：从 ConcurrentHashMap 看演进
### 6.3 无锁队列与 Disruptor 思想

## 七、总结：选择容器就是选择权衡

