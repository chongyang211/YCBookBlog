# 5.7 网络IO模型设计

> 我们已经讨论了进程内的消息机制和跨进程通信。但当通信跨越机器边界，问题陡然升维：**几万、几十万的连接同时在线，一台服务器到底是怎么扛下来的？为什么 Java NIO 比 BIO 快？为什么 Linux 的 epoll 比 select 快？为什么 io_uring 又被称为"下一代"？**
>
> 这不是简单的 API 优劣对比，而是一连串关于"操作系统调度成本 vs 编程复杂度"的根本权衡。本章把 IO 模型拆开，从阻塞、非阻塞、多路复用、异步一路讲到 Reactor / Proactor / io_uring。

## 一、问题引入：一台机器扛 10 万连接是怎么做到的

### 1.1 真实案例：从 Tomcat BIO 切到 Netty 后单机连接数从 1k 飙到 10w
### 1.2 矛盾点浮现：操作系统资源没变，凭什么能撑这么多

## 二、IO 模型的五个层次

### 2.1 阻塞 IO（BIO）：最直观也最浪费
### 2.2 非阻塞 IO：轮询的代价
### 2.3 IO 多路复用：让一个线程看多个 fd
### 2.4 信号驱动：理论很美但少有人用
### 2.5 异步 IO：内核完成后通知

## 三、select / poll / epoll 的演进史

### 3.1 select 的三大原罪（fd_set 大小、线性扫描、内核拷贝）
### 3.2 poll 修了一个问题，但没修关键的
### 3.3 epoll：红黑树 + 就绪链表的精妙数据结构
### 3.4 LT vs ET：水平触发与边缘触发的真实差异

## 四、Reactor 模式：从内核能力到编程范式

### 4.1 单 Reactor 单线程：Redis 的极简之美
### 4.2 主从 Reactor：Netty 的标准架构
### 4.3 为什么 Reactor 模式上的回调地狱不可避免

## 五、Proactor 与异步 IO 的真正落地

### 5.1 Windows IOCP 为什么是真正的异步
### 5.2 Linux 的 AIO 为什么"名存实亡"
### 5.3 io_uring：用环形队列重塑系统调用

## 六、零拷贝的几种武器

### 6.1 sendfile / splice
### 6.2 mmap + write
### 6.3 用户态协议栈（DPDK / kernel bypass）

## 七、总结与思考：选 IO 模型就是选系统形态

