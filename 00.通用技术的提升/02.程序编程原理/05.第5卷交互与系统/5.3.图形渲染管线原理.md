# 5.3 图形渲染管线原理

> 上一章我们看到"视图加载渲染"在框架层做了什么。但视图最终怎么变成屏幕上的像素？为什么 60fps 是一道生死线？为什么有时画面"撕裂"，有时又"卡顿掉帧"？这一切答案都在另一个层次——**GPU 渲染管线**。
>
> 本章从一个"动画明明很简单却卡顿"的案例切入，沿着像素从应用层到屏幕的完整旅程，揭开光栅化、合成、双缓冲、垂直同步、合成器、Skia/Metal 的本质。

## 一、问题引入：明明只是一个透明遮罩，为什么 FPS 掉到 30

### 1.1 真实案例：UI 动画看似流畅，profiler 却频繁丢帧
### 1.2 矛盾点浮现：CPU 不忙、内存够用，瓶颈在哪？

## 二、像素的旅程：从 View 树到屏幕的 7 个阶段

### 2.1 测量与布局
### 2.2 绘制命令录制（Display List）
### 2.3 GPU 提交
### 2.4 顶点变换
### 2.5 光栅化
### 2.6 片元着色
### 2.7 合成与显示

## 三、双缓冲与垂直同步：消除撕裂的工程艺术

### 3.1 单缓冲为何会"撕裂"
### 3.2 双缓冲、三缓冲的取舍
### 3.3 VSync 的作用与代价
### 3.4 移动端的 Choreographer 节拍器

## 四、合成器（Compositor）：把多层贴在一起

### 4.1 为什么现代 UI 都走分层合成
### 4.2 硬件合成 vs 软件合成
### 4.3 过度绘制（Overdraw）的代价
### 4.4 GPU 纹理上传的隐形开销

## 五、深入图形 API：OpenGL / Metal / Vulkan

### 5.1 为什么 OpenGL 被淘汰
### 5.2 显式 API（Vulkan/Metal）让上层负担"调度"
### 5.3 移动端为何更追求"低能耗 / 低发热"

## 六、Skia：跨平台 2D 渲染的事实标准

### 6.1 Skia 的命令录制 + 后端绘制架构
### 6.2 为什么 Flutter / Chrome / Android 都选了它

## 七、总结与思考：渲染优化的真正抓手

