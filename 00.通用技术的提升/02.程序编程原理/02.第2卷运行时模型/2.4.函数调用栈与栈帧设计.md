# 2.4 函数调用栈与栈帧设计

> 上一章我们看到了"对象与函数访问"如何被解析。但当我们真正运行起来后，一连串问题随之而来：**为什么函数能层层调用又层层返回？参数到底是怎么传过去的？为什么递归过深会栈溢出？为什么在 C 里返回局部变量地址是大忌？**
>
> 这些看似各自独立的问题，其实都指向一个核心数据结构——**调用栈（Call Stack）**。本章从一个工作中遇到的"诡异栈溢出"案例出发，剖开调用栈和栈帧的内部构造，理解 ABI 这个"无形契约"。

## 一、问题引入：一次 StackOverflow 引发的反思

### 1.1 真实案例：一个看似正常的递归为何 50 层就崩了
### 1.2 矛盾点浮现：为什么 C++ 默认栈只有 8MB？

## 二、栈帧的诞生：函数调用的物理痕迹

### 2.1 没有栈的世界：早期机器是如何调用函数的
### 2.2 栈的伟大发明：把"调用关系"转化为"数据结构"
### 2.3 一个栈帧里到底装了什么
### 2.4 SP / BP / PC：三个寄存器的协作之美

## 三、参数传递：从压栈到寄存器调用

### 3.1 为什么早期都用栈传参
### 3.2 现代 ABI：寄存器优先（System V / Windows x64 / ARM AAPCS）
### 3.3 可变参数和栈对齐的"暗坑"

## 四、栈展开：异常如何"穿透"多层函数

### 4.1 同步展开 vs 异步展开
### 4.2 SJLJ vs 表驱动（DWARF）的两种实现
### 4.3 RAII 是如何被栈展开机制保证的

## 五、尾调用优化：递归不再恐惧栈溢出

### 5.1 尾调用的本质：调用即跳转
### 5.2 哪些语言强制 TCO，哪些不支持，为什么
### 5.3 自己手动改造：把递归变成迭代

## 六、深入：协程为什么需要"自己的栈"
（与第3卷协程章节做前后呼应）

## 七、总结与思考

