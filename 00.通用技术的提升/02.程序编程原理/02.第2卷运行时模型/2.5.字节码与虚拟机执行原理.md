# 2.5 字节码与虚拟机执行原理

> 我们已经看清"调用栈"如何承载一次函数调用。但还有一个更基础的问题没回答：**CPU 直接读懂的是机器指令，那 Java、Python、JavaScript、Lua 这些"中间语言"凭什么也能跑起来？为什么同一份字节码能在不同平台上无缝运行？**
>
> 答案藏在一个被广泛使用却鲜少被正确理解的概念里——**虚拟机（VM）**。本章从一个跨平台移植的真实案例切入，揭开字节码、解释器、栈式机/寄存器机的本质。

## 一、问题引入：同一份代码为何能跨 Windows、Mac、Linux 运行

### 1.1 真实案例：把 Java 服务从 x86 迁到 ARM 的"零成本"
### 1.2 矛盾点浮现：那 native 编译为何就要重新编译？

## 二、虚拟机的本质：建造一台"逻辑 CPU"

### 2.1 为什么需要中间表示（IR）
### 2.2 字节码与机器码的本质差异
### 2.3 平台无关性的真正来源

## 三、栈式虚拟机 vs 寄存器式虚拟机

### 3.1 JVM 为什么选了栈式
### 3.2 Lua / Dalvik 为什么选了寄存器式
### 3.3 两者的指令密度、解码代价、流水线友好性对比

## 四、解释器的进化之路

### 4.1 朴素 switch-case 解释器
### 4.2 直接线索（Direct Threading）
### 4.3 计算 goto 与超指令融合
### 4.4 模板解释器：从字节码生成机器码片段

## 五、字节码到底长什么样：以一段 Java 为例

### 5.1 反编译一段 `i++` 看到了什么
### 5.2 操作数栈、局部变量表、常量池的协作
### 5.3 为什么 `i = i++` 在不同语言行为不同

## 六、深入：WebAssembly 把 VM 思想推向了哪里
### 6.1 为什么 WASM 选择栈式 + 验证型字节码
### 6.2 沙箱安全模型的三道防线

## 七、总结与思考：从字节码看抽象的力量

