# 2.6 JIT 与运行时优化

> 上一章我们看到字节码经过解释执行可以跨平台运行，但解释执行有一个躲不掉的代价——**慢**，每条字节码都要"翻译一遍"。那为什么 Java、JavaScript、LuaJIT 在某些场景下能逼近甚至超越 C 的性能？
>
> 秘密武器叫 **JIT（Just-In-Time Compilation）**，但它远不只是"把字节码再编译成机器码"这么简单。本章从一个"代码越跑越快"的诡异现象切入，剖开 JIT 的核心：**热点检测、分层编译、内联、逃逸分析、去优化**。

## 一、问题引入：为什么 JVM 服务"跑一会儿才进入状态"

### 1.1 真实案例：压测前几秒 QPS 1000，几分钟后到 5000
### 1.2 矛盾点浮现：代码没变，机器没变，性能凭空翻倍？

## 二、解释器的天花板与 JIT 的诞生

### 2.1 解释执行的三个固有开销
### 2.2 为什么不"AOT 一次编译完"——动态语言的优势丢失问题
### 2.3 JIT 的核心思想：用"运行时信息"换"编译质量"

## 三、热点检测与分层编译

### 3.1 计数器法 vs 采样法
### 3.2 C1（快）和 C2（强）的分工
### 3.3 编译队列与 OSR（栈上替换）

## 四、内联：性能优化的"万恶之源"也是"万善之根"

### 4.1 内联为什么能带来连锁优化
### 4.2 多态调用如何被"去虚化"
### 4.3 内联预算：为什么不能无限内联

## 五、逃逸分析：让"看起来在堆上"的对象其实在栈上

### 5.1 标量替换：把对象拆成几个寄存器
### 5.2 锁消除：单线程访问的同步开销归零
### 5.3 为什么 Go 编译器把它做得更彻底

## 六、去优化：当假设被打破怎么办

### 6.1 类加载、新分支命中导致的失效
### 6.2 守卫指令与 Deopt 流程
### 6.3 反复 Deopt 引发的"性能悬崖"

## 七、总结：JIT 的哲学是"乐观假设 + 兜底回退"

