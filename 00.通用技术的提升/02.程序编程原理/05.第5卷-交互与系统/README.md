# 第 5 卷｜交互与系统

> **从硬件触点到屏幕像素**——用户交互背后的事件驱动架构。

---

## 🎯 这一卷要回答什么

App 用了十年，你写的每一行 `setText`、`onClick`、`addEventListener` 背后到底发生了什么？

- 屏幕上的"按钮"——它真的是个"按钮"吗？还是只是**一片像素 + 一段事件路由代码**？
- 当你点击屏幕，从硬件中断到 `onClick` 被调用，**OS 与运行时一共做了多少事**？
- iOS 的 ResponderChain、Android 的 dispatchTouchEvent、Web 的事件冒泡——三套机制看似各异，**底层是同一件事**吗？
- Handler / Looper / MessageQueue —— 为什么所有 GUI 框架都长这样？
- HTTPS 的握手到底在握什么？AES、RSA、SHA 这些算法当初是为了解决什么矛盾？

这一卷把"看得见的 UI"层层撕开，让你看到下面**事件、消息、加密、渲染**这四条独立但相通的脉络。

---

## 📖 篇章总览（5 篇）

| 序号 | 文档 | 核心矛盾 |
|------|------|---------|
| 40 | [窗口核心设计思想](./40.窗口核心设计思想.md) | 为什么窗口是"矩形 + 事件 + 坐标系"——窗口系统的本质 |
| 41 | [视图加载渲染设计](./41.视图加载渲染设计.md) | measure / layout / draw 三大阶段为何是这种顺序 |
| 42 | [手势事件设计灵魂](./42.手势事件设计灵魂.md) | 触点流 → 状态机 → 手势语义的多层抽象 |
| 43 | [数据加密和解密](./43.数据加密和解密.md) | 对称 / 非对称 / 哈希 —— 三种密码学武器各自的矛盾 |
| 44 | [消息机制设计思想](./44.消息机制设计思想.md) | Handler / Looper / MessageQueue 为何是 GUI 的标配 |

---

## 🔗 知识脉络

```mermaid
flowchart TB
    subgraph 屏幕呈现三部曲
        A[40 窗口<br/>"在哪画"]
        B[41 视图渲染<br/>"画什么"]
        C[42 手势事件<br/>"用户怎么交互"]
    end
    subgraph 系统支撑
        D[44 消息机制<br/>事件如何被调度]
        E[43 加密解密<br/>数据如何被保护]
    end
    A --> B --> C --> D
    D --> A
    style A fill:#fff3cd
    style B fill:#fff3cd
    style C fill:#fff3cd
```

**40 / 41 / 42 是"屏幕呈现三部曲"**：窗口决定画在哪、视图决定画什么、手势决定用户怎么操作。**44 是这三者的调度中枢**——所有事件最终都流入消息循环。**43 是数据安全维度**，独立但与前四篇通过"序列化 + 网络传输"接上。

---

## 🌉 与其他卷的承接

- **承接第 3 卷**：消息机制 = 单线程模型 + 事件循环 + 无锁队列，是第 3 卷范式篇的实战延伸。
- **承接第 4 卷**：手势事件每秒产生 60+ MotionEvent，对象池是第 4 卷拷贝原理的典型应用。
- **延伸第 1 卷**：加密章节回到了"数据如何被表示"这一最初命题，闭合了整个专栏的环。

---

## 💡 学完你能回答

- 为什么 Android 主线程必须有 Looper，子线程默认没有？子线程开 Looper 之后会不会内存泄漏？
- iOS 的 RunLoop、Android 的 Looper、JS 的 Event Loop —— **三者本质是不是同一个东西**？差异在哪？
- 一个 `RecyclerView` 滚动时为什么会卡？布局 / 测量 / 绘制三大阶段哪一步最容易成为瓶颈？
- 为什么 HTTPS 握手要先用非对称、然后切到对称？性能 trade-off 算下来到底差多少？
- 手势冲突的"父子拦截"在 Android 是 `onInterceptTouchEvent`，在 iOS 是 `requireGestureRecognizerToFail`，**两种机制是不是同一种思想的两种表达**？

---

## 🛣 推荐阅读顺序

```
40 窗口（先理解"舞台"）
   ↓
41 视图渲染（再理解"演员"）
   ↓
42 手势事件（再理解"观众的互动"）
   ↓
44 消息机制（最后理解"幕后调度"）
   ↓
43 数据加密（独立支线，可任意时机阅读）
```
