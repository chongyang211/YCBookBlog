# 4.4 内存对齐与缓存局部性

> 我们已经知道堆和栈的内存布局，也理解了内存模型保障可见性。但还有一个性能层面的"暗默约束"在不显眼地塑造一切——**CPU 缓存**。
>
> 同样一段循环，仅仅把数组的访问方向从"行优先"换成"列优先"，性能可能差 10 倍；两个互不相干的变量，仅仅因为"挨得太近"，就会让多线程性能崩塌。本章揭开内存对齐、Cache Line、伪共享、空间/时间局部性背后的机制。

## 一、问题引入：一个改一行代码就快 10 倍的循环

### 1.1 真实案例：二维数组遍历方向引发的性能悬崖
### 1.2 矛盾点浮现：明明访问的数据量一样多

## 二、CPU 缓存层级与内存墙

### 2.1 寄存器、L1、L2、L3、主存的速度差距
### 2.2 内存墙（Memory Wall）：为什么 CPU 越快越"等"内存
### 2.3 缓存的两条黄金法则：空间局部性、时间局部性

## 三、Cache Line：缓存的最小搬运单位

### 3.1 为什么是 64 字节
### 3.2 一次访问 = 一整行加载
### 3.3 缓存命中 / 失效 / 替换策略

## 四、内存对齐：硬件给软件的"隐形税"

### 4.1 为什么 8 字节对齐能比 1 字节快很多
### 4.2 结构体填充（padding）的产生
### 4.3 字段重排：用顺序换空间

## 五、伪共享：多核时代的"隐形性能杀手"

### 5.1 现象：两个线程更新两个变量却互相拖慢
### 5.2 原因：MESI 协议下的缓存行抢夺
### 5.3 解决方案：填充 / @Contended / 隔离布局

## 六、数据布局优化的工程实践

### 6.1 数组结构体（AoS）vs 结构体数组（SoA）
### 6.2 列式存储为何更适合 OLAP
### 6.3 Disruptor 是如何把缓存利用率拉满的

## 七、总结与思考：写代码时要"记得 CPU 在搬什么"

