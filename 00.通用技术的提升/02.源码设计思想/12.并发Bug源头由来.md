#### 目录介绍
- 01.并发编程挑战
  - 1.1 并发的挑战
- 02.多线程脏数据
  - 2.1 CPU缓存一致性
  - 2.2 指令重排序
  - 2.3 原子性缺失
  - 2.4 Bug分类与源头
- 03.线程模型设计
  - 3.1 Java内存模型
  - 3.2 C++内存模型
- 03.脏数据案例
  - 3.1 银行转账问题
  - 3.2 计数器问题
  - 3.3 单例模式问题



## 01.并发编程挑战

### 1.1 并发的挑战

并发编程的核心挑战在于：**多个执行单元共享可变状态时，执行顺序不可预测。**

单线程程序是确定性的——相同输入总能得到相同输出。而并发程序中，线程调度由OS内核控制，开发者无法控制线程何时被中断、何时恢复，这引入了**不确定性**。

并发问题难以调试的根源：

- **时序依赖**：Bug只在特定调度顺序下触发
- **不可复现**：同样的代码运行多次，结果可能不同
- **海森堡效应**：加日志/断点后，时序改变，Bug消失

### 1.2 核心挑战

- **数据竞争**：多个线程同时访问共享数据
- **原子性**：操作的不可分割性
- **可见性**：一个线程对共享变量的修改对其他线程的可见性
- **有序性**：程序执行的顺序性保证

## 02.多线程脏数据

为什么会出现脏数据？脏数据的产生主要源于以下几个方面：

### 2.1 CPU缓存一致性

```mermaid
graph TB
    subgraph "CPU架构"
        CPU1[CPU Core 1]
        CPU2[CPU Core 2]
        L1_1[L1 Cache]
        L1_2[L1 Cache]
        L2[L2 Cache]
        L3[L3 Cache]
        RAM[主内存]
        
        CPU1 --> L1_1
        CPU2 --> L1_2
        L1_1 --> L2
        L1_2 --> L2
        L2 --> L3
        L3 --> RAM
    end
```

### 2.2 指令重排序

```mermaid
sequenceDiagram
    participant T1 as 线程1
    participant T2 as 线程2
    participant Memory as 内存
    
    Note over T1,T2: 原始代码顺序
    T1->>Memory: x = 1
    T1->>Memory: flag = true
    T2->>Memory: if(flag) print(x)
    
    Note over T1,T2: 重排序后可能的执行顺序
    T1->>Memory: flag = true
    T2->>Memory: if(flag) print(x) // 可能打印0
    T1->>Memory: x = 1
```

### 2.3 原子性缺失

多步操作在执行过程中被其他线程中断，导致数据不一致。

### 2.4 Bug分类与源头

```mermaid
mindmap
  root((并发Bug源头))
    竞态条件
      数据竞争
      时序依赖
      检查后使用
    死锁
      循环等待
      资源竞争
      锁顺序不当
    活锁
      线程饥饿
      优先级反转
      资源抢占
    内存模型
      可见性问题
      重排序
      缓存一致性
```

## 03.线程模型设计

### 3.1 Java内存模型

```mermaid
graph TB
    subgraph "Java内存模型"
        subgraph "线程1"
            T1_Stack[线程栈]
            T1_Local[本地变量]
            T1_Cache[工作内存]
        end
        
        subgraph "线程2"
            T2_Stack[线程栈]
            T2_Local[本地变量]
            T2_Cache[工作内存]
        end
        
        subgraph "主内存"
            Heap[堆内存]
            Method[方法区]
            Shared[共享变量]
        end
        
        T1_Cache <--> Shared
        T2_Cache <--> Shared
    end
```

### 3.2 C++内存模型

```mermaid
graph LR
    subgraph "C++内存模型"
        A[memory_order_relaxed] --> B[memory_order_acquire]
        B --> C[memory_order_release]
        C --> D[memory_order_acq_rel]
        D --> E[memory_order_seq_cst]
        
        A -.-> F[最弱排序]
        E -.-> G[最强排序]
    end
```

## 03.脏数据案例

### 3.1 银行转账问题


案例1：银行转账问题（Java）

```java
public class BankAccount {
    private volatile int balance = 1000;
    
    // 问题代码：非原子操作
    public void withdraw(int amount) {
        if (balance >= amount) {  // 步骤1：检查余额
            // 此处可能被其他线程中断
            balance -= amount;    // 步骤2：扣减余额
        }
    }
}
```

**问题分析时序图：**

```mermaid
sequenceDiagram
    participant T1 as 线程1(取款500)
    participant T2 as 线程2(取款600)
    participant Account as 账户(余额1000)
    
    T1->>Account: 检查余额 >= 500 ✓
    T2->>Account: 检查余额 >= 600 ✓
    T1->>Account: balance = 1000 - 500 = 500
    T2->>Account: balance = 1000 - 600 = 400
    
    Note over Account: 最终余额400，但实际应该拒绝其中一个操作
```

### 3.2 计数器问题

```cpp
class Counter {
private:
    int count = 0;
public:
    void increment() {
        count++;  // 非原子操作：读取->增加->写入
    }
    
    int getCount() {
        return count;
    }
};
```

### 3.3 单例模式问题

```objc
@implementation Singleton

+ (instancetype)sharedInstance {
    static Singleton *instance = nil;
    if (instance == nil) {  // 问题：双重检查但无同步
        instance = [[Singleton alloc] init];
    }
    return instance;
}

@end
```
















