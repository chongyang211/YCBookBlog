# 02.数据加密和解密实践
#### 目录介绍
- [01.加解密基础概念](#01加解密基础概念)
  - [1.1 加密的本质与由来](#11-加密的本质与由来)
  - [1.2 加解密算法分类](#12-加解密算法分类)
- [02.加解密使用场景](#02加解密使用场景)
  - [2.1 单向加密算法](#21-单向加密算法)
  - [2.2 对称加密算法](#22-对称加密算法)
  - [2.3 非对称加密算法](#23-非对称加密算法)
- [03.MD5加密设计](#03md5加密设计)
  - [3.1 MD5算法摘要](#31-md5算法摘要)
  - [3.2 MD5算法特点](#32-md5算法特点)
  - [3.3 MD5使用场景](#33-md5使用场景)
  - [3.4 注意事项说明](#34-注意事项说明)
  - [3.5 MD5如何破解](#35-md5如何破解)
- [05.SHA哈希函数](#05sha哈希函数)
  - [5.1 Hash算法说明](#51-hash算法说明)
- [06.DES对称加解密](#06des对称加解密)
  - [6.1 DES算法介绍](#61-des算法介绍)
  - [6.2 DES工作原理](#62-des工作原理)
  - [6.3 DES的安全性](#63-des的安全性)
- [07.AES对称加解密](#07aes对称加解密)
  - [7.1 AES算法介绍](#71-aes算法介绍)
  - [7.2 AES工作模式](#72-aes工作模式)
  - [7.3 各语言AES实现](#73-各语言aes实现)
- [08.RC4对称加解密](#08rc4对称加解密)
  - [8.1 RC4算法介绍](#81-rc4算法介绍)
- [09.RSA非对称加解密](#09rsa非对称加解密)
  - [9.1 RSA算法介绍](#91-rsa算法介绍)
  - [9.2 RSA核心原理](#92-rsa核心原理)
  - [9.3 理解密钥生成](#93-理解密钥生成)
  - [9.4 加解密过程](#94-加解密过程)
- [10.DSA非对称加解密](#10dsa非对称加解密)
  - [10.1 DSA算法介绍](#101-dsa算法介绍)
  - [10.2 DSA vs RSA 对比](#102-dsa-vs-rsa-对比)
  - [10.3 ECDSA](#103-ecdsa)
- [11.SM国密算法](#11sm国密算法)
  - [11.1 SM加密是什么](#111-sm加密是什么)
  - [11.2 SM2算法介绍](#112-sm2算法介绍)
  - [11.3 SM3算法介绍](#113-sm3算法介绍)
  - [11.4 SM4算法介绍](#114-sm4算法介绍)
  - [11.5 OpenSsl与SM](#115-openssl与sm)


## 01.加解密基础概念

### 1.1 加密的本质与由来

**疑惑**：计算机数据不都是0和1吗，为什么需要加密？

**答疑**：因为网络传输是**公开信道**。数据从A到B，中间经过路由器、交换机、运营商骨干网，任何一个节点都可以"偷看"或"篡改"数据。加密的本质就是**将可读数据变为不可读数据，同时保证授权方可以还原**。

**论证——从古典密码到现代密码的演变**：

```
公元前5世纪  斯巴达密码棒（Scytale）—— 物理换位密码
  |
公元前1世纪  凯撒密码 —— 字母替换（每个字母向后移3位）
  |
1467年      Alberti密码盘 —— 多表替换密码
  |
1918年      Enigma密码机 —— 机械化多表替换 + 接线板
  |
1949年      Shannon《保密系统通信理论》—— 密码学成为数学学科
  |
1976年      Diffie-Hellman 密钥交换 —— 公钥密码学开端
  |
1977年      RSA算法发表 —— 非对称加密里程碑
  |
2001年      AES 被选为新标准 —— 取代DES
  |
今天         后量子密码学研究 —— 应对量子计算机威胁
```

**加密的三大安全目标（CIA三角）**：

| 目标 | 英文 | 说明 | 对应技术 |
|------|------|------|---------|
| 机密性 | Confidentiality | 数据不被未授权读取 | 对称/非对称加密 |
| 完整性 | Integrity | 数据未被篡改 | 哈希/数字签名 |
| 认证性 | Authentication | 确认数据来源 | 数字签名/证书 |

### 1.2 加解密算法分类

数据加解密算法有很多种，其中一些常见的包括：

1. **对称加密算法：**

- **AES（Advanced Encryption Standard）**：一种流行的对称加密算法，用于加密和解密数据。
- **DES（Data Encryption Standard）**：较为古老的对称加密算法，已逐渐被 AES 取代。
- **3DES（Triple Data Encryption Standard）**：对 DES 进行三次加密以增强安全性。

2. **非对称加密算法：**

- **RSA（Rivest-Shamir-Adleman）**：一种常见的非对称加密算法，用于加密和数字签名。
- **DSA（Digital Signature Algorithm）**：专门用于数字签名的非对称加密算法。
- **ECC（Elliptic Curve Cryptography）**：基于椭圆曲线的非对称加密算法，具有较高的安全性和效率。

3. **哈希函数：**

- **MD5（Message Digest Algorithm 5）**：一种常见的哈希函数，用于生成固定长度的哈希值。
- **SHA（Secure Hash Algorithm）系列**：包括 SHA-1、SHA-256、SHA-512 等不同长度的哈希函数。

4. **密码学协议：**

- **TLS/SSL（Transport Layer Security/Secure Sockets Layer）**：用于安全通信的协议，包括对称加密、非对称加密和数字证书等技术。
- **PGP（Pretty Good Privacy）**：用于加密、签名和认证的开放标准。

这些是一些常见的数据加解密算法和技术，用于保护数据的机密性、完整性和认证性。在实际应用中，根据需求和安全性要求，选择适合的加密算法和协议非常重要。

## 02.加解密使用场景

### 2.1 单向加密算法

输入明文后由算法直接加密成密文，密文无法解密，只有重新输入密文，并经过同样的加密算法处理，得到形同的密文并被系统重新识别后，才能真正的解密。

### 2.2 对称加密算法

**一句话概括：加密和解密都是用相同密钥**。这类加密算法技术较为成熟，加密效率高。

- 密钥可以自己指定，只有一把密钥，如果密钥暴露，文件就会被暴露。
- 特点是加密速度很快，但是缺点是安全性较低，因为只要密钥暴漏，数据就可以被解密了。

常见的对称加密和解密：DES算法；AES算法

### 2.3 非对称加密算法

收发双方使用不同的密钥，发方使用公开密钥对消息进行加密，收发使用私有密钥机型解密，保密性更高，但效率更低。

加密和解密的规则是：**公钥加密只能私钥解密，私钥加密只能公钥解密**。特点是加密速度慢些，但是安全系数很高

应用场景举例：在集成支付宝支付sdk时，需要生成私钥和公钥，公钥需要设置到支付宝网站的管理后台，在程序中调用支付接口的时候，使用我们自己的私钥进行加密，这样支付宝由于有公钥可以解密，其他人即时劫持了数据，但是没有公钥，也无法解密。

常见非对称加密：RSA

## 03.MD5加密设计

### 3.1 MD5算法摘要

信息-摘要算法。用于确保信息传输完整一致。是计算机广泛使用的杂凑算法之一（又译摘要算法、哈希算法），主流编程语言普遍已有MD5实现。

### 3.2 MD5算法特点

MD5算法具有特点，如下所示：

- 1、压缩性：任意长度的数据，算出的MD5值长度都是固定的。
- 2、容易计算：从原数据计算出MD5值很容易。
- 3、抗修改性：对原数据进行任何改动，哪怕只修改1个字节，所得到的MD5值都有很大区别。
- 4、强抗碰撞：已知原数据和其MD5值，想找到一个具有相同MD5值的数据（即伪造数据）是非常困难的。
- 5、MD5加密是不可逆的。

### 3.3 MD5使用场景

1.用户登录的时候，系统把用户输入的password计算成MD5值，然后保存该值，每次用户输入密码用md5对比即可知道密码是否正确。

2.文件的完整性校验，在传递文件的过程中附带传递文件的md5值，接收端通过比较文件的md5值判断文件的完整性。

### 3.4 注意事项说明

注意事项说明，由于网络MD5数据库比较大，如果直接使用md5加密的话，可能会被破译出来，这是在加密的过程中”加盐”就可以大大避免直接被破译的危险。

### 3.5 MD5如何破解

MD5本身是不可逆的，无法从哈希值反推原始数据。但可以通过以下方式"破解"：

**1. 彩虹表攻击**：预先计算大量常见密码的MD5值，形成"密码→MD5"映射表。攻击者拿到MD5后查表即可。

```
常见密码        MD5值
───────────    ──────────────────────────────
123456         e10adc3949ba59abbe56e057f20f883e
password       5f4dcc3b5aa765d61d8327deb882cf99
admin          21232f297a57a5a743894a0e4a801fc3
```

**2. 碰撞攻击**：2004年王小云教授团队找到了MD5碰撞方法，可以构造两个不同的输入产生相同的MD5值。这意味着MD5不适合用于数字签名。

**3. 加盐防御**：

```java
// 错误：直接MD5
String hash = md5(password);  // 容易被彩虹表破解

// 正确：加盐后MD5
String salt = generateRandomSalt(16);  // 每个用户不同的随机盐值
String hash = md5(salt + password);
// 存储时同时保存 salt 和 hash
```

**4. 更好的方案**：密码存储应使用专用的密码哈希函数（bcrypt、scrypt、Argon2），它们内置加盐和可调慢速计算。

```java
// 推荐：使用 BCrypt（Java）
String hashed = BCrypt.hashpw(password, BCrypt.gensalt(12));
boolean match = BCrypt.checkpw(password, hashed);
```


## 05.SHA哈希函数

### 5.1 Hash算法说明

- 散列算法是什么
    - 是把任意长度的输入（又叫做预映射 pre-image ）通过算法变换成固定长度的输出。散列是信息的提炼，通常其长度要比信息小得多，且为一个固定长度。
- 特性：
    - 1.高效：可以快速计算出哈希值；2.不可逆：从哈希值不能反向推导出原始数据；3.输入敏感：原始数据只要有一点变动，得到的哈希值也会差别很大；4.冲突避免：对于不同的原始数据，哈希值相同的概率非常小。
- 使用场景：
    - 安全加密。 例如网络传输密码。
    - 唯一标识。例如在海量的图库中，搜索一张图是否存在。可以给每一个图片取一个唯一标识，或者说信息摘要，通过这个唯一标识来判定图片是否在图库中，这样就可以减少很多工作量
    - 数据校验。例如下载文件的完整性
    - 负载均衡。通过哈希算法，对客户端 IP 地址或者会话 ID 计算哈希值，将取得的哈希值与服务器个数进行取模运算，最终得到的值就是应该被路由到的服务器编号
- Hash算法常见案例(MD5、SHA1、SHA256)：
    - MD4 1990年，输出128位 （已经不安全）
    - MD5 1991年，输出128位 （已经不安全）
    - SHA-0 1993年，输出160位 （发布之后很快就被撤回，是SHA-1的前身）
    - SHA-1 1995年，输出160位 （已经不安全）
    - SHA-2 包括 SHA-224、SHA-256、SHA-384，和 SHA-512，分别输出 224、256、384、512位。 (目前安全)


## 06.DES对称加解密

### 6.1 DES算法介绍

DES（Data Encryption Standard）是1977年美国联邦政府采纳的第一个加密标准，由IBM在NSA协助下开发。

**核心参数**：密钥长度56位有效位 + 8位校验位 = 64位；分组长度64位（8字节）；加密轮数16轮Feistel网络。

### 6.2 DES工作原理

```
明文（64位）→ 初始置换IP → 16轮Feistel网络（每轮用48位子密钥）→ 逆初始置换IP⁻¹ → 密文（64位）

每轮核心：L(i) = R(i-1),  R(i) = L(i-1) ⊕ F(R(i-1), Ki)
```

### 6.3 DES的安全性

**DES已不安全**。1999年，EFF用不到一天暴力破解了DES。56位密钥空间仅2^56≈7.2×10^16种可能。

**3DES（Triple DES）**：使用三个56位密钥进行三次DES加密，有效密钥长度112位。加密公式：`C = E(K3, D(K2, E(K1, P)))`。

```java
// Java 3DES加密
SecretKeySpec keySpec = new SecretKeySpec(key, "DESede");
Cipher cipher = Cipher.getInstance("DESede/ECB/PKCS5Padding");
cipher.init(Cipher.ENCRYPT_MODE, keySpec);
byte[] ciphertext = cipher.doFinal(plaintext);
```

## 07.AES对称加解密

### 7.1 AES算法介绍

AES（Advanced Encryption Standard）是2001年NIST选定的新一代对称加密标准。密钥长度支持128/192/256位，分组长度128位。

| 参数 | AES-128 | AES-192 | AES-256 |
|------|---------|---------|---------|
| 密钥长度 | 128位 | 192位 | 256位 |
| 加密轮数 | 10轮 | 12轮 | 14轮 |

### 7.2 AES工作模式

| 模式 | 全称 | 特点 | 推荐度 |
|------|------|------|--------|
| ECB | Electronic Codebook | 每块独立加密，相同明文→相同密文 | 不推荐 |
| CBC | Cipher Block Chaining | 链式加密，需要IV | 推荐 |
| GCM | Galois/Counter Mode | CTR + 认证标签 | **强烈推荐** |

### 7.3 各语言AES实现

**Java AES-GCM**：

```java
import javax.crypto.Cipher;
import javax.crypto.spec.GCMParameterSpec;
import java.security.SecureRandom;

public class AESGCMUtil {
    private static final int GCM_TAG_LENGTH = 128;
    private static final int GCM_IV_LENGTH = 12;

    public static byte[] encrypt(byte[] plaintext, SecretKey key) throws Exception {
        byte[] iv = new byte[GCM_IV_LENGTH];
        new SecureRandom().nextBytes(iv);
        Cipher cipher = Cipher.getInstance("AES/GCM/NoPadding");
        cipher.init(Cipher.ENCRYPT_MODE, key, new GCMParameterSpec(GCM_TAG_LENGTH, iv));
        byte[] ciphertext = cipher.doFinal(plaintext);
        // IV + 密文一起返回
        byte[] result = new byte[iv.length + ciphertext.length];
        System.arraycopy(iv, 0, result, 0, iv.length);
        System.arraycopy(ciphertext, 0, result, iv.length, ciphertext.length);
        return result;
    }
}
```

**Go AES-GCM**：

```go
func encrypt(plaintext, key []byte) ([]byte, error) {
    block, err := aes.NewCipher(key)
    if err != nil { return nil, err }
    aesGCM, _ := cipher.NewGCM(block)
    nonce := make([]byte, aesGCM.NonceSize())
    io.ReadFull(rand.Reader, nonce)
    return aesGCM.Seal(nonce, nonce, plaintext, nil), nil
}
```


## 08.RC4对称加解密
### 8.1 RC4算法介绍

- RC4算法
    - RC4是一种对称密码算法，它属于对称密码算法中的序列密码(streamcipher,也称为流密码)，它是可变密钥长度，面向字节操作的流密码。
- RC4算法特点：
    - (1)、算法简洁易于软件实现，加密速度快，安全性比较高(在存储大量的信息时，RC4展现出它的实用性，面对大量的数据，运行简单，速度快)；
    - (2)、密钥长度可变，一般用256个字节。
- RC4工作规则
    - 第一步：输入密钥+需要加密的信息；第二步：通过密钥逐字节的对信息进行加密；第三步：接收方通过密钥解密数据

## 09.RSA非对称加解密
### 9.1 RSA算法介绍

RSA是由Ron Rivest、Adi Shamir和Leonard Adleman于1977年提出的非对称加密算法，名称取自三人姓氏首字母。RSA是第一个既能用于加密又能用于数字签名的算法。

**核心思想**：基于**大整数因式分解难题**——将两个大素数相乘很容易，但将其乘积分解回两个素数极其困难。

**密钥长度建议**：
- 1024位：已不安全（2010年后不推荐）
- 2048位：当前主流最低标准
- 4096位：高安全性场景推荐

### 9.2 RSA核心原理

**数学基础**：

1. 选取两个大素数 p 和 q，计算 n = p × q
2. 计算欧拉函数 φ(n) = (p-1)(q-1)
3. 选取公钥指数 e，使得 1 < e < φ(n) 且 gcd(e, φ(n)) = 1（通常 e = 65537）
4. 计算私钥指数 d，使得 d × e ≡ 1 (mod φ(n))

**加解密过程**：
- 公钥 = (n, e)，私钥 = (n, d)
- 加密：C = M^e mod n
- 解密：M = C^d mod n
### 9.3 理解密钥生成

在 Java 中，可以使用 KeyPairGenerator 生成 RSA 密钥对：

``` java
// 创建 KeyPairGenerator 实例
KeyPairGenerator keyPairGenerator = KeyPairGenerator.getInstance("RSA");
keyPairGenerator.initialize(2048); // 设置密钥长度，密钥长度范围：512～2048

// 生成密钥对
KeyPair keyPair = keyPairGenerator.generateKeyPair();

// 获取公钥和私钥
System.out.println("Public Key: " + keyPair.getPublic());
System.out.println("Private Key: " + keyPair.getPrivate());
```


### 9.4 加解密过程

**RSA加密**（用公钥加密）：

```java
// Java RSA加密
public static byte[] encrypt(byte[] data, PublicKey publicKey) throws Exception {
    Cipher cipher = Cipher.getInstance("RSA/ECB/PKCS1Padding");
    cipher.init(Cipher.ENCRYPT_MODE, publicKey);
    return cipher.doFinal(data);
}

// Java RSA解密
public static byte[] decrypt(byte[] data, PrivateKey privateKey) throws Exception {
    Cipher cipher = Cipher.getInstance("RSA/ECB/PKCS1Padding");
    cipher.init(Cipher.DECRYPT_MODE, privateKey);
    return cipher.doFinal(data);
}
```

**RSA分段加密**：RSA加密明文长度有限制（密钥长度/8 - 11字节），大数据需要分段处理。

**Go RSA加解密**：

```go
// 加密
ciphertext, err := rsa.EncryptPKCS1v15(rand.Reader, &publicKey, plaintext)
// 解密
plaintext, err := rsa.DecryptPKCS1v15(rand.Reader, privateKey, ciphertext)
// 签名
signature, err := rsa.SignPKCS1v15(rand.Reader, privateKey, crypto.SHA256, hashed)
```

## 10.DSA非对称加解密

### 10.1 DSA算法介绍

DSA（Digital Signature Algorithm）是美国NIST于1991年提出的数字签名标准（DSS）中的核心算法。与RSA不同，DSA**只能用于数字签名，不能用于加密**。

### 10.2 DSA vs RSA 对比

| 特性 | DSA | RSA |
|------|-----|-----|
| 用途 | 仅签名 | 加密 + 签名 |
| 速度 | 签名快，验签慢 | 签名慢，验签快 |
| 安全基础 | 离散对数问题 | 大整数分解问题 |

### 10.3 ECDSA

ECDSA（椭圆曲线数字签名算法）是DSA的椭圆曲线版本，更短的密钥达到更高的安全性。256位ECDSA密钥安全性等同于3072位RSA。ECDSA广泛用于HTTPS证书、比特币交易签名、移动端App签名。


## 11.SM国密算法

### 11.1 SM加密是什么

国密即国家密码局认定的国产密码算法。主要有SM1，SM2，SM3，SM4和最新的sm9。在国内环境主要使用的国家密码局认定的算法。

主要包含SM1、SM2、SM3、SM4几种方式。

- SM1：对称加密，且算法不公开，使用硬件加密，本文不做叙述；
- SM2：非对称加密，签名以及生成秘钥速度优于RSA，基于ECC算法，运算效率更高，且更安全；
- SM3：摘要，国产杂凑算法，生成长度为256比特，优于MD5以及SHA-1算法；
- SM4： 无线局域网标准的分组数据算法。对称加密，密钥长度和分组长度均为128位；

SM替代算法核心

- 对称算法： （DES 3DES AES）===》  SM1、SM4【其中SM1依赖硬件，因此不予实现，也未公开】
- 非对称密码算法： (RSA)  ===》 SM2(椭圆曲线密码)
- 散列算法： (MD5、SHA-1、SHA-256、SHA-384、SHA512)  ===》 SM3

SM算法实现应用说明

- SM2：创建密钥对、获取公钥、获取私钥、加密、解密、签名、验签
- SM3：加密
- SM4：创建秘钥、加密、解密

### 11.2 SM2算法介绍

非对称加密，SM2椭圆曲线公钥密码算法是我国自主设计的公钥密码算法，包括SM2-1椭圆曲线数字签名算法，SM2-2椭圆曲线密钥交换协议，SM2-3椭圆曲线公钥加密算法，分别用于实现数字签名密钥协商和数据加密等功能。

1. 生成 SM2 密钥对

```java
// 使用 Bouncy Castle 生成SM2密钥对
Security.addProvider(new BouncyCastleProvider());
KeyPairGenerator kpg = KeyPairGenerator.getInstance("EC", "BC");
ECGenParameterSpec spec = new ECGenParameterSpec("sm2p256v1");
kpg.initialize(spec, new SecureRandom());
KeyPair keyPair = kpg.generateKeyPair();
PublicKey publicKey = keyPair.getPublic();
PrivateKey privateKey = keyPair.getPrivate();
```

密钥对生成完成：一旦生成了私钥和公钥，就形成了一对密钥对，可以用于加密、解密或数字签名等操作。

2. SM2 加密和解密

```java
// SM2加密
Cipher cipher = Cipher.getInstance("SM2", "BC");
cipher.init(Cipher.ENCRYPT_MODE, publicKey);
byte[] ciphertext = cipher.doFinal(plaintext);

// SM2解密
cipher.init(Cipher.DECRYPT_MODE, privateKey);
byte[] decrypted = cipher.doFinal(ciphertext);
```

3. SM2 签名和验签

```java
// SM2签名
Signature signature = Signature.getInstance("SM3withSM2", "BC");
signature.initSign(privateKey);
signature.update(data);
byte[] signBytes = signature.sign();

// SM2验签
signature.initVerify(publicKey);
signature.update(data);
boolean valid = signature.verify(signBytes);
```

### 11.3 SM3算法介绍

SM3 算法 是中国国家密码管理局（State Cryptography Administration, SCA）发布的一种密码哈希算法，属于国密算法（GM/T 0004-2012 标准）的一部分。

它是一种密码哈希函数，用于生成固定长度的消息摘要（通常为 256 位），广泛应用于数据完整性校验、数字签名、消息认证等领域。

可以类比MD5算法，与MD5相似，但是比md5更长，具有更高的安全性。

### 11.4 SM4算法介绍

对称加密，SM4 算法 是中国国家密码管理局（State Cryptography Administration, SCA）发布的一种对称加密算法，属于国密算法（GM/T 0002-2012 标准）的一部分。

它是一种分组加密算法，采用 128 位密钥和 128 位分组长度，广泛应用于数据加密、安全通信等领域。用于实现数据的加密/解密运算，以保证数据和信息的机密性。

可以类比AES算法，与AES相似。SM4算法与AES算法具有相同的密钥长度分组长度128比特，因此在安全性上高于3DES算法。


### 11.5 OpenSsl与SM

- 使用openssl实现SM算法
    - 在算法实现中，不免要使用openssl库来进行操作，openssl 很强大，提供了健壮的，全功能的加密套件。
    - 有兴趣的同学可以详细去学习下，openssl有着大量的API可以使用，可以实现大量的密码算法。







- Android常见网络数据加密：https://www.jianshu.com/p/f1081e7a6e20
- Android数据加密概述及多种加密方式：https://www.cnblogs.com/zsychanpin/p/7308866.html
- Android传输数据时加密详解：https://cloud.tencent.com/developer/article/2086547
- 使用RSA分段加解密：https://blog.csdn.net/dongaddxing/article/details/116041314
- Android应用安全开发之浅谈加密算法的坑: https://www.lmlphp.com/user/58551/article/item/645568/


