
#### 目录介绍
- 1.多种引用背景
  - 1.1 根本矛盾



## 1.多种引用背景

### 1.1 根本矛盾

程序的核心矛盾之一：**对象的"使用权"与"生命周期"的耦合问题**。

```
最朴素的模型：
  我持有一个指针 → 我能用这个对象 → 这个对象必须活着 → 谁来保证它活着？

三个不可能同时满足的需求：
  ① 自动内存管理（不想手动free）
  ② 灵活的对象共享（多处引用同一对象）
  ③ 及时的内存回收（不用的对象立刻释放）

只有一种引用类型时，②和③严重冲突。
```





