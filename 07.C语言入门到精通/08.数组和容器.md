#### 目录介绍
- 8.1 数组的概念
- 8.2 数组使用
  - 8.2.1 数组的声明
  - 8.2.2 数组初始化
  - 8.2.3 数组访问
  - 8.2.4 数组的遍历
- 8.3 数组长度
- 8.4 多维数组
- 8.5 变长数据
- 8.6 数组和指针
- 8.7 数组和函数
- 8.8 数组注意事项
- 8.9 数组常见算法


## 8.1 数组的概念

在 C 语言中，**数组**是一种用于存储相同类型数据的集合。数组中的每个元素通过索引访问，索引从 `0` 开始。

**数组的基本概念**

- **数组**：一组相同类型的数据元素。
- **元素**：数组中的每个数据项。
- **索引**：用于访问数组元素的下标，从 `0` 开始。

## 8.2 数组使用
### 8.2.1 数组的声明

**语法**

```c
数据类型 数组名[数组大小];
```

**示例**

```c
int arr[5]; // 声明一个包含 5 个整数的数组
```

















