# 04.循环和选择
#### 目录介绍
- 4.1 语句和程序块
- 4.2 选择结构
  - 4.2.1 if语句
  - 4.2.2 if-else语句
  - 4.2.3 多条件语句
  - 4.2.4 switch语句
  - 4.2.5 嵌套选择结构
  - 4.2.6 三元运算符
  - 4.2.7 注意事项说明
  - 4.2.8 综合示例
- 4.3 循环结构
  - 4.3.1 for循环
  - 4.3.2 while循环
  - 4.3.3 do-while循环
  - 4.3.4 嵌套循环
  - 4.3.5 无限循环
  - 4.3.6 循环注意事项
- 4.4 跳出循环
  - 4.4.1 break语句
  - 4.4.2 continue语句
  - 4.4.3 goto语句



## 4.2 选择结构

### 4.2.1 if语句

`if` 语句用于在条件为真时执行代码块。

**语法**

```c
if (条件) {
    // 条件为真时执行的代码
}
```

**示例**

```c
int age = 18;
if (age >= 18) {
    printf("You are an adult.\n");
}
```

### 4.2.2 if-else语句

`if-else` 语句用于在条件为真时执行一个代码块，否则执行另一个代码块。**语法**

```c
if (条件) {
    // 条件为真时执行的代码
} else {
    // 条件为假时执行的代码
}
```

**示例**

```c
int age = 16;
if (age >= 18) {
    printf("You are an adult.\n");
} else {
    printf("You are a minor.\n");
}
```

### 4.2.3 多条件语句

`if-else if-else` 语句用于处理多个条件。

**语法**

```c
if (条件1) {
    // 条件1为真时执行的代码
} else if (条件2) {
    // 条件2为真时执行的代码
} else {
    // 所有条件为假时执行的代码
}
```

**示例**

```c
int score = 85;
if (score >= 90) {
    printf("Grade: A\n");
} else if (score >= 80) {
    printf("Grade: B\n");
} else if (score >= 70) {
    printf("Grade: C\n");
} else {
    printf("Grade: F\n");
}
```

### 4.2.4 switch语句

`switch` 语句用于根据变量的值执行不同的代码块。

```c
switch (表达式) {
    case 值1:
        // 表达式等于值1时执行的代码
        break;
    case 值2:
        // 表达式等于值2时执行的代码
        break;
    default:
        // 表达式不等于任何值时执行的代码
}
```

**示例**

```c
int day = 3;
switch (day) {
    case 1:
        printf("Monday\n");
        break;
    case 2:
        printf("Tuesday\n");
        break;
    case 3:
        printf("Wednesday\n");
        break;
    default:
        printf("Invalid day\n");
}
```

**注意事项**

- `switch` 表达式必须是整型或字符型。
- 每个 `case` 块通常以 `break` 结束，否则会继续执行下一个 `case` 块。
- `default` 块是可选的，用于处理未匹配的情况。

### 4.2.5 嵌套选择结构

选择结构可以嵌套使用。**示例**

```c
int age = 20;
char gender = 'M';

if (age >= 18) {
    if (gender == 'M') {
        printf("Adult male\n");
    } else {
        printf("Adult female\n");
    }
} else {
    printf("Minor\n");
}
```

### 4.2.6 三元运算符

三元运算符是一种简化的选择结构。**语法**

```c
条件 ? 表达式1 : 表达式2;
```

**示例**

```c
int age = 20;
char *status = (age >= 18) ? "Adult" : "Minor";
printf("%s\n", status);
```

### 4.2.7 注意事项说明

1. **条件表达式**： 条件表达式的结果必须是 `true`（非零）或 `false`（零）。
2. `if` 和 `else` 的匹配**：`else` 总是与最近的 `if` 匹配。
3. `switch` 的 `break`：如果忘记写 `break`，程序会继续执行下一个 `case` 块。
4. **代码风格**：使用大括号 `{}` 明确代码块的范围，即使只有一行代码。

### 4.2.8 综合示例

```c
#include <stdio.h>

int main() {
    int score;
    printf("Enter your score: ");
    scanf("%d", &score);

    if (score >= 90) {
        printf("Grade: A\n");
    } else if (score >= 80) {
        printf("Grade: B\n");
    } else if (score >= 70) {
        printf("Grade: C\n");
    } else if (score >= 60) {
        printf("Grade: D\n");
    } else {
        printf("Grade: F\n");
    }

    char grade;
    switch (score / 10) {
        case 10:
        case 9:
            grade = 'A';
            break;
        case 8:
            grade = 'B';
            break;
        case 7:
            grade = 'C';
            break;
        case 6:
            grade = 'D';
            break;
        default:
            grade = 'F';
    }
    printf("Grade: %c\n", grade);

    return 0;
}
```

## 4.3 循环结构

在 C 语言中，**循环结构**用于重复执行一段代码，直到满足特定条件。C 语言提供了三种主要的循环结构：`for` 循环、`while` 循环和 `do-while` 循环。

### 4.3.1 for循环

`for` 循环用于在已知循环次数的情况下重复执行代码。

**语法**

```c
for (初始化; 条件; 更新) {
    // 循环体
}
```

**执行流程**

1. 执行初始化语句。
2. 检查条件是否为真：
    - 如果为真，执行循环体。
    - 如果为假，退出循环。
3. 执行更新语句。
4. 重复步骤 2 和 3。

**示例**

```c
for (int i = 0; i < 5; i++) {
    printf("%d ", i);
}
```

**输出：**

```
0 1 2 3 4
```

### 4.3.2 while循环

`while` 循环用于在条件为真时重复执行代码。

**语法**

```c
while (条件) {
    // 循环体
}
```

**执行流程**

1. 检查条件是否为真：
    - 如果为真，执行循环体。
    - 如果为假，退出循环。
2. 重复步骤 1。

**示例**

```c
int i = 0;
while (i < 5) {
    printf("%d ", i);
    i++;
}
```

**输出：**
```
0 1 2 3 4
```

### 4.3.3 do-while循环

`do-while` 循环先执行循环体，然后检查条件是否为真。

**语法**
```c
do {
    // 循环体
} while (条件);
```

**执行流程**
1. 执行循环体。
2. 检查条件是否为真：
    - 如果为真，重复步骤 1。
    - 如果为假，退出循环。

**示例**

```c
int i = 0;
do {
    printf("%d ", i);
    i++;
} while (i < 5);
```

**输出：**
```
0 1 2 3 4
```

### 4.3.4 嵌套循环

循环可以嵌套使用，即在一个循环中包含另一个循环。

**示例**

```c
for (int i = 1; i <= 3; i++) {
    for (int j = 1; j <= 3; j++) {
        printf("(%d, %d) ", i, j);
    }
    printf("\n");
}
```

**输出：**
```
(1, 1) (1, 2) (1, 3) 
(2, 1) (2, 2) (2, 3) 
(3, 1) (3, 2) (3, 3) 
```

### 4.3.5 无限循环

无限循环是指条件永远为真的循环，通常用于需要持续运行的程序。

**示例**

```c
while (1) {
    printf("This is an infinite loop.\n");
}
```

### 4.3.6 循环注意事项

1. **循环条件**：确保循环条件最终会变为假，否则会导致无限循环。
2. **循环变量**：在 `for` 循环中，循环变量的作用域仅限于循环体。
3. **代码风格**：使用大括号 `{}` 明确循环体的范围，即使只有一行代码。
4. **性能优化**：避免在循环体内执行不必要的操作，以提高性能。

## 4.4 跳出循环
### 4.4.1 break语句

`break` 语句用于立即退出循环。

**示例**
```c
for (int i = 0; i < 10; i++) {
    if (i == 5) {
        break;
    }
    printf("%d ", i);
}
```

**输出：**
```
0 1 2 3 4
```


### 4.4.2 continue语句

`continue` 语句用于跳过当前循环的剩余部分，直接进入下一次循环。

**示例**
```c
for (int i = 0; i < 5; i++) {
    if (i == 2) {
        continue;
    }
    printf("%d ", i);
}
```


### 4.4.3 goto语句












