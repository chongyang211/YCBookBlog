#### 目录介绍
- [3.1 算术运算符](#31-算术运算符)
  - [3.1.1 加法运算符](#311-加法运算符)
  - [3.1.2 余数运算符](#312-余数运算符)
  - [3.1.3 自增和自减](#313-自增和自减)
- [3.2 赋值运算符](#32-赋值运算符)
  - [3.2.1 赋值运算符](#321-赋值运算符)
- [3.3 比较运算符](#33-比较运算符)
  - [3.3.1 相等运算符](#331-相等运算符)
  - [3.3.2 严格等于](#332-严格等于)
- [3.4 逻辑运算符](#34-逻辑运算符)
- [3.5 位运算符](#35-位运算符)
- [3.6 三元运算符](#36-三元运算符)
- [3.7 类型运算符](#37-类型运算符)
- [3.8 其他运算符](#38-其他运算符)
- [3.9 运算符优先级](#39-运算符优先级)
- [3.10 综合案例](#310-综合案例)
- [3.11 思考题](#311-思考题)



## 3.1 算术运算符

用于执行基本的数学运算。

| 运算符 | 描述           | 示例           |
|--------|----------------|----------------|
| `+`    | 加法           | `3 + 2` → `5`  |
| `-`    | 减法           | `5 - 2` → `3`  |
| `*`    | 乘法           | `3 * 2` → `6`  |
| `/`    | 除法           | `6 / 2` → `3`  |
| `%`    | 取余           | `5 % 2` → `1`  |
| `**`   | 指数（幂）     | `2 ** 3` → `8` |
| `++`   | 自增           | `let a = 1; a++` → `2` |
| `--`   | 自减           | `let a = 1; a--` → `0` |

### 3.1.1 加法运算符

加法运算符（`+`）是最常见的运算符，用来求两个数值的和。

```javascript
1 + 1 // 2
true + true // 2
1 + true // 2
'a' + 'bc' // "abc"
```

加法运算符是在运行时决定，到底是执行相加，还是执行连接。也就是说，运算子的不同，导致了不同的语法行为，这种现象称为"重载"（overload）。由于加法运算符存在重载，可能执行两种运算，使用的时候必须很小心。

**加法运算符的底层原理**：V8 引擎在执行 `+` 运算时，会先对两个操作数调用内部的 `ToPrimitive()` 抽象操作，将对象转为原始值。如果转换结果中**有一个是字符串**，则另一个也被转为字符串，执行字符串拼接（String Concatenation）；否则两个操作数都被转为数字执行数值加法。这就解释了为什么 `'3' + 4 + 5` 结果是 `"345"`（从左到右，`'3' + 4` 先变成 `"34"`，再 `"34" + 5` 变成 `"345"`），而 `3 + 4 + '5'` 结果是 `"75"`（`3 + 4` 先算出 `7`，再 `7 + '5'` 变成 `"75"`）。

```javascript
'3' + 4 + 5 // "345"
3 + 4 + '5' // "75"
```

### 3.1.2 余数运算符

余数运算符（`%`）返回前一个运算子被后一个运算子除，所得的余数。

```javascript
12 % 5 // 2

// 需要注意的是，运算结果的正负号由第一个运算子的正负号决定。
-1 % 2 // -1
1 % -2 // 1

// 余数运算符还可以用于浮点数的运算。但是，由于浮点数不是精确的值，无法得到完全准确的结果。
6.5 % 2.1   // 0.19999999999999973
```

### 3.1.3 自增和自减

自增和自减运算符，是一元运算符，只需要一个运算子。它们的作用是将运算子首先转为数值，然后加上1或者减去1。它们会修改原始变量。

```javascript
var x = 1;
++x // 2
x // 2

--x // 1
x // 1
```

## 3.2 赋值运算符

用于为变量赋值。

| 运算符 | 描述           | 示例                |
|--------|----------------|---------------------|
| `=`    | 赋值           | `let a = 5`         |
| `+=`   | 加后赋值       | `a += 2` → `a = a + 2` |
| `-=`   | 减后赋值       | `a -= 2` → `a = a - 2` |
| `*=`   | 乘后赋值       | `a *= 2` → `a = a * 2` |
| `/=`   | 除后赋值       | `a /= 2` → `a = a / 2` |
| `%=`   | 取余后赋值     | `a %= 2` → `a = a % 2` |
| `**=`  | 指数后赋值     | `a **= 2` → `a = a ** 2` |

### 3.2.1 赋值运算符

赋值运算符（Assignment Operators）用于给变量赋值。最常见的赋值运算符，当然就是等号（`=`）。

```javascript
// 将 1 赋值给变量 x
var x = 1;

// 将变量 y 的值赋值给变量 x
var x = y;
```

## 3.3 比较运算符

用于比较两个值，返回布尔值（`true` 或 `false`）。

| 运算符 | 描述           | 示例                |
|--------|----------------|---------------------|
| `==`   | 等于（值相等） | `5 == '5'` → `true` |
| `===`  | 严格等于（值和类型都相等） | `5 === '5'` → `false` |
| `!=`   | 不等于         | `5 != '5'` → `false` |
| `!==`  | 严格不等于     | `5 !== '5'` → `true` |
| `>`    | 大于           | `5 > 3` → `true`    |
| `<`    | 小于           | `5 < 3` → `false`   |
| `>=`   | 大于等于       | `5 >= 5` → `true`   |
| `<=`   | 小于等于       | `5 <= 3` → `false`  |

### 3.3.1 相等运算符

描述：比较两个值是否相等（会进行类型转换）。

```javascript
1 == 1.0
// 等同于
1 === 1.0
```

相等运算符隐藏的类型转换，会带来一些违反直觉的结果。

```javascript
0 == ''             // true
0 == '0'            // true

2 == true           // false
2 == false          // false

false == 'false'    // false
false == '0'        // true

false == undefined  // false
false == null       // false
null == undefined   // true

' \t\r\n ' == 0     // true
```

### 3.3.2 严格等于

描述：比较两个值是否相等（不会进行类型转换，值和类型都必须相同）。

如果两个值的类型不同，直接返回`false`。

```javascript
1 === "1" // false
true === "true" // false
```

同一类型的原始类型的值（数值、字符串、布尔值）比较时，值相同就返回`true`，值不同就返回`false`。

```javascript
1 === 0x1 // true
```

`undefined`和`null`与自身严格相等。

```javascript
undefined === undefined // true
null === null // true
```

## 3.4 逻辑运算符

用于组合或操作布尔值。

| 运算符 | 描述           | 示例                |
|--------|----------------|---------------------|
| `&&`   | 逻辑与         | `true && false` → `false` |
| `||`   | 逻辑或         | `true || false` → `true`  |
| `!`    | 逻辑非         | `!true` → `false`         |

## 3.5 位运算符

用于对二进制数进行操作。

| 运算符 | 描述           | 示例                |
|--------|----------------|---------------------|
| `&`    | 按位与         | `5 & 1` → `1`       |
| `|`    | 按位或         | `5 | 1` → `5`       |
| `^`    | 按位异或       | `5 ^ 1` → `4`       |
| `~`    | 按位非         | `~5` → `-6`         |
| `<<`   | 左移           | `5 << 1` → `10`     |
| `>>`   | 右移           | `5 >> 1` → `2`      |
| `>>>`  | 无符号右移     | `-5 >>> 1` → `2147483645` |

## 3.6 三元运算符

用于简化 `if-else` 语句。

| 运算符 | 描述           | 示例                |
|--------|----------------|---------------------|
| `? :`  | 三元运算符     | `5 > 3 ? 'Yes' : 'No'` → `'Yes'` |

## 3.7 类型运算符

用于检查变量类型。

| 运算符 | 描述           | 示例                |
|--------|----------------|---------------------|
| `typeof` | 返回变量类型 | `typeof 5` → `'number'` |
| `instanceof` | 检查对象是否为某类的实例 | `[] instanceof Array` → `true` |


## 3.8 其他运算符

| 运算符 | 描述           | 示例                |
|--------|----------------|---------------------|
| `,`    | 逗号运算符     | `let a = (1, 2, 3)` → `a = 3` |
| `?.`   | 可选链运算符   | `obj?.prop` → 如果 `obj` 为 `null` 或 `undefined`，返回 `undefined` |
| `??`   | 空值合并运算符 | `null ?? 'default'` → `'default'` |
| `...`  | 展开运算符     | `[...[1, 2], 3]` → `[1, 2, 3]` |

## 3.9 运算符优先级

JavaScript 中的运算符优先级决定了表达式中运算的顺序。以下是一些常见的优先级（从高到低）：

1. `()`（分组）
2. `++`, `--`（自增、自减）
3. `*`, `/`, `%`（乘法、除法、取余）
4. `+`, `-`（加法、减法）
5. `<`, `<=`, `>`, `>=`（比较）
6. `==`, `!=`, `===`, `!==`（相等）
7. `&&`（逻辑与）
8. `||`（逻辑或）
9. `=`（赋值）

**短路求值的底层原理**：逻辑运算符 `&&` 和 `||` 在 JavaScript 中实现了**短路求值（Short-circuit Evaluation）**。`&&` 在第一个操作数为 falsy 时直接返回第一个操作数，不再计算第二个；`||` 在第一个操作数为 truthy 时直接返回第一个操作数。注意它们返回的是**操作数的值本身**，而不是布尔值。这是 `a || 'default'` 设置默认值和 `obj && obj.prop` 安全访问属性的原理。ES2020 引入的 `??`（空值合并运算符）则只在左侧为 `null` 或 `undefined` 时才返回右侧，避免了 `||` 对 `0`、`''`、`false` 等 falsy 值的误判。

## 3.10 综合案例

**案例：表达式求值与类型转换陷阱**

```javascript
// 隐式类型转换陷阱大全
const traps = [
  { expr: '"5" + 3',       result: "5" + 3,       expected: '"53"' },
  { expr: '"5" - 3',       result: "5" - 3,       expected: '2' },
  { expr: 'true + true',   result: true + true,   expected: '2' },
  { expr: '[] + []',       result: [] + [],       expected: '""' },
  { expr: '[] + {}',       result: [] + {},       expected: '"[object Object]"' },
  { expr: 'null + 1',      result: null + 1,      expected: '1' },
  { expr: 'undefined + 1', result: undefined + 1, expected: 'NaN' },
  { expr: '"" == false',   result: "" == false,   expected: 'true' },
  { expr: '"0" == false',  result: "0" == false,  expected: 'true' },
  { expr: '0 ?? "default"',result: 0 ?? "default",expected: '0' },
  { expr: '0 || "default"',result: 0 || "default",expected: '"default"' },
];

console.table(traps.map(t => ({
  表达式: t.expr,
  结果: JSON.stringify(t.result),
  说明: t.expected
})));

// 安全的数值比较
function safeEqual(a, b, precision = 1e-10) {
  return Math.abs(a - b) < precision;
}
console.log('0.1 + 0.2 === 0.3:', 0.1 + 0.2 === 0.3);       // false
console.log('safeEqual:', safeEqual(0.1 + 0.2, 0.3));         // true
```

## 3.11 思考题

1. `==` 在比较 `null` 和 `undefined` 时的规则是什么？为什么 `null == undefined` 为 `true` 但 `null == 0` 为 `false`？
2. 位运算符 `|0` 常被用来快速取整（如 `3.7 | 0` 得到 `3`），这是为什么？有什么限制？
3. `?.`（可选链运算符）和 `??`（空值合并运算符）在实际项目中如何配合使用？请举例说明。
4. 为什么 `typeof NaN === "number"`？`NaN === NaN` 为什么是 `false`？如何正确判断一个值是否为 `NaN`？
5. 短路求值 `a && b()` 和 `if (a) b()` 在性能和可读性上有什么区别？在什么场景下使用哪种更合适？


