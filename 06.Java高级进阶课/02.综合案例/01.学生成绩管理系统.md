# 01.学生成绩管理系统（骨架版）

> 入门教程第 1-6 章的"小白起步案例"。**不用 class、不用集合、不用异常**，只用基本类型 + 数组 + 静态方法 + Scanner。让读者第一次写出"超过 300 行还能维护"的 Java 程序。
>
> 📍 **当前文档为骨架版**，详细内容将在后续轮次扩写至 ~50 KB 完整教学文档。

---

## 00.案例元信息

| 项目 | 内容 |
|------|------|
| 难度 | ★☆☆☆☆ |
| 预估时长 | 3 小时 |
| 前置章节 | 入门第 1-6 章（基础语法 / 数据类型 / 运算符 / 字符串和数组 / 流程语句 / 函数方法）|
| 编译命令 | `javac StudentScore.java && java StudentScore` |
| 最终产物 | 单文件 `StudentScore.java`（~ 350 行）+ CSV 数据文件 `scores.csv`（仅作为成果展示，文件读写实际是 02 案例引入）|
| 代码规模 | 约 350 行（含注释）|

---

## 01.教学目标（首先确认这个案例要练什么）

**主练 6 个章节的 12 个核心知识点**：

| 章节 | 知识点 | 在本案例的落地点 |
|---|---|---|
| 第 1 章 | `package` / `import` / `static final` | 文件头声明 + 常量 `MAX_STUDENTS = 50` |
| 第 1 章 | 单行 / 多行 / 文档注释 | 每个静态方法的 javadoc 注释 |
| 第 2 章 | `int` / `double` / `String` 基本类型 | 学号 / 分数 / 姓名字段 |
| 第 2 章 | 包装类 `Integer.parseInt` / `Double.parseDouble` | 解析用户输入字符串 |
| 第 3 章 | 算术 / 比较 / 逻辑运算符 | 计算总分 / 平均分 / 及格判断 |
| 第 4 章 | `String` 不可变 / `StringBuilder` | 拼接 CSV 行用 StringBuilder |
| 第 4 章 | 二维数组 `String[][]` / `double[][]` | 班级分数表 |
| 第 4 章 | `String.split` / `String.format` | CSV 解析与格式化 |
| 第 5 章 | `if-else` / `switch` 字符串 | 菜单分发 |
| 第 5 章 | `while` / `for` / 增强 for | 主菜单循环 / 遍历分数 |
| 第 5 章 | `break` / `continue` | 退出主循环 / 跳过非法输入 |
| 第 6 章 | 静态方法 + 重载 + 可变参数 | 业务方法拆分 / `printSeparator(char...)` |

---

## 02.分阶段开发计划（共 6 个阶段，每阶段 30-40 分钟）

### 🎯 阶段① · 跑通 Hello（30 min）

**目标**：编译命令熟悉 + 主菜单空壳 + 死循环不崩溃

| Step | 内容 | 教学高峰 |
|---|---|---|
| Step 1.1 | `public class StudentScore` 与 `main` 方法骨架 | — |
| Step 1.2 | `Scanner sc = new Scanner(System.in)` 读输入 | — |
| Step 1.3 | 主菜单 while + switch 字符串分发 | — |
| Step 1.4 | 7 个菜单项打 `// TODO` 占位 | — |

**📌 阶段① 小结**：~50 行，菜单可循环、能正常退出。

---

### 🎯 阶段② · 数据结构定义（30 min）

**目标**：用三个并行数组表达学生成绩表

| Step | 内容 | 教学高峰 |
|---|---|---|
| Step 2.0 | 🤔 **灵魂三问**：为什么不用 class？为什么用三个并行数组而不是一个二维数组？为什么 `MAX_STUDENTS = 50` 写成 `static final`？| 灵魂三问 #1 |
| Step 2.1 | 全局静态字段 `String[] names` / `int[] ids` / `double[][] scores`（每人 3 科）| — |
| Step 2.2 | 全局静态字段 `static int count` 表示当前学生数 | — |
| Step 2.3 | 初始化方法 `initData()` 灌 5 条假数据 | — |

**📌 阶段② 小结**：~80 行，数据结构就位，可以打印固定 5 人的表。

---

### 🎯 阶段③ · 增删查改 4 个核心方法（45 min）

**目标**：实现 CRUD 静态方法

| Step | 内容 | 教学高峰 |
|---|---|---|
| Step 3.1 | `addStudent()` 新增学生 + 边界判断（满了？学号重复？）| — |
| Step 3.2 | `deleteStudent(int id)` 删除学生（用"末尾元素填洞法"避免大量移动）| — |
| Step 3.3 | `findStudent(int id)` 查找学生 → 返回索引 | — |
| Step 3.4 | ⚠️ **故意造 BUG**：用 `==` 比较 `String name`（如查找时按姓名匹配） | 造 BUG #1：`==` vs `.equals()` |
| Step 3.5 | 修复：改用 `name.equals(target)`，并解释字符串常量池 | — |
| Step 3.6 | `updateScore(int id, int subject, double score)` 改分数 | — |

**📌 阶段③ 小结**：~180 行，CRUD 全通，能完成主菜单 70% 功能。

---

### 🎯 阶段④ · 统计计算（30 min）

**目标**：练算术运算符 + 数组遍历

| Step | 内容 | 教学高峰 |
|---|---|---|
| Step 4.0 | 🤔 **灵魂三问**：为什么平均分用 `double` 不用 `int`？为什么循环里要用 `++count` 而不是 `count = count + 1`？为什么及格判断要用 `>= 60` 而不是 `> 59`？| 灵魂三问 #2 |
| Step 4.1 | `calcTotal(int idx)` 算单人总分 | — |
| Step 4.2 | `calcAverage(int idx)` 算单人平均分 | — |
| Step 4.3 | `calcSubjectAvg(int subject)` 算全班单科平均分 | — |
| Step 4.4 | `findTopStudent()` 找全班总分最高 → 用"打擂台"算法 | — |
| Step 4.5 | `countPassed(int subject)` 统计单科及格人数 | — |

**📌 阶段④ 小结**：~240 行，统计功能齐全。

---

### 🎯 阶段⑤ · 排序与展示（30 min）

**目标**：手写冒泡排序 + 表格化输出

| Step | 内容 | 教学高峰 |
|---|---|---|
| Step 5.1 | `sortByTotal()` 按总分降序——**手写冒泡** | — |
| Step 5.2 | ⚠️ **故意造 BUG**：排序时只交换 `scores[][]` 不交换 `names[]` 和 `ids[]` → 数据错乱 | 造 BUG #2：并行数组同步交换 |
| Step 5.3 | 修复：抽出 `swapAll(i, j)` 静态方法一次换三个数组 | — |
| Step 5.4 | `printTable()` 用 `String.format("%-10s%-8d%6.2f", ...)` 对齐输出 | — |
| Step 5.5 | `printSeparator(char... chars)` 演示**可变参数** | — |

**📌 阶段⑤ 小结**：~290 行，能输出对齐的成绩表。

---

### 🎯 阶段⑥ · 输入校验与健壮性（30 min）

**目标**：让程序"输入垃圾不崩溃"

| Step | 内容 | 教学高峰 |
|---|---|---|
| Step 6.0 | 🤔 **灵魂三问**：为什么不用 try-catch（剧透：第 10 章才学）？为什么 Scanner 要用 `nextLine` 不用 `nextInt`？为什么用包装类 `Integer.parseInt` 解析？| 灵魂三问 #3 |
| Step 6.1 | 自定义 `readInt(String prompt, int min, int max)` 静态方法：循环直到合法 | — |
| Step 6.2 | 自定义 `readDouble` / `readNonEmptyString` 同款 | — |
| Step 6.3 | 用方法重载演示：`readInt(String prompt)` 不带范围版本 | — |
| Step 6.4 | 主菜单全部改用 `readInt` 替代裸 `sc.nextLine()` | — |

**📌 阶段⑥ 小结**：~350 行，输入再野蛮也不崩。

---

## 03.教学高峰索引

| 类型 | 数量 | 位置 |
|---|---|---|
| 🤔 灵魂三问 | 3 处 | Step 2.0 / Step 4.0 / Step 6.0 |
| ⚠️ 造 BUG | 2 处 | Step 3.4（== vs equals）/ Step 5.2（并行数组同步交换）|
| 📌 阶段小结 | 6 处 | 每阶段末尾 |

---

## 04.项目总结（待扩写）

- 整体架构图：单文件 + 静态方法群
- 与下一案例的差异对比表（CSV 持久化 / class 化 / 异常处理 这三件事在 02 案例统一升级）

---

## 05.衔接与延伸（待扩写）

- **痛点引子（→ 02 案例）**：本案例数据只在内存、关程序就丢；并行数组同步交换易出错；扩展第 4 科目要改 N 处代码
- **延伸挑战 3 个**：
  - 基础：加一个"按学号排序"功能
  - 进阶：改用 `String.format` 输出 Markdown 表格到控制台
  - 现代化：用 `Comparator.comparingDouble` + `Arrays.sort` 重写阶段⑤（剧透 03 案例的 Lambda 用法）

---

## 06.附：完整目录（扩写时填入）

> 后续轮次扩写时，本节将变成"目录快速导航"，引用所有 Step 锚点。

---

> 📌 **本骨架文档**：~ 7 KB，扩写后预计 **~ 50 KB**（约 1100 行 Markdown + 350 行 Java 代码）。
