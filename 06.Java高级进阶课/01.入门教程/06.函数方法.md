# 06.函数方法
#### 目录介绍
- [6.1 概述和定义](#61-概述和定义)
  - [6.1.1 方法概念](#611-方法概念)
  - [6.1.2 方法声明和定义](#612-方法声明和定义)
  - [6.1.3 方法分类](#613-方法分类)
  - [6.1.4 静态方法和实例方法](#614-静态方法和实例方法)
- [6.2 方法参数](#62-方法参数)
  - [6.2.1 值传递](#621-值传递)
  - [6.2.2 引用类型传参](#622-引用类型传参)
  - [6.2.3 可变参数](#623-可变参数)
- [6.3 方法返回值](#63-方法返回值)
- [6.4 方法重载](#64-方法重载)
  - [6.4.1 方法重载案例](#641-方法重载案例)
  - [6.4.2 重载注意事项](#642-重载注意事项)
- [6.5 递归](#65-递归)
  - [6.5.1 递归概念](#651-递归概念)
  - [6.5.2 递归案例](#652-递归案例)
- [6.6 方法引用（JDK8+）](#66-方法引用jdk8)
- [6.7 综合案例：数学工具箱](#67-综合案例数学工具箱)
- [6.8 思考题](#68-思考题)


## 6.1 概述和定义

### 6.1.1 方法概念

**方法**（Method）是 Java 中一段可重用的代码块，用于执行特定的任务。Java 中的方法**必须定义在类中**，不能像 C++ 那样定义独立的函数。

> Java 中"函数"叫做"方法"，因为所有代码都必须在类中。

### 6.1.2 方法声明和定义

```java
// 方法定义的语法
访问修饰符 返回类型 方法名(参数列表) {
    方法体;
    return 返回值;
}
```

示例：

```java
public class Calculator {
    // 有参有返回值
    public int add(int a, int b) {
        return a + b;
    }

    // 无参无返回值
    public void sayHello() {
        System.out.println("Hello!");
    }

    // 有参无返回值
    public void printSum(int a, int b) {
        System.out.println("sum = " + (a + b));
    }
}
```

**对比 C++**：C++ 的函数可以定义在类外面，Java 的方法必须在类里面。C++ 需要在头文件中声明、在 cpp 文件中实现，Java 声明和实现在一起。

### 6.1.3 方法分类

```java
public class Account {
    private double balance;

    // 实例方法：需要通过对象调用
    public double getBalance() {
        return balance;
    }

    // 静态方法：通过类名调用
    public static double calculateInterest(double amount, double rate) {
        return amount * rate;
    }
}

// 调用
Account acc = new Account();
acc.getBalance();                         // 实例方法
Account.calculateInterest(1000, 0.05);    // 静态方法
```

### 6.1.4 静态方法和实例方法

| 对比项 | 静态方法 | 实例方法 |
|--------|---------|---------|
| 关键字 | `static` 修饰 | 无 `static` |
| 调用方式 | 类名.方法名() | 对象.方法名() |
| 能否访问实例变量 | 不能 | 能 |
| 能否使用 this | 不能 | 能 |
| 典型用途 | 工具方法、工厂方法 | 操作对象状态 |

```java
public class MathUtils {
    // 静态方法：不依赖对象状态
    public static int max(int a, int b) {
        return a > b ? a : b;
    }
}

// 直接用类名调用
int result = MathUtils.max(10, 20);
```

## 6.2 方法参数

### 6.2.1 值传递

**Java 只有值传递**，没有 C++ 那样的引用传递。

**值传递的底层原理**：在 JVM 层面，每次方法调用会在**栈帧（Stack Frame）**中创建一个新的**局部变量表（Local Variable Table）**。方法参数被复制到新栈帧的局部变量表中。对于基本类型，复制的是值本身（如 `int a = 10`，复制的是 `10`）；对于引用类型，复制的是**引用值**（即对象在堆上的地址），所以通过这个引用副本可以修改堆上的对象内容，但无法让原引用指向新对象。这就是"Java 只有值传递"的本质。

对于**基本类型**，传递的是值的副本：

```java
public static void change(int num) {
    num = 100;  // 只修改了副本
}

public static void main(String[] args) {
    int a = 10;
    change(a);
    System.out.println(a);  // 10（没有改变）
}
```

### 6.2.2 引用类型传参

对于**引用类型**，传递的是引用的副本（地址的副本），所以可以通过引用修改对象的内容：

```java
public static void changeName(Account acc) {
    acc.setName("李四");  // 通过引用修改对象内容，有效
}

public static void reassign(Account acc) {
    acc = new Account("王五");  // 只改变了副本的指向，无效
}

public static void main(String[] args) {
    Account a = new Account("张三");
    changeName(a);
    System.out.println(a.getName());  // 李四（对象被修改了）

    reassign(a);
    System.out.println(a.getName());  // 李四（没有变成王五）
}
```

**对比 C++**：C++ 有值传递、引用传递（`&`）、指针传递（`*`）三种方式。Java 只有值传递，但引用类型的值传递效果类似于 C++ 的指针传递。

### 6.2.3 可变参数

Java 5 引入了可变参数（varargs），用 `...` 表示：

```java
public static int sum(int... nums) {
    int total = 0;
    for (int num : nums) {
        total += num;
    }
    return total;
}

// 调用
System.out.println(sum(1, 2, 3));        // 6
System.out.println(sum(1, 2, 3, 4, 5));  // 15
System.out.println(sum());               // 0
```

> 注意：可变参数必须是方法的最后一个参数，一个方法只能有一个可变参数。

## 6.3 方法返回值

```java
// 返回基本类型
public static int add(int a, int b) {
    return a + b;
}

// 返回引用类型
public static int[] createArray(int size) {
    return new int[size];
}

// 返回 void（无返回值）
public static void printMessage(String msg) {
    System.out.println(msg);
    // return;  // 可以省略
}
```

## 6.4 方法重载

### 6.4.1 方法重载案例

方法重载（Overload）是指在同一个类中，方法名相同但**参数列表不同**：

```java
public class Calculator {
    public int add(int a, int b) {
        return a + b;
    }

    public double add(double a, double b) {
        return a + b;
    }

    public int add(int a, int b, int c) {
        return a + b + c;
    }
}

Calculator calc = new Calculator();
System.out.println(calc.add(1, 2));        // 调用 int 版本
System.out.println(calc.add(1.5, 2.5));    // 调用 double 版本
System.out.println(calc.add(1, 2, 3));     // 调用三参数版本
```

### 6.4.2 重载注意事项

1. 参数列表必须不同（参数个数、类型或顺序不同）。
2. **返回类型不同不构成重载**。
3. 访问修饰符不同也不构成重载。

```java
// 以下两个方法不构成重载，会编译错误
// public int add(int a, int b) { return a + b; }
// public double add(int a, int b) { return a + b; }  // 只有返回类型不同
```

**对比 C++**：C++ 也支持函数重载，规则基本相同。

## 6.5 递归

### 6.5.1 递归概念

递归是指方法调用自身。递归必须有**终止条件**（基准情形），否则会无限递归导致栈溢出（`StackOverflowError`）。

**递归的底层原理**：每次方法调用都会在 JVM 栈中压入一个新的**栈帧**，包含局部变量表、操作数栈、返回地址等。递归调用 N 次就会有 N 个栈帧同时存在。JVM 栈的大小是有限的（默认约 512KB~1MB，可通过 `-Xss` 参数调整），所以递归深度过大会导致 `StackOverflowError`。Java **不支持尾递归优化**（TCO），因此即使是尾递归写法，JVM 也不会自动优化成循环。对于深层递归问题，建议改写为**迭代 + 显式栈**的方式。

### 6.5.2 递归案例

```java
// 阶乘
public static long factorial(int n) {
    if (n <= 1) return 1;       // 终止条件
    return n * factorial(n - 1); // 递归调用
}

// 斐波那契数列
public static int fibonacci(int n) {
    if (n <= 1) return n;
    return fibonacci(n - 1) + fibonacci(n - 2);
}

public static void main(String[] args) {
    System.out.println(factorial(5));   // 120
    System.out.println(fibonacci(10));  // 55
}
```

## 6.6 方法引用（JDK8+）

JDK 8 引入了方法引用，是 Lambda 表达式的简写：

```java
import java.util.Arrays;

String[] names = {"Charlie", "Alice", "Bob"};

// Lambda 写法
Arrays.sort(names, (a, b) -> a.compareTo(b));

// 方法引用写法
Arrays.sort(names, String::compareTo);

System.out.println(Arrays.toString(names));
// [Alice, Bob, Charlie]
```

## 6.7 综合案例：数学工具箱

本案例融合函数方法章节的核心知识点：方法定义、参数传递（值传递/引用传递）、可变参数、方法重载、递归、静态方法、方法引用等。

```java
import java.util.Arrays;

/**
 * 数学工具箱
 * 融合知识点：方法重载、可变参数、递归、值传递、方法引用、静态方法
 */
public class MathToolbox {

    // ========== 方法重载：求最大值 ==========
    public static int max(int a, int b) {
        return a > b ? a : b;
    }

    public static double max(double a, double b) {
        return a > b ? a : b;
    }

    public static int max(int a, int b, int c) {
        return max(max(a, b), c);  // 复用两参数版本
    }

    // ========== 可变参数：求和 ==========
    public static int sum(int... nums) {
        int total = 0;
        for (int n : nums) total += n;
        return total;
    }

    // 可变参数 + 固定参数
    public static double weightedAverage(double[] weights, double... values) {
        if (weights.length != values.length) {
            throw new IllegalArgumentException("权重和数值数量不匹配");
        }
        double weightedSum = 0, weightSum = 0;
        for (int i = 0; i < values.length; i++) {
            weightedSum += values[i] * weights[i];
            weightSum += weights[i];
        }
        return weightedSum / weightSum;
    }

    // ========== 递归：经典问题 ==========

    // 阶乘（递归）
    public static long factorial(int n) {
        if (n < 0) throw new IllegalArgumentException("不能为负数");
        if (n <= 1) return 1;
        return n * factorial(n - 1);
    }

    // 斐波那契（带记忆化的递归）
    private static long[] memo = new long[100];
    public static long fibonacci(int n) {
        if (n <= 1) return n;
        if (memo[n] != 0) return memo[n];
        memo[n] = fibonacci(n - 1) + fibonacci(n - 2);
        return memo[n];
    }

    // 汉诺塔（递归经典）
    public static void hanoi(int n, char from, char helper, char to) {
        if (n == 1) {
            System.out.printf("  移动盘子 %d：%c → %c%n", n, from, to);
            return;
        }
        hanoi(n - 1, from, to, helper);
        System.out.printf("  移动盘子 %d：%c → %c%n", n, from, to);
        hanoi(n - 1, helper, from, to);
    }

    // ========== 值传递演示 ==========

    // 基本类型：无法修改原值
    public static void tryModify(int value) {
        value = 999;
    }

    // 数组（引用类型）：可以修改内容
    public static void doubleArray(int[] arr) {
        for (int i = 0; i < arr.length; i++) {
            arr[i] *= 2;
        }
    }

    // 交换数组中两个元素（利用引用传递的特性）
    public static void swap(int[] arr, int i, int j) {
        int temp = arr[i];
        arr[i] = arr[j];
        arr[j] = temp;
    }

    // ========== 方法作为参数的思想 ==========

    // 对数组每个元素应用操作
    public static int[] applyToEach(int[] arr, java.util.function.IntUnaryOperator op) {
        int[] result = new int[arr.length];
        for (int i = 0; i < arr.length; i++) {
            result[i] = op.applyAsInt(arr[i]);
        }
        return result;
    }

    public static void main(String[] args) {
        // 方法重载演示
        System.out.println("=== 方法重载 ===");
        System.out.println("max(10, 20) = " + max(10, 20));
        System.out.println("max(3.14, 2.71) = " + max(3.14, 2.71));
        System.out.println("max(5, 8, 3) = " + max(5, 8, 3));

        // 可变参数演示
        System.out.println("\n=== 可变参数 ===");
        System.out.println("sum() = " + sum());
        System.out.println("sum(1,2,3) = " + sum(1, 2, 3));
        System.out.println("sum(1,2,3,4,5) = " + sum(1, 2, 3, 4, 5));

        double[] weights = {0.3, 0.3, 0.4};
        System.out.printf("加权平均(语:80 数:90 英:85) = %.1f%n",
                weightedAverage(weights, 80, 90, 85));

        // 递归演示
        System.out.println("\n=== 递归 ===");
        System.out.println("5! = " + factorial(5));
        System.out.println("fibonacci(10) = " + fibonacci(10));
        System.out.println("fibonacci(40) = " + fibonacci(40) + "（记忆化，秒出结果）");

        System.out.println("\n汉诺塔（3个盘子）：");
        hanoi(3, 'A', 'B', 'C');

        // 值传递演示
        System.out.println("\n=== 值传递 ===");
        int x = 10;
        tryModify(x);
        System.out.println("修改基本类型后 x = " + x + "（未改变）");

        int[] arr = {1, 2, 3, 4, 5};
        doubleArray(arr);
        System.out.println("修改数组后 arr = " + Arrays.toString(arr) + "（已改变）");

        // 方法引用演示
        System.out.println("\n=== 方法引用 ===");
        String[] names = {"Charlie", "Alice", "Bob"};
        Arrays.sort(names, String::compareTo);  // 方法引用
        System.out.println("排序后：" + Arrays.toString(names));

        // Lambda + 方法作为参数
        int[] nums = {1, 2, 3, 4, 5};
        int[] squared = applyToEach(nums, n -> n * n);
        int[] doubled = applyToEach(nums, n -> n * 2);
        System.out.println("原数组：" + Arrays.toString(nums));
        System.out.println("平方：" + Arrays.toString(squared));
        System.out.println("翻倍：" + Arrays.toString(doubled));
    }
}
```

**运行效果：**

```
=== 方法重载 ===
max(10, 20) = 20
max(3.14, 2.71) = 3.14
max(5, 8, 3) = 8

=== 可变参数 ===
sum() = 0
sum(1,2,3) = 6
sum(1,2,3,4,5) = 15
加权平均(语:80 数:90 英:85) = 85.0

=== 递归 ===
5! = 120
fibonacci(10) = 55
fibonacci(40) = 102334155（记忆化，秒出结果）

汉诺塔（3个盘子）：
  移动盘子 1：A → C
  移动盘子 2：A → B
  移动盘子 1：C → B
  移动盘子 3：A → C
  移动盘子 1：B → A
  移动盘子 2：B → C
  移动盘子 1：A → C

=== 值传递 ===
修改基本类型后 x = 10（未改变）
修改数组后 arr = [2, 4, 6, 8, 10]（已改变）

=== 方法引用 ===
排序后：[Alice, Bob, Charlie]
原数组：[1, 2, 3, 4, 5]
平方：[1, 4, 9, 16, 25]
翻倍：[2, 4, 6, 8, 10]
```

**案例知识点总结：**

| 知识点 | 案例中的体现 |
|--------|-------------|
| 方法定义 | 静态方法、实例方法、有参/无参、有返回值/void |
| 方法重载 | `max(int,int)`、`max(double,double)`、`max(int,int,int)` |
| 可变参数 | `sum(int... nums)`、`weightedAverage(double[], double...)` |
| 递归 | 阶乘、斐波那契（记忆化）、汉诺塔 |
| 值传递 | 基本类型不可修改、引用类型可修改内容 |
| 方法引用 | `String::compareTo` |
| Lambda | `n -> n * n` 作为方法参数 |

## 6.8 思考题

1. **Java 中"只有值传递"，那为什么通过方法可以修改数组内容？请画图解释基本类型传参和引用类型传参的内存模型。**

2. **可变参数 `int... args` 和 数组参数 `int[] args` 有什么区别？它们可以同时存在于方法重载中吗？**

3. **斐波那契数列的普通递归时间复杂度是 O(2^n)，记忆化后是 O(n)。请解释为什么，并尝试用迭代（非递归）方式实现，比较三者的性能。**

4. **以下代码能编译通过吗？如果可以，输出是什么？**
   ```java
   public static void print(int a, int b) { System.out.println("int"); }
   public static void print(long a, long b) { System.out.println("long"); }

   print(1, 2);     // ?
   print(1L, 2);     // ?
   print(1, 2L);     // ?
   ```

5. **请实现一个递归方法 `boolean isPalindrome(String s)`，判断一个字符串是否是回文串（忽略大小写和非字母字符）。例如 "A man, a plan, a canal: Panama" 应返回 true。**


