# 7.1类的加载过程和原理
#### 目录介绍
- 01.类加载概念
  - 1.1 什么是类加载
  - 1.2 类加载的步骤
  - 1.3 类加载器
  - 1.4 类加载机制
  - 1.5 类加载触发时机
  - 1.6 常见问题
- 03.第一阶段加载
  - 3.1 触发加载
  - 3.2 查找.class文件
  - 3.3 读取字节码数据
  - 3.4 创建Class对象
  - 3.5 关联类加载器
- 04.第二阶段连接
  - 4.1 验证（Verification）
  - 4.2 准备（Preparation）
  - 4.3 解析（Resolution）
- 05.第三阶段初始化
  - 5.1 执行静态代码块
  - 5.2 显式赋值
  - 5.3 执行父类初始化
  - 5.4 确保线程安全
- 06.第四阶段使用
  - 6.1 访问类的成员
  - 6.2 调用类静态成员
  - 6.3 使用类的功能
  - 6.4 对象生命周期
- 07.第五阶段卸载
  - 7.1 卸载的条件
  - 7.2 卸载阶段过程
  - 7.3 释放类元数据
  - 7.4 释放类静态变量
  - 7.5 释放类Class对象
  - 7.6 卸载总结



## 01.类加载概念

### 1.1 什么是类加载

类加载（Class Loading）是 Java 虚拟机（JVM）在运行时将类的字节码文件（`.class` 文件）加载到内存中，并转换为 JVM 可以使用的 `Class` 对象的过程。

类加载是 Java 程序运行的基础，它确保了类的动态加载和连接。

### 1.2 类加载的步骤

类加载过程分为以下三个阶段：

1. **加载（Loading）**：
    - 通过类的全限定名（包名 + 类名）查找类的字节码文件（`.class` 文件）。
    - 将字节码文件加载到内存中，并创建一个 `java.lang.Class` 对象来表示该类。

2. **链接（Linking）**：
    - **验证（Verification）**：检查字节码文件的正确性和安全性，确保其符合 JVM 规范。
    - **准备（Preparation）**：为类的静态变量分配内存，并设置默认初始值（如 `0`、`null` 等）。
    - **解析（Resolution）**：将类、方法、字段的符号引用转换为直接引用。

3. **初始化（Initialization）**：
    - 执行类的静态代码块（`static {}`）和静态变量的显式初始化。
    - 这是类加载的最后一步，只有在类首次被主动使用时才会触发。


### 1.3 类加载器

类加载器是负责加载类的组件。JVM 中有以下三种内置的类加载器：

1. **Bootstrap ClassLoader（启动类加载器）**：
    - 负责加载 JVM 核心类库（如 `java.lang.*`、`java.util.*` 等），通常位于 `JAVA_HOME/lib` 目录下。
    - 由 C/C++ 实现，是 JVM 的一部分。

2. **Extension ClassLoader（扩展类加载器）**：
    - 负责加载扩展类库（如 `JAVA_HOME/lib/ext` 目录下的类）。
    - 是 `java.lang.ClassLoader` 的子类。

3. **Application ClassLoader（应用程序类加载器）**：
    - 负责加载应用程序类路径（Classpath）下的类。
    - 是 `java.lang.ClassLoader` 的子类。

### 1.4 类加载机制

1. **双亲委派模型（Parent Delegation Model）**：
    - 当一个类加载器需要加载类时，首先会委托其父类加载器尝试加载。
    - 如果父类加载器无法加载，才会由当前类加载器尝试加载。
    - 这种机制确保了类的唯一性和安全性，避免了类的重复加载。

2. **自定义类加载器**：
    - 可以通过继承 `java.lang.ClassLoader` 类实现自定义类加载器。
    - 常用于动态加载类、热部署、插件化等场景。

### 1.5 类加载触发时机

类加载在以下情况下会被触发：

1. 创建类的实例（如 `new MyClass()`）。
2. 访问类的静态变量或静态方法。
3. 使用反射加载类（如 `Class.forName("com.example.MyClass")`）。
4. 初始化类的子类时，父类会先被加载。
5. JVM 启动时加载包含 `main` 方法的类。

### 1.6 常见问题

1. **ClassNotFoundException**：类加载器找不到指定的类时抛出。通常是由于类路径配置错误或类文件缺失。
2. **NoClassDefFoundError**：类加载器找到了类，但在链接或初始化阶段失败时抛出。通常是由于类的依赖缺失或类文件损坏。
3. **如何打破双亲委派模型**：自定义类加载器时，可以重写 `loadClass` 方法，直接加载类而不委托父类加载器。

## 02.来看一个案例

### 2.1 案例代码

### 2.2 加载（Loading）

### 2.3 连接（Linking）

### 2.4 初始化（Initialization）

**触发时机**：类首次被主动使用时（如创建实例、访问静态变量或静态方法）。

**过程**：

- 执行 `MyClass` 的静态代码块（`static {}`）。
- 执行静态变量的显式初始化（如 `MESSAGE = "Hello, World!"`）。

**结果**：

- 静态代码块输出：`MyClass is loaded and initialized!`
- 静态变量 `MESSAGE` 被赋值为 `"Hello, World!"`。

### 2.5 使用（Usage）

**过程**：

在 `Main` 类的 `main` 方法中，创建 `MyClass` 的实例：

```java
MyClass myClass = new MyClass();
```

调用 `MyClass` 的 `sayHello` 方法：

```java
myClass.sayHello();
```

`sayHello` 方法输出：`Hello, World!`

**结果**：`MyClass` 类的功能被正常使用。

### 2.6 卸载（Unloading）

**触发时机**：当类的 `Class` 对象不再被引用，且满足以下条件时，类会被卸载：

- 类的所有实例都已被回收。
- 类的 `ClassLoader` 已被回收。
- 类的 `Class` 对象不再被引用。

**过程**：

- JVM 的垃圾回收器（GC）回收 `MyClass` 的 `Class` 对象。
- 释放 `MyClass` 类占用的内存。

**结果**：`MyClass` 类被卸载，内存被释放。

### 2.7 该案例总结

类的加载流程包括以下步骤：

1. **加载**：将 `.class` 文件加载到内存中，创建 `Class` 对象。
2. **连接**：
    - 验证：检查字节码的正确性。
    - 准备：为静态变量分配内存并设置默认值。
    - 解析：将符号引用转换为直接引用。
3. **初始化**：执行静态代码块和静态变量的显式初始化。
4. **使用**：创建实例并调用方法。
5. **卸载**：当类不再被使用时，由 JVM 卸载并回收内存。

## 03.第一阶段加载

**加载（Loading）** 是第一个阶段，它的主要任务是将类的字节码文件（`.class` 文件）加载到内存中，并为后续的连接和初始化阶段做准备。

加载阶段是类加载流程的第一步，主要任务包括：

1. 查找和加载 `.class` 文件。
2. 将字节码数据加载到内存中。
3. 创建 `Class` 对象。
4. 将 `Class` 对象与类加载器关联。

### 3.1 触发加载

当 JVM 需要加载一个类时（如创建类的实例、访问类的静态成员、使用反射等），会触发加载过程。

例如，在以下代码中，`MyClass` 类会被加载：

```java
MyClass obj = new MyClass();
```

### 3.2 查找.class文件

JVM 根据类的全限定名（如 `com.example.MyClass`）查找 `.class` 文件。

查找的位置包括：

- 类路径（Classpath）。
- JVM 的核心类库（如 `java.lang.*`）。
- 扩展类库（如 `JAVA_HOME/lib/ext` 目录）。
- 自定义类加载器指定的路径。

查找顺序遵循类加载器的双亲委派模型：

1. **Bootstrap ClassLoader**：查找 JVM 核心类库。
2. **Extension ClassLoader**：查找扩展类库。
3. **Application ClassLoader**：查找应用程序类路径（Classpath）。
4. **自定义 ClassLoader**：查找自定义路径。

### 3.3 读取字节码数据

找到 `.class` 文件后，JVM 读取文件的字节码数据。

字节码数据通常以二进制形式存储。

### 3.4 创建Class对象

JVM 在内存中创建一个 `java.lang.Class` 对象，表示该类。

这个对象包含了类的元数据，如：

- 类名、父类、接口。
- 方法、字段、构造器。
- 注解、泛型信息等。

### 3.5 关联类加载器

将 `Class` 对象与加载它的类加载器关联。

例如，如果 `MyClass` 是由 `Application ClassLoader` 加载的，那么 `MyClass` 的 `Class` 对象会与 `Application ClassLoader` 关联。

## 04.第二阶段连接

**连接（Linking）** 是加载阶段之后的第二个阶段，它的主要任务是将加载到内存中的类进行验证、准备和解析，以确保类的正确性和可用性。

连接阶段是类加载流程中的重要环节，分为三个子阶段：

1. **验证**：确保字节码文件的正确性和安全性。
2. **准备**：为静态变量分配内存并设置默认值。
3. **解析**：将符号引用转换为直接引用。


### 4.1 验证（Verification）

**目的**：确保加载的字节码文件符合 JVM 规范，保证其安全性和正确性。**具体任务**：

1. **文件格式验证**：检查 `.class` 文件的格式是否符合 JVM 规范（如魔数、版本号等）。
2. **元数据验证**：检查类的元数据是否符合 Java 语言规范（如类的继承关系、字段和方法是否合法）。
3. **字节码验证**：检查字节码指令是否合法，是否存在非法操作（如操作数栈溢出、类型不匹配等）。
4. **符号引用验证**：检查类中引用的其他类、方法和字段是否存在，是否符合访问权限。

**结果**：如果验证通过，进入准备阶段。如果验证失败，抛出 `VerifyError`。

### 4.2 准备（Preparation）

**目的**：为类的静态变量分配内存，并设置默认初始值。**具体任务**：

1. **分配内存**： 为类的静态变量（`static` 修饰的变量）分配内存。
2. **设置默认值**：
   - 为静态变量设置默认初始值（如 `0`、`null`、`false` 等）。
   - 例如：`int` 类型的静态变量默认值为 `0`。`boolean` 类型的静态变量默认值为 `false`。引用类型的静态变量默认值为 `null`。
3. **常量优化**：
   - 如果静态变量是常量（`static final` 修饰），且其值在编译期已知，则直接赋值为编译期的值。
   - 例如：`public static final int VALUE = 10;`在准备阶段，`VALUE` 会被直接赋值为 `10`，而不是默认值 `0`。

**结果**：静态变量的内存分配完成，并设置默认值或常量值。

### 4.3 解析（Resolution）

**目的**：将类、方法、字段的符号引用转换为直接引用。**具体任务**：

1. **符号引用**：符号引用是一种逻辑引用，用于描述类、方法或字段的名称和描述符。 例如，`java/lang/System.out` 是一个符号引用。
2. **直接引用**：直接引用是内存中的具体地址，指向类、方法或字段的实际位置。
3. **解析过程**：将符号引用转换为直接引用。 例如：将 `java/lang/System.out` 解析为 `System` 类中 `out` 字段的实际内存地址。
4. **延迟解析**：JVM 可以选择在类加载时立即解析所有符号引用，也可以在第一次使用时才解析（延迟解析）。

**结果**：符号引用被转换为直接引用，类可以正常调用其他类、方法和字段。

## 05.第三阶段初始化

在 Java 类的加载流程中，**初始化（Initialization）** 是连接阶段之后的第三个阶段，也是类加载流程的最后一步。

初始化的主要任务是执行类的静态初始化代码（如静态代码块和静态变量的显式赋值），为类的使用做好准备。

初始化阶段不会在类加载后立即执行，而是在以下情况下触发：

1. **创建类的实例**：
    - 例如：
      ```java
      MyClass obj = new MyClass();
      ```
2. **访问类的静态成员**：
    - 例如：
      ```java
      int value = MyClass.VALUE;
      ```
3. **调用类的静态方法**：
    - 例如：
      ```java
      MyClass.staticMethod();
      ```
4. **使用反射调用类**：
    - 例如：
      ```java
      Class.forName("com.example.MyClass");
      ```
5. **初始化子类**：
    - 如果当前类是父类，且其子类被初始化，父类会先被初始化。


### 5.1 执行静态代码块

**执行静态代码块**：
- 执行类中定义的静态代码块（`static {}`）。
- 静态代码块通常用于初始化静态变量或执行一些静态初始化逻辑。


### 5.2 显式赋值

**执行静态变量的显式赋值**：
- 为静态变量赋予显式初始值（如果在声明时指定了初始值）。
- 例如：
```java
public static int value = 10;
```
在初始化阶段，`value` 会被赋值为 `10`。


### 5.3 执行父类初始化

**执行父类初始化**：
- 如果当前类有父类，JVM 会先初始化父类（递归执行父类的初始化阶段）。
- 确保父类的静态代码块和静态变量先于子类初始化。


### 5.4 确保线程安全

**确保线程安全**：
- 初始化阶段是线程安全的，JVM 会通过加锁机制确保一个类只会被初始化一次。

## 06.第四阶段使用

在 Java 类的加载流程中，**使用（Using）** 是类加载完成后的阶段，即类已经被加载、连接和初始化，可以被 JVM 正常使用。

这个阶段是类的生命周期中最重要的阶段，因为类的实际功能和行为都是在这个阶段体现的。

### 6.1 访问类的成员

**访问类的成员**：访问类的实例变量和实例方法。例如：

```java
obj.setValue(10);
int value = obj.getValue();
```

### 6.2 调用类静态成员

**调用类静态成员**：访问类的静态变量和静态方法。例如：

```java
int value = MyClass.STATIC_VALUE;
MyClass.staticMethod();
```

### 6.3 使用类的功能

**使用类的功能**：调用类的方法实现具体的业务逻辑。例如：

```java
obj.doSomething();
```

### 6.4 对象生命周期

**处理类的对象生命周期**：管理对象的创建、使用和销毁。

例如：通过垃圾回收机制（GC）回收不再使用的对象。

## 07.第五阶段卸载

在 Java 类的加载流程中，**卸载（Unloading）** 是类生命周期的最后一个阶段。当一个类不再被使用时，JVM 会将其从内存中卸载，以释放资源。

卸载是一个由 JVM 自动管理的过程，开发者通常无法直接控制。

### 7.1 卸载的条件

一个类可以被卸载的条件是：

1. **类的所有实例都已被回收**：该类创建的所有对象都已被垃圾回收。
2. **类的 ClassLoader 已被回收**：加载该类的 `ClassLoader` 实例已被垃圾回收。
3. **类的 Class 对象没有被引用**：该类的 `Class` 对象没有被任何地方引用（如静态变量、反射等）。

只有同时满足以上三个条件，JVM 才会卸载该类。

### 7.2 卸载阶段过程

以下是一个简单的示例，展示类的卸载过程：

```java
class MyClass {
    static {
        System.out.println("MyClass is initialized!");
    }

    @Override
    protected void finalize() throws Throwable {
        System.out.println("MyClass is finalized!");
    }
}

public class Main {
    public static void main(String[] args) throws Exception {
        // 创建自定义 ClassLoader
        ClassLoader classLoader = new ClassLoader() {};

        // 加载类
        Class<?> clazz = classLoader.loadClass("MyClass");

        // 创建实例
        Object obj = clazz.getDeclaredConstructor().newInstance();

        // 释放引用
        obj = null;
        clazz = null;
        classLoader = null;

        // 触发垃圾回收
        System.gc();

        // 等待垃圾回收完成
        Thread.sleep(1000);
    }
}
```

**卸载阶段的过程**：

1. 使用自定义 `ClassLoader` 加载 `MyClass` 类。
2. 创建 `MyClass` 的实例。
3. 释放对 `MyClass` 实例、`Class` 对象和 `ClassLoader` 的引用。
4. 调用 `System.gc()` 触发垃圾回收。
5. 如果满足卸载条件，JVM 会卸载 `MyClass` 类，并调用 `finalize()` 方法。

**输出**：

```text
MyClass is initialized!
MyClass is finalized!
```

### 7.3 释放类元数据

**释放类的元数据**：卸载类时，JVM 会释放该类在方法区（Metaspace 或永久代）中占用的内存，包括类的元数据（如类名、字段、方法、常量池等）。

### 7.4 释放类静态变量

**释放类的静态变量**：类的静态变量占用的内存也会被释放。

### 7.5 释放类Class对象

**释放类的 Class 对象**：类的 `Class` 对象（即 `java.lang.Class` 实例）会被垃圾回收。

### 7.6 卸载总结

卸载阶段是类生命周期的最后一个阶段，主要任务包括：

1. 释放类的元数据和静态变量。
2. 释放类的 `Class` 对象。
3. 确保满足卸载条件（类的实例、`ClassLoader` 和 `Class` 对象都被回收）。

卸载是一个由 JVM 自动管理的过程，开发者通常无法直接干预。理解卸载机制有助于优化内存管理和避免内存泄漏。



## 参考博客

Java虚拟机是如何加载Java类的?

https://time.geekbang.org/column/article/11523

请介绍类加载过程，什么是双亲委派模型？

https://time.geekbang.org/column/article/9946

深入拆解Java虚拟机(二)Java虚拟机是如何加载Java类的?

https://www.cnblogs.com/SmartCat994/p/14252262.html

JVM实战：类加载机制+JVM调优实战+代码优化

https://www.jianshu.com/p/da4dc3202ff7
