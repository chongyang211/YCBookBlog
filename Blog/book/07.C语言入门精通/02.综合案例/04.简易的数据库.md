# 04.简易的数据库
#### 目录介绍
- 01.数据库系统需求
  - 1.1 需求介绍
  - 1.2 功能介绍
  - 1.3 核心功能模块
  - 1.4 功能分析
- 02.设计核心思路
  - 2.1 核心目标
  - 2.2 设计原则
  - 2.3 技术选型
  - 2.4 主菜单设计
- 03.定义数据结构
  - 3.1 列结构体
  - 3.2 行结构体
  - 3.3 表结构体
  - 3.4 数据库结构体
- 04.核心功能实现
  - 4.1 创建表
  - 4.2 插入行数据
  - 4.3 删除行数据
  - 4.4 更新行数据
  - 4.5 查询表操作
- 05.文件File
  - 5.1 保存数据到文件
  - 5.2 从文件加载数据
- 06.错误处理
  - 6.1 空指针检查
  - 6.2 文件打开检查
  - 6.3 输入验证
- 07.拓展延伸
  - 7.1 示例运行流程
  - 7.2 扩展性设计



## 01.数据库系统需求

### 1.1 需求介绍


### 1.2 功能介绍


### 1.3 核心功能模块


### 1.4 涉及技术点

该案例涉及了以下技术点：

1. **数据结构**：结构体、枚举、数组。
2. **文件操作**：文件读写、二进制文件、序列化与反序列化。
3. **内存管理**：动态内存分配与释放。
4. **字符串处理**：字符串复制与比较。
5. **用户交互**：命令行界面、输入处理。
6. **错误处理**：空指针检查、文件打开检查、输入验证。
7. **循环和条件语句**：`for` 循环、`while` 循环、`if-else`、`switch`。
8. **模块化设计**：函数封装、头文件。


## 02.设计核心思路

### 2.1 核心目标

实现一个基于文件的简易数据库系统，支持以下功能：

- 创建表结构。
- 插入、删除、更新数据。
- 查询数据。
- 将数据保存到文件，并从文件加载数据。

### 2.2 设计原则

- **模块化**：将功能划分为不同的模块（如数据库管理、表管理、文件操作等），便于维护和扩展。
- **面向对象思想**：使用结构体和函数封装数据和操作，模拟面向对象的设计。
- **数据持久化**：通过文件存储数据，确保程序关闭后数据不丢失。
- **用户交互**：通过命令行界面与用户交互，提供简单的操作菜单。

### 2.3 技术选型

- **C 语言**：作为底层语言，适合实现高效的文件操作和数据结构。
- **文件操作**：使用 `fopen`、`fread`、`fwrite` 等函数实现数据的读写。
- **动态内存管理**：使用 `malloc` 和 `free` 动态分配和释放内存。
- **数据结构**：使用结构体表示表、列和行，使用数组存储多行数据。

### 2.4 主菜单设计

**主菜单**：在头文件中设计菜单，提供功能选项，如创建表、插入行、删除行、更新行、查询表、保存数据、加载数据等。

```c
//菜单展示
void Menu();
```

具体实现如下所示：

```c
void Menu() {
    printf("*************************************************\n");
    printf("*\t欢迎简易数据库系统1.0\t*\n");
    printf("*\t\t请选择功能\t\t\t*\n");
    printf("*************************************************\n");
    printf("*\t\t1.创建表\t\t\t*\n");
    printf("*\t\t2.插入行\t\t\t*\n");
    printf("*\t\t3.删除行\t\t\t*\n");
    printf("*\t\t4.更新行\t\t\t*\n");
    printf("*\t\t5.查询表\t\t\t*\n");
    printf("*\t\t6.保存数据到文件\t\t*\n");
    printf("*\t\t7.从文件读取数据\t\t*\n");
    printf("*\t\t0.退出系统\t\t\t*\n");
    printf("*************************************************\n");
}
```

**输入处理**：接收用户输入，调用相应的功能函数。

```
int main() {
    int choice;
    while (1) {
        //打印菜单
        Menu();
        //读取控制台字符
        scanf("%d", &choice);
        printf("输入的字符是：%c \n", choice);
        switch (choice) {
            case 1: //1.创建表
                printf("创建表\n");
                break;
            case 2: //2.插入行
                printf("插入行\n");
                break;
            case 3: //3.删除行
                printf("删除行\n");
                break;
            case 4: //4.更新行
                printf("更新行\n");
                break;
            case 5: //5.查询表
                printf("查询表\n");
                break;
            case 6: //6.保存数据到文件
                printf("保存数据到文件\n");
                break;
            case 7: //7.从文件读取数据
                printf("从文件读取数据\n");
                break;
            case 0: //0.退出系统
                printf("退出系统\n");
                return 0;
            default:
                printf("输入有误，没有该功能\n\n");
                break;
        }
    }
    return 0;
}
```

## 03.定义数据结构

- **Column 结构体**：表示表中的列，包含列名和数据类型。
- **Row 结构体**：表示表中的一行数据，使用指针数组存储每列的数据。
- **Table 结构体**：表示表，包含表名、列信息、行数据和行数。
- **Database 结构体**：表示数据库，包含多个表和表数。

不管是行数据，还是列数据，都有相似的数据。我们可以把这部门抽出来，定义通用数据类型。如下所示：

```c
// 数据类型枚举
typedef enum {
    //int类型数据
    TYPE_INT,
    //float类型数据
    TYPE_FLOAT,
    //字符串类型数据
    TYPE_STRING
} DataType;
```

用于表示列的数据类型（如整数、浮点数、字符串）。

### 3.1 列结构体

```c
// 列结构体
typedef struct {
    char name[MAX_NAME_LENGTH]; // 列名
    DataType type;              // 数据类型
} Column;
```

### 3.2 行结构体

```c
typedef struct {
    void *data[MAX_COLUMNS]; // 存储每列的数据
} Row;
```

### 3.3 表结构体

```c
typedef struct {
    char name[MAX_NAME_LENGTH]; // 表名
    Column columns[MAX_COLUMNS]; // 列信息
    int columnCount;            // 列数
    Row rows[MAX_ROWS];         // 行数据
    int rowCount;               // 行数
} Table;
```

使用数组存储多列和多行数据：

- `Column columns[MAX_COLUMNS]`：存储表中的列信息。
- `Row rows[MAX_ROWS]`：存储表中的行数据。


### 3.4 数据库结构体

```c
// 数据库结构体
typedef struct {
    Table tables[MAX_COLUMNS]; // 所有表
    int tableCount;            // 表数
} Database;
```

## 04.核心功能实现

### 4.1 创建表

**创建表**：接收表名和列信息，初始化表结构。将表添加到数据库中。

首先定义函数

```c
void createTable(Database *db, const char *tableName, Column *columns, int columnCount);
```

然后在.c文件中做具体的实现，代码如下所示：

```c
void createTable(Database *db, const char *tableName, Column *columns, int columnCount) {
    if (db->tableCount >= MAX_COLUMNS) {
        printf("无法创建表：数据库表数量已达上限！\n");
        return;
    }
    //获取索引
    int tableCount = db->tableCount;
    //获取目标表指针
    Table *table = &db->tables[tableCount];
    strncpy(table->name, tableName, MAX_NAME_LENGTH);
    table->columnCount = columnCount;
    for (int i = 0; i < columnCount; i++) {
        strncpy(table->columns[i].name, columns[i].name, MAX_NAME_LENGTH);
        table->columns[i].type = columns[i].type;
    }
    table->rowCount = 0;
    db->tableCount++;
    printf("表 '%s' 创建成功！\n", tableName);
}
```

### 4.2 插入行数据

**插入行**：根据表名找到目标表。将数据插入到表的行数组中。

```c
void insertRow(Database *db, const char *tableName, void **data);
```

根据定义，实现插入行的逻辑。代码：

```c
void insertRow(Database *db, const char *tableName, void **data) {
    Table *table = NULL;
    //遍历查找目标表
    for (int i = 0; i < db->tableCount; i++) {
        if (strcmp(db->tables[i].name, tableName) == 0) {
            table = &db->tables[i];
            break;
        }
    }

    if (!table) {
        printf("表 '%s' 不存在！\n", tableName);
        return;
    }
    if (table->rowCount >= MAX_ROWS) {
        printf("无法插入行：表 '%s' 行数已达上限！\n", tableName);
        return;
    }

    Row *row = &table->rows[table->rowCount];
    for (int i = 0; i < table->columnCount; i++) {
        switch (table->columns[i].type) {
            case TYPE_INT:
                row->data[i] = malloc(sizeof(int));
                *(int *)row->data[i] = *(int *)data[i];
                break;
            case TYPE_FLOAT:
                row->data[i] = malloc(sizeof(float));
                *(float *)row->data[i] = *(float *)data[i];
                break;
            case TYPE_STRING:
                row->data[i] = malloc(MAX_STRING_LENGTH);
                strncpy((char *)row->data[i], (char *)data[i], MAX_STRING_LENGTH);
                break;
        }
    }
    table->rowCount++;
    printf("行插入成功！\n");
}
```

### 4.3 删除行数据

```c
void deleteRow(Database *db, const char *tableName, int rowIndex);
```

**删除行**：根据表名和行索引删除指定行。将后续行前移，覆盖被删除的行。

```c
void deleteRow(Database *db, const char *tableName, int rowIndex) {
    Table *table = NULL;
    for (int i = 0; i < db->tableCount; i++) {
        if (strcmp(db->tables[i].name, tableName) == 0) {
            table = &db->tables[i];
            break;
        }
    }
    if (!table) {
        printf("表 '%s' 不存在！\n", tableName);
        return;
    }
    if (rowIndex < 0 || rowIndex >= table->rowCount) {
        printf("行索引无效！\n");
        return;
    }
    for (int i = rowIndex; i < table->rowCount - 1; i++) {
        table->rows[i] = table->rows[i + 1];
    }
    table->rowCount--;
    printf("行删除成功！\n");
}
```

### 4.4 更新行数据

```c
void updateRow(Database *db, const char *tableName, int rowIndex, void **data);
```

**更新行**：根据表名和行索引更新指定行的数据。

```c
void updateRow(Database *db, const char *tableName, int rowIndex, void **data) {
    Table *table = NULL;
    for (int i = 0; i < db->tableCount; i++) {
        if (strcmp(db->tables[i].name, tableName) == 0) {
            table = &db->tables[i];
            break;
        }
    }
    if (!table) {
        printf("表 '%s' 不存在！\n", tableName);
        return;
    }
    if (rowIndex < 0 || rowIndex >= table->rowCount) {
        printf("行索引无效！\n");
        return;
    }
    Row *row = &table->rows[rowIndex];
    for (int i = 0; i < table->columnCount; i++) {
        switch (table->columns[i].type) {
            case TYPE_INT:
                *(int *)row->data[i] = *(int *)data[i];
                break;
            case TYPE_FLOAT:
                *(float *)row->data[i] = *(float *)data[i];
                break;
            case TYPE_STRING:
                strncpy((char *)row->data[i], (char *)data[i], MAX_STRING_LENGTH);
                break;
        }
    }
    printf("行更新成功！\n");
}
```

### 4.5 查询表操作

**查询表**：根据表名找到目标表。遍历表的所有行，打印数据。

```c
void queryTable(Database *db, const char *tableName);
```

具体实现如下所示：

```c
void queryTable(Database *db, const char *tableName) {
    Table *table = NULL;
    for (int i = 0; i < db->tableCount; i++) {
        if (strcmp(db->tables[i].name, tableName) == 0) {
            table = &db->tables[i];
            break;
        }
    }
    if (!table) {
        printf("表 '%s' 不存在！\n", tableName);
        return;
    }
    printf("表 '%s' 数据：\n", tableName);
    for (int i = 0; i < table->columnCount; i++) {
        printf("%-20s", table->columns[i].name);
    }
    printf("\n");
    for (int i = 0; i < table->rowCount; i++) {
        Row *row = &table->rows[i];
        for (int j = 0; j < table->columnCount; j++) {
            switch (table->columns[j].type) {
                case TYPE_INT:
                    printf("%-20d", *(int *)row->data[j]);
                    break;
                case TYPE_FLOAT:
                    printf("%-20.2f", *(float *)row->data[j]);
                    break;
                case TYPE_STRING:
                    printf("%-20s", (char *)row->data[j]);
                    break;
            }
        }
        printf("\n");
    }
}
```

## 05.文件File

使用 `fopen`、`fread`、`fwrite`、`fclose` 等函数实现文件的读写操作。 将数据库的表结构和数据保存到文件中，并从文件加载数据。

使用二进制格式存储数据，确保高效读写：

```c
FILE *file = fopen(filename, "wb"); // 写二进制文件
FILE *file = fopen(filename, "rb"); // 读二进制文件
```

### 5.1 保存数据到文件

**保存数据库到文件**： 打开文件，将数据库中的表结构和数据写入文件。使用二进制格式存储数据，确保高效读写。

```c
void saveDatabase(Database *db, const char *filename) {
    //打开文件
    FILE *file = fopen(filename, "wb");
    if (!file) {
        printf("无法打开文件 '%s'！\n", filename);
        return;
    }
    fwrite(&db->tableCount, sizeof(int), 1, file);
    for (int i = 0; i < db->tableCount; i++) {
        Table *table = &db->tables[i];
        fwrite(table->name, sizeof(char), MAX_NAME_LENGTH, file);
        fwrite(&table->columnCount, sizeof(int), 1, file);
        fwrite(table->columns, sizeof(Column), MAX_COLUMNS, file);
        fwrite(&table->rowCount, sizeof(int), 1, file);
        //写行数据
        for (int j = 0; j < table->rowCount; j++) {
            Row *row = &table->rows[j];
            for (int k = 0; k < table->columnCount; k++) {
                switch (table->columns[k].type) {
                    case TYPE_INT:
                        fwrite(row->data[k], sizeof(int), 1, file);
                        break;
                    case TYPE_FLOAT:
                        fwrite(row->data[k], sizeof(float), 1, file);
                        break;
                    case TYPE_STRING:
                        fwrite(row->data[k], sizeof(char), MAX_STRING_LENGTH, file);
                        break;
                }
            }
        }
    }
    fclose(file);
    printf("数据库保存成功！\n");
}
```

### 5.2 从文件加载数据

**从文件加载数据库**： 打开文件，读取表结构和数据，初始化数据库。动态分配内存存储数据。

```c
void loadDatabase(Database *db, const char *filename) {
    FILE *file = fopen(filename, "rb");
    if (!file) {
        printf("无法打开文件 '%s'！\n", filename);
        return;
    }
    fread(&db->tableCount, sizeof(int), 1, file);
    for (int i = 0; i < db->tableCount; i++) {
        Table *table = &db->tables[i];
        fread(table->name, sizeof(char), MAX_NAME_LENGTH, file);
        fread(&table->columnCount, sizeof(int), 1, file);
        fread(table->columns, sizeof(Column), MAX_COLUMNS, file);
        fread(&table->rowCount, sizeof(int), 1, file);
        for (int j = 0; j < table->rowCount; j++) {
            Row *row = &table->rows[j];
            for (int k = 0; k < table->columnCount; k++) {
                switch (table->columns[k].type) {
                    case TYPE_INT:
                        row->data[k] = malloc(sizeof(int));
                        fread(row->data[k], sizeof(int), 1, file);
                        break;
                    case TYPE_FLOAT:
                        row->data[k] = malloc(sizeof(float));
                        fread(row->data[k], sizeof(float), 1, file);
                        break;
                    case TYPE_STRING:
                        row->data[k] = malloc(MAX_STRING_LENGTH);
                        fread(row->data[k], sizeof(char), MAX_STRING_LENGTH, file);
                        break;
                }
            }
        }
    }
    fclose(file);
    printf("数据库加载成功！\n");
}
```

## 06.错误处理

### 6.1 空指针检查

在查找表时，检查返回的指针是否为空：

```c
if (!table) {
    printf("表 '%s' 不存在！\n", tableName);
    return;
}
```

### 6.2 文件打开检查

在读写文件时，检查文件是否成功打开：

```c
FILE *file = fopen(filename, "rb");
if (!file) {
    printf("无法打开文件 '%s'！\n", filename);
    return;
}
```

### 6.3 输入验证

检查用户输入的行索引是否有效：

```c
if (rowIndex < 0 || rowIndex >= table->rowCount) {
    printf("行索引无效！\n");
    return;
}
```

### 7.2 扩展性设计

- **支持更多数据类型**：可以扩展 `DataType` 枚举，支持更多数据类型（如日期、布尔值等）。
- **支持更多操作**：可以添加更多功能（如修改表结构、删除表等）。
- **支持更多文件格式**：可以扩展文件操作模块，支持 JSON、XML 等文件格式。





