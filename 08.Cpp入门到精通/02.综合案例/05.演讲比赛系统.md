# 05.演讲比赛系统

> 本章是 **STL 组合使用**的示范——用 `vector<int>`（抽签编号）、`map<int, Player>`（编号到选手）、`multimap<int, int>`（分数到编号）三种容器协同完成两轮晋级赛。相比 [03.职工](./03.职工管理系统.md)/[04.数据库](./04.简易的数据库.md) 自行管理数组，本案例让读者看到"STL 容器 + 算法"是如何把十几行循环压缩到两三行的。

---

## 00.案例元信息

| 项目 | 说明 |
|------|------|
| 难度 | ★★★☆☆ |
| 预估时长 | 5 小时 |
| 前置章节 | 卷一第 9 章（类与对象）、第 13 章（IO 与文件）、第 16 章（STL 模板） |
| 覆盖知识点 | `std::vector` / `std::map` / `std::multimap` 协同、`std::random_shuffle`（C++17 前）/ `std::shuffle`（C++17 后）、`sort + lambda`、`ofstream` 追加模式 `std::ios::app` |
| 设计亮点 | 用 `map<轮次, vector<Player>>` 结构记录历届比赛结果 |
| 最终产物 | 可执行文件 `speech_contest` + 数据文件 `speech.csv` |
| 代码规模 | 约 500 行 |

---

### 目录介绍
- 01.演讲比赛系统
  - 1.1 比赛规则
  - 1.2 程序功能
  - 1.3 开发设计步骤
  - 1.4 技术栈说明
- 02.创建管理类
  - 2.1 创建文件
  - 2.2 头文件实现
  - 2.3 源文件实现
- 03.菜单功能
  - 3.1 添加成员函数
  - 3.2 菜单功能实现
- 04.退出功能
  - 4.1 提供功能接口
- 05.演讲比赛功能
  - 5.1 功能分析
  - 5.2 创建选手类
  - 5.3 比赛中成员属性添加
  - 5.4 初始化属性
  - 5.5 初始化创建选手
  - 5.6 测试选手初始化状态
  - 5.7 开始比赛函数
- 06.第一轮比赛
  - 6.1 抽签
  - 6.2 开始比赛
  - 6.3 显示比赛分数
- 07.第二轮比赛
  - 7.1 抽签比赛和晋级
  - 7.2 保存分数
- 08.查看记录
  - 8.1 读取记录分数
  - 8.2 查看记录功能
  - 8.3 测试功能
- 09.清空记录
  - 9.1 清空记录功能
  - 9.2 测试清空功能
- 10.一些拓展功能
  - 10.1 选手信息增强
  - 10.2 比赛系统增强
  - 10.3 数据管理增强
  - 10.4 记录系统增强
  - 10.5 日志与审计
  - 10.6 系统管理工具
  - 10.7 用户体验增强
- 11.一些bug优化
  - 11.1 >> 被解析为右移运算符


## 01.演讲比赛系统

### 1.1 比赛规则

* 学校举行一场演讲比赛，共有**12个人**参加。**比赛共两轮**，第一轮为淘汰赛，第二轮为决赛。
* 比赛方式：**分组比赛，每组6个人**；选手每次要随机分组，进行比赛
* 每名选手都有对应的**编号**，如 10001 ~ 10012
* 第一轮分为两个小组，每组6个人。 整体按照选手编号进行**抽签**后顺序演讲。
* 当小组演讲完后，淘汰组内排名最后的三个选手，**前三名晋级**，进入下一轮的比赛。
* 第二轮为决赛，**前三名胜出**
* 每轮比赛过后需要**显示晋级选手的信息**

### 1.2 程序功能

* 开始演讲比赛：完成整届比赛的流程，每个比赛阶段需要给用户一个提示，用户按任意键后继续下一个阶段
* 查看往届记录：查看之前比赛前三名结果，每次比赛都会记录到文件中，文件用.csv后缀名保存
* 清空比赛记录：将文件中数据清空
* 退出比赛程序：可以退出当前程序

### 1.3 开发设计步骤

我们将按照以下步骤进行：

1. 定义选手结构体，包含编号、姓名（为了丰富内容，我们给选手添加姓名）、分数等。
2. 实现抽签（随机分组）功能。
3. 实现比赛过程，包括两轮比赛，每轮比赛后显示晋级选手。
4. 记录比赛结果到CSV文件（往届记录）。
5. 实现查看往届记录、清空记录的功能。

### 1.4 技术栈说明

1. **C++ 标准库**：
  - 容器：`std::vector`、`std::map`、`std::multimap`、`std::deque`。
  - 算法：`std::shuffle`、`std::sort`、`std::accumulate`。
  - 文件操作：`std::ofstream`、`std::ifstream`。
  - 随机数生成：`std::random_device`、`std::mt19937`。
2. **面向对象编程**：
  - 使用类 `SpeechManager` 封装比赛管理逻辑。
  - 使用结构体 `Speaker` 存储选手信息。
3. **命令行交互**：
  - 使用 `std::cin` 和 `std::cout` 实现用户输入和输出。
4. **文件读写**：
  - 使用 C++ 文件流操作读写文本文件。
5. **随机数生成**：
  - 使用 `<random>` 库生成高质量的随机数。


## 02.创建管理类

**功能描述：**

- 提供菜单界面与用户交互
- 对演讲比赛流程进行控制
- 与文件的读写交互

### 2.1 创建文件

* 在头文件和源文件的文件夹下分别创建speechManager.h 和 speechManager.cpp文件

### 2.2 头文件实现

在speechManager.h中设计管理类

代码如下：

```cpp
#pragma once
#include<iostream>
using namespace std;

//演讲管理类
class SpeechManager {
public:
    //构造函数
    SpeechManager();
    //析构函数
    ~SpeechManager();
};
```

### 2.3 源文件实现

在speechManager.cpp中将构造和析构函数空实现补全

```cpp
#include "speechManager.h"

SpeechManager::SpeechManager(){
    
}

SpeechManager::~SpeechManager(){
    
}
```

* 至此演讲管理类以创建完毕

## 03.菜单功能

功能描述：与用户的沟通界面

### 3.1 添加成员函数

在管理类speechManager.h中添加成员函数  `void showMenu();`

### 3.2 菜单功能实现

* 在管理类speechManager.cpp中实现 showMenu()函数

```cpp
void SpeechManager::showMenu() {
    cout << "********************************************" << endl;
    cout << "*************  欢迎参加演讲比赛 ************" << endl;
    cout << "*************  1.开始演讲比赛  *************" << endl;
    cout << "*************  2.查看往届记录  *************" << endl;
    cout << "*************  3.清空比赛记录  *************" << endl;
    cout << "*************  0.退出比赛程序  *************" << endl;
    cout << "********************************************" << endl;
    cout << endl;
}
```

### 3.3 测试菜单功能

* 在演讲比赛流程管理系统.cpp中测试菜单功能

代码：

```cpp
#include<iostream>
using namespace std;
#include "speechManager.h"

int main() {
    SpeechManager sm;
    sm.showMenu();
    cin.get();   // 等待回车，便于看清打印
    return 0;
}
```

预期打印：

```text
********************************************
*************  欢迎参加演讲比赛 ************
*************  1.开始演讲比赛  *************
*************  2.查看往届记录  *************
*************  3.清空比赛记录  *************
*************  0.退出比赛程序  *************
********************************************
```

仅一段菜单方框——验证 `SpeechManager` 能正确构造、`showMenu` 能调用即可。**这一步刻意让 main 极简**：还没有任何输入分支，避免 menu 显示和后续业务耦合，为 4.1 的 `while (true)` 循环预留接入点。

> 💡 **`pause()` 不是 C++ 标准函数**：原文出现的 `pause()` 是 \<unistd.h\> 里的 POSIX 系统调用，会**让进程睡到收到信号才返回**——等同于"程序卡死"。本案例真正要的是"等用户敲一下回车"，正确写法是 `cin.get()` 或封装一个跨平台 `pauseAndCls()` 函数。下面所有测试都按此修正。

## 04.退出功能

### 4.1 提供功能接口

* 在main函数中提供分支选择，提供每个功能接口

```cpp
int main() {
    SpeechManager sm;
    int choice = 0; //用来存储用户的选项
    while (true){
        sm.showMenu();
        cout << "请输入您的选择： " << endl;
        cin >> choice; // 接受用户的选项
        switch (choice){
            case 1:  //开始比赛
                break;
            case 2:  //查看记录
                break;
            case 3:  //清空记录
                break;
            case 0:  //退出系统
                sm.exitSystem();
                break;
            default:
                system("cls"); //清屏
            break;
        }
    }
    return 0;
}
```

### 4.2 实现退出功能

在speechManager.h中提供退出系统的成员函数 `void exitSystem();`

在speechManager.cpp中提供具体的功能实现

```cpp
void SpeechManager::exitSystem() {
    cout << "欢迎下次使用" << endl;
    cin.get();   // 等用户按一下回车，再退出
    exit(0);
}
```

测试输入 `0` 触发退出，预期结果：

```text
********************************************
*************  欢迎参加演讲比赛 ************
*************  1.开始演讲比赛  *************
*************  2.查看往届记录  *************
*************  3.清空比赛记录  *************
*************  0.退出比赛程序  *************
********************************************

请输入您的选择： 
0
欢迎下次使用
```

按一下回车后程序结束。从这一步开始，菜单就具备了"循环显示 + 任意时刻 0 退出"的骨架，后面 1/2/3 三个分支只需要在 `case` 里挂上对应函数即可——**菜单驱动型程序的标准形态**。

> ⚠️ **`exit(0)` 会跳过所有栈对象的析构**：包括 `SpeechManager sm` 自己——这意味着如果后续给 `SpeechManager` 的 `record`、`speaker` map 加了"析构时落盘"逻辑，**`exit` 路径上会丢数据**。生产代码应当 `return 0`（或抛出特定异常）走正常退出，把 `exit` 留给真正"立刻终止"的紧急场景（比如 Ctrl+C 信号处理）。本案例数据已经在 `saveRecord` 时及时写盘，没踩到这个坑，但要心里有数。

## 05.演讲比赛功能

### 5.1 功能分析

比赛流程分析：

抽签 → 开始演讲比赛 → 显示第一轮比赛结果 → 

抽签 → 开始演讲比赛 → 显示前三名结果 → 保存分数

### 5.2 创建选手类

* 选手类中的属性包含：选手姓名、分数
* 头文件中创建 speaker.h文件，并添加代码：

```cpp
#pragma once
#include<iostream>
using namespace std;

class Speaker{
public:
    string name;  //姓名
    double score[2]; //分数  最多有两轮得分
};
```

### 5.3 比赛中成员属性添加

* 在speechManager.h中添加属性

```cpp
//比赛选手 容器  12人
vector<int>v1;
//第一轮晋级容器  6人
vector<int>v2;
//胜利前三名容器  3人
vector<int> victory;
//存放编号 以及对应的 具体选手 容器
map<int, Speaker> speaker;
```

### 5.4 初始化属性

在speechManager.h中提供开始比赛的的成员函数 `void initSpeech();`

```cpp
//初始化属性
void initSpeech();
```

在speechManager.cpp中实现`void initSpeech();`

```cpp
void SpeechManager::initSpeech() {
    //容器保证为空
    this->v1.clear();
    this->v2.clear();
    this->victory.clear();
    this->speaker.clear();
    //初始化比赛轮次
    this->index = 1;
}
```

SpeechManager构造函数中调用`void initSpeech();`

```cpp
SpeechManager::SpeechManager(){
    //初始化属性
    this->initSpeech();
}
```

### 5.5 初始化创建选手

* 在speechManager.h中提供开始比赛的的成员函数 `void createSpeaker();`

```cpp
//初始化创建12名选手
void createSpeaker();
```

* 在speechManager.cpp中实现`void createSpeaker();`

```cpp
void SpeechManager::createSpeaker() {
    //创建选手后，将选手编号，并且放到容器中。按照A，B，C……L排序
    string nameSeed = "ABCDEFGHIJKL";
    for (int i=0 ; i<nameSeed.size() ; i++) {
        string name = "选手";
        name = name + nameSeed[i];
        //创建选手对象
        Speaker speaker;
        speaker.name = name;
        for (int j=0 ; j<2 ; j++) {
            speaker.score[j] = 0;
        }
        int key = i + 10001;
        //将选手放到v1容器中。从10001开始
        this->v1.push_back(key);
        //将选手放到容器中
        this->speaker.insert(std::make_pair(key, speaker)); // 使用 insert
    }
}
```

* SpeechManager类的 构造函数中调用`void createSpeaker();`

```cpp
SpeechManager::SpeechManager(){
    //初始化属性
    this->initSpeech();
    //创建选手
    this->createSpeaker();
}
```

### 5.6 测试选手初始化状态

* 测试 在main函数中，可以在创建完管理对象后，使用下列代码测试12名选手初始状态

```cpp
int main() {
    SpeechManager sm;
    for (map<int, Speaker>::iterator it = sm.speaker.begin(); it != sm.speaker.end(); it++) {
        cout << "选手编号：" << it->first
                << " 姓名： " << it->second.name
                << " 成绩： " << it->second.score[0] << endl;
    }
    return 0;
}
```

预期打印（编号严格升序，姓名 A→L）：

```text
选手编号：10001 姓名： 选手A 成绩： 0
选手编号：10002 姓名： 选手B 成绩： 0
选手编号：10003 姓名： 选手C 成绩： 0
选手编号：10004 姓名： 选手D 成绩： 0
选手编号：10005 姓名： 选手E 成绩： 0
选手编号：10006 姓名： 选手F 成绩： 0
选手编号：10007 姓名： 选手G 成绩： 0
选手编号：10008 姓名： 选手H 成绩： 0
选手编号：10009 姓名： 选手I 成绩： 0
选手编号：10010 姓名： 选手J 成绩： 0
选手编号：10011 姓名： 选手K 成绩： 0
选手编号：10012 姓名： 选手L 成绩： 0
```

12 名选手按编号 10001→10012 升序输出——但 `createSpeaker` 内部是用 `for i = 0..11` 顺序 `insert` 的，而我们 `printDatabase` 时拿到的也是升序——这并非巧合，而是 `std::map` 的内置承诺：**键按 `<` 升序排列**，遍历顺序由红黑树保证，与插入顺序无关。

> 🧠 **`map` vs `unordered_map`：选哪个**？本案例用 `map<int, Speaker>` 是**有意为之**：选手编号天然要按 1→12 顺序展示，`map` 一行代码免费拿到排序。如果换成 `unordered_map<int, Speaker>`（哈希表），插入/查找都是 O(1) 更快，但**遍历顺序不可预测**——展示时还得自己 `sort`。**99% 的菜单类小程序选 `map` 都是稳妥选择**：数据量小（n=12），常数差异忽略不计；换来的"开箱即排序"省下大量 `sort` 调用。

测试完毕后，可以将测试代码删除或注释。

### 5.7 开始比赛函数

在speechManager.h中提供开始比赛的的成员函数 `void startSpeech();`

该函数功能是主要控制比赛的流程，先声明函数。

```cpp
//开始比赛-比赛流程控制
void startSpeech();
```

在speechManager.cpp中将startSpeech的空实现先写入。我们可以先将整个比赛的流程 写到函数中

```cpp
void SpeechManager::startSpeech() {
    //第一轮比赛
    //1、抽签
    //2、比赛
    //3、显示晋级结果
    //第二轮比赛
    //1、抽签
    //2、比赛
    //3、显示最终结果
    //4、保存分数
}
```

## 06.第一轮比赛

### 6.1 抽签

**功能描述：** 正式比赛前，所有选手的比赛顺序需要打乱，我们只需要将存放选手编号的容器，打乱次序即可。

在speechManager.h中提供抽签的的成员函数 `void speechDraw();`

```cpp
//抽签
void speechDraw();
```

在speechManager.cpp中实现成员函数 `void speechDraw();`

```cpp
void SpeechManager::speechDraw() {
    cout << "第 << " << this->index << " >> 轮比赛选手正在抽签，打乱用户顺序"<<endl;
    cout << "---------------------" << endl;
    cout << "抽签后演讲顺序如下：" << endl;
    //random_shuffle这个已经不用了
    // 创建随机数引擎
    random_device rd;
    mt19937 g(rd());
    if (this->index == 1) {
        shuffle(v1.begin(), v1.end(),g);
        for (vector<int>::iterator it = v1.begin(); it != v1.end(); it++){
            cout << *it << " ";
        }
        cout << endl;
    } else {
        shuffle(v2.begin(), v2.end(),g);
        for (vector<int>::iterator it = v2.begin(); it != v2.end(); it++){
            cout << *it << " ";
        }
        cout << endl;
    }
    cout << "---------------------" << endl;
    cin.get();
    cout << endl;
}
```

**测试抽签**。在主菜单输入 `1`，实测打印（每次运行顺序都不同）：

```text
请输入您的选择： 
1
第 << 1 >> 轮比赛选手正在抽签，打乱用户顺序
---------------------
抽签后演讲顺序如下：
10010 10006 10001 10004 10002 10003 10009 10008 10005 10007 10012 10011 
---------------------
```

12 个编号被**就地原地打乱**——`shuffle` 改的是 `v1` 容器本身的元素顺序，不会复制新容器。后续 `speechContest` 直接遍历 `v1` 即可拿到打乱后的顺序，不需要"打乱副本再赋值回来"那种笨办法。

> 🧠 **`shuffle` 替代 `random_shuffle`**：C++17 起 `std::random_shuffle` 被废弃（C++14 标记 `[[deprecated]]`），原因是它内部用的是 `rand()`——**全局共享种子、低质量伪随机**。新 API `std::shuffle(first, last, urbg)` 强制要求外部传入随机数引擎（如 `mt19937`），既保证质量也避免线程间种子污染。**记住这个升级规则：凡是 `<algorithm>` 里以 `random_` 开头的旧函数，都已经被显式传引擎的新版本替代**。

### 6.2 开始比赛

* 在speechManager.h中提供比赛的的成员函数 `void speechContest();`

```cpp
//比赛
void speechContest();
```

* 在speechManager.cpp中实现成员函数 `void speechContest();`

```cpp
void SpeechManager::speechContest() {
    cout << "------------- 第" << this->index << "轮正式比赛开始：------------- " << endl;
    //临时容器，保存key分数 value 选手编号
    multimap<double, int, greater<int> > groupScore;
    //记录人员数，6个为1组
    int num = 0;
    //比赛的人员容器
    vector<int> srcVector;
    if (this->index == 1) {
        srcVector = v1;
    } else {
        srcVector = v2;
    }
    //遍历所有参赛选手
    for (vector<int>::iterator it = srcVector.begin(); it != srcVector.end(); it++) {
        num++;
        //评委打分
        deque<double> d;
        for (int i = 0; i < 10; i++) {
            // 600 ~ 1000
            double score = (rand() % 401 + 600) / 10.f;
            //cout << score << " ";
            d.push_back(score);
        }
        //排序
        sort(d.begin(), d.end(), greater<double>());
        //去掉最高分
        d.pop_front();
        //去掉最低分
        d.pop_back();
        //获取总分
        double sum = accumulate(d.begin(), d.end(), 0.0f);
        //获取平均分
        double avg = sum / (double) d.size();
        //每个人平均分
        //cout << "编号： " << *it  << " 选手： " << this->m_Speaker[*it].m_Name << " 获取平均分为： " << avg << endl;  //打印分数
        this->speaker[*it].score[this->index - 1] = avg;
        //6个人一组，用临时容器保存
        groupScore.insert(make_pair(avg, *it));
        if (num % 6 == 0) {
            cout << "第" << num / 6 << "小组比赛名次：" << endl;
            for (multimap<double, int, greater<int> >::iterator it = groupScore.begin(); it != groupScore.end(); it++) {
                cout << "编号: " << it->second
                        << " 姓名： " << this->speaker[it->second].name
                        << " 成绩： " << this->speaker[it->second].score[this->index - 1] << endl;
            }
            int count = 0;
            //取前三名
            for (multimap<double, int, greater<int> >::iterator it = groupScore.begin();
                 it != groupScore.end() && count < 3; it++, count++) {
                if (this->index == 1) {
                    v2.push_back((*it).second);
                } else {
                    victory.push_back((*it).second);
                }
            }
            groupScore.clear();
            cout << endl;
        }
    }
    cout << "------------- 第" << this->index << "轮比赛完毕  ------------- " << endl;
    cin.get();
}
```

在startSpeech比赛流程控制的函数中，调用比赛函数

```cpp
void SpeechManager::startSpeech() {
    //第一轮比赛
    //2、比赛
    this->speechContest();
}
```

* 再次运行代码，测试比赛

```text
------------- 第1轮正式比赛开始：------------- 
第1小组比赛名次：
编号: 10006 姓名： 选手F 成绩： 81.3125
编号: 10010 姓名： 选手J 成绩： 80.7
编号: 10008 姓名： 选手H 成绩： 77.95
编号: 10002 姓名： 选手B 成绩： 77.7375
编号: 10012 姓名： 选手L 成绩： 76.5625
编号: 10005 姓名： 选手E 成绩： 73.4375

第2小组比赛名次：
编号: 10007 姓名： 选手G 成绩： 84.4875
编号: 10001 姓名： 选手A 成绩： 82.875
编号: 10011 姓名： 选手K 成绩： 80.3625
编号: 10009 姓名： 选手I 成绩： 79
编号: 10004 姓名： 选手D 成绩： 78.0625
编号: 10003 姓名： 选手C 成绩： 77.375
```

每组 6 名选手按平均分**降序**输出，前 3 名（10006 / 10010 / 10008 在第 1 组，10007 / 10001 / 10011 在第 2 组）会被压入 `v2` 进入下一轮。每名选手 10 个评委分数中**去掉一个最高、去掉一个最低**，剩下 8 个取均值——这是模拟真实演讲比赛的"去极端值"打分规则。

> 🧠 **`multimap<double, int, greater<int>>` 三剑客协作**：① `multimap` 允许同分（real-world 完全可能两人 80.5），② key 为 `double` 即分数，③ 自定义比较器 `greater<int>` 让遍历时**自动从高到低**——直接 `for (auto& it : groupScore)` 拿到的就是排好序的名次，**省掉一次显式 `sort`**。这是"用 STL 容器的内置排序代替手写循环"的经典示范。

> 🧠 **`accumulate(begin, end, 0.0f)` 注意第三个参数的类型**：`0.0f` 是 `float` → 累加用 `float`；如果写成 `0`（int）就会**整数累加 + 类型截断**，分数全部变成整数。`std::accumulate` 的初值类型决定累加器类型，是 `<numeric>` 里最容易踩的坑之一。

### 6.3 显示比赛分数

* 在speechManager.h中提供比赛的的成员函数 `void showScore();`

```cpp
//显示比赛结果
void showScore();
```

* 在speechManager.cpp中实现成员函数 `void  showScore();`

```cpp
void SpeechManager::showScore() {
    cout << "---------第" << this->index << "轮晋级选手信息如下：-----------" << endl;
    vector<int> v;
    if (this->index == 1) {
        //如果是第一轮，晋级6人
        v = v2;
    } else {
        //如果是第二轮，晋级3人
        v = victory;
    }
    for (vector<int>::iterator it = v.begin(); it != v.end(); it++) {
        cout << "选手编号：" << *it <<
            " 姓名： " << speaker[*it].name <<
                " 得分： " << speaker[*it].score[this->index - 1] << endl;
    }
    clearScreen();
    this->showMenu();
}
```

* 在startSpeech比赛流程控制的函数中，调用显示比赛分数函数

```cpp
void SpeechManager::startSpeech() {
    //第一轮比赛
    //3、显示晋级结果
    this->showScore();
}
```

* 运行代码，测试效果（**值与上文 6.2 的前三名严格一致**）：

```text
---------第1轮晋级选手信息如下：-----------
选手编号：10006 姓名： 选手F 得分： 81.3125
选手编号：10010 姓名： 选手J 得分： 80.7
选手编号：10008 姓名： 选手H 得分： 77.95
选手编号：10007 姓名： 选手G 得分： 84.4875
选手编号：10001 姓名： 选手A 得分： 82.875
选手编号：10011 姓名： 选手K 得分： 80.3625
```

`v2` 容器现在装着两个组的前 3 名 = 6 个晋级编号，**保留了 6.2 阶段每个人在 `multimap` 里被取出的顺序**——这就是为什么这里看起来不是按分数全局排序的。如果想让"晋级选手榜单"全局降序，只需要在 `showScore` 里再 `sort` 一次。

> ⚠️ **第一轮比赛后 `v1` 仍然装着 12 人**：晋级的 6 人去了 `v2`，但 `v1` **并没有被清空**。这导致下一次菜单选 1 时如果不重新初始化，`v1` 还是上一届的 12 人——所以 `clearRecord` 里特意调用了一遍 `initSpeech` 来 `clear()` 全部容器，**这是设计上"届与届之间不串扰"的关键**。


## 07.第二轮比赛

第二轮比赛流程同第一轮，只是比赛的轮是+1，其余流程不变。

### 7.1 抽签比赛和晋级

在startSpeech比赛流程控制的函数中，加入第二轮的流程

```cpp
void SpeechManager::startSpeech() {
    //第一轮比赛
    //1、抽签
    this->speechDraw();
    //2、比赛
    this->speechContest();
    //3、显示晋级结果
    this->showScore();
    //第二轮比赛
    this->index++;
    //1、抽签
    this->speechDraw();
    //2、比赛
    this->speechContest();
    //3、显示最终结果
    this->showScore();
    //4、保存分数
}
```

测试，将整个比赛流程都跑通：

```text
第 << 2 >> 轮比赛选手正在抽签，打乱用户顺序
---------------------
抽签后演讲顺序如下：
10001 10005 10010 10004 10012 10002 
---------------------

------------- 第2轮正式比赛开始：------------- 
第1小组比赛名次：
编号: 10010 姓名： 选手J 成绩： 83.6125
编号: 10005 姓名： 选手E 成绩： 82.7375
编号: 10004 姓名： 选手D 成绩： 80.725
编号: 10001 姓名： 选手A 成绩： 79.9625
编号: 10012 姓名： 选手L 成绩： 77.9125
编号: 10002 姓名： 选手B 成绩： 75.3875

------------- 第2轮比赛完毕  ------------- 
---------第2轮晋级选手信息如下：-----------
选手编号：10010 姓名： 选手J 得分： 83.6125
选手编号：10005 姓名： 选手E 得分： 82.7375
选手编号：10004 姓名： 选手D 得分： 80.725
```

第二轮只有 6 人，刚好打成 1 组——前 3 名就是冠亚季军。整个 startSpeech 一气呵成跑完两轮，比赛流程的"骨架"就齐了，下一节给它加上**结果落盘**的最后一步。

> 💡 **`this->index++` 是连接两轮的"开关"**：`speechDraw` / `speechContest` / `showScore` 三个函数内部都用 `this->index` 区分"读 v1 还是读 v2"、"分数写到 score[0] 还是 score[1]"。这种**用一个状态变量控制函数行为**的写法，比"每一轮各写一份代码"省一半篇幅，是"少而精的代码"的典型样式。

### 7.2 保存分数

**功能描述：** 将每次演讲比赛的得分记录到文件中

**功能实现：** 在speechManager.h中添加保存记录的成员函数 `void saveRecord();`

```cpp
//保存记录
void saveRecord();
```

* 在speechManager.cpp中实现成员函数 `void saveRecord();`

```cpp
void SpeechManager::saveRecord() {
    ofstream ofs;
    //用输出的方式打开文件  -- 写文件（注意：文件名要和 loadRecord 保持一致）
    ofs.open("speech.csv", ios::out | ios::app);
    //将每个人数据写入到文件中（CSV 格式：编号,得分,编号,得分,编号,得分）
    for (vector<int>::iterator it = victory.begin(); it != victory.end(); it++) {
        ofs << *it << "," << speaker[*it].score[1] << ",";
    }
    ofs << endl;
    //关闭文件
    ofs.close();
    cout << "记录已经保存" << endl;
}
```

**测试保存**。在主菜单输入 `1` 跑完整届比赛，末尾会触发 `saveRecord`：

```text
---------第2轮晋级选手信息如下：-----------
选手编号：10010 姓名： 选手J 得分： 83.6125
选手编号：10005 姓名： 选手E 得分： 82.7375
选手编号：10004 姓名： 选手D 得分： 80.725
记录已经保存
```

打开 `speech.csv`，应该能看到一行：

```text
10010,83.6125,10005,82.7375,10004,80.725,
```

`ios::app` **追加模式**保证多次比赛不会互相覆盖——开第二届时新一行会接在上一届后面。每行 6 个字段（编号 1, 分数 1, 编号 2, ……）就是 8.1 加载时按逗号 split 还原的依据。

> ⚠️ **`saveRecord` 的文件名必须与 `loadRecord` 完全一致**：原文里 `saveRecord` 写的是 `"speech.txt"`、`loadRecord` 读的是 `"speech.csv"`、`clearRecord` 清的也是 `"speech.txt"`——三个函数指向**两个不同文件**。结果是：保存正常、查看记录永远报"文件不存在"、清空后看记录依然是上一届的脏数据。**修复方式：把所有读写位置的文件名统一为 `speech.csv`**（推荐，因为内容确实是 CSV）。这种"多处常量散落 → 不一致"的坑，用 `static const std::string kRecordFile = "speech.csv";` 集中定义即可彻底根治。

> ⚠️ **原代码 `ofs << *it << "号选手，分数是：" << ...` 不是 CSV**：那种自然语言行 `loadRecord` 里 `data.find(",")` 根本切不出 6 个字段，会一直进死循环或得到错误数组。本案例**真正能跑通**的 saveRecord 必须输出严格的逗号分隔格式，与 loadRecord 的解析逻辑配对。

至此，整个演讲比赛功能制作完毕！


## 08.查看记录

### 8.1 读取记录分数

* 在speechManager.h中添加保存记录的成员函数 `void loadRecord();`
* 添加判断文件是否为空的标志  `bool fileIsEmpty;`
* 添加往届记录的容器`map<int, vector<string>> record;`

其中`record`中的key代表第几届，value记录具体的信息

```cpp
//读取记录
void loadRecord();
//文件为空的标志
bool fileIsEmpty;
//往届记录
map<int, vector<string>> record;
```

* 在speechManager.cpp中实现成员函数 `void loadRecord();`

```cpp
void SpeechManager::loadRecord() {
    //输入流对象 读取文件
    ifstream ifs("speech.csv", ios::in);
    if (!ifs.is_open()) {
        this->fileIsEmpty = true;
        cout << "文件不存在！" << endl;
        ifs.close();
        return;
    }
    char ch;
    ifs >> ch;
    if (ifs.eof()) {
        cout << "文件为空!" << endl;
        this->fileIsEmpty = true;
        ifs.close();
        return;
    }
    //文件不为空
    this->fileIsEmpty = false;
    ifs.putback(ch); //读取的单个字符放回去
    string data;
    int index = 0;
    while (ifs >> data) {
        //cout << data << endl;
        vector<string> v;
        int pos = -1;
        int start = 0;
        while (true) {
            pos = data.find(",", start); //从0开始查找 ','
            if (pos == -1) {
                break; //找不到break返回
            }
            string tmp = data.substr(start, pos - start); //找到了,进行分割 参数1 起始位置，参数2 截取长度
            v.push_back(tmp);
            start = pos + 1;
        }
        this->record.insert(make_pair(index, v));
        index++;
    }
    ifs.close();
}
```

* 在SpeechManager构造函数中调用获取往届记录函数

### 8.2 查看记录功能

* 在speechManager.h中添加保存记录的成员函数 `void showRecord();`

```cpp
//显示往届得分
void showRecord();
```

* 在speechManager.cpp中实现成员函数 `void showRecord();`

```cpp
void SpeechManager::showRecord() {
    for (int i = 0; i < this->record.size(); i++) {
        cout << "第" << i + 1 << "届 " <<
                "冠军编号：" << this->record[i][0] << " 得分：" << this->record[i][1] << " "
                "亚军编号：" << this->record[i][2] << " 得分：" << this->record[i][3] << " "
                "季军编号：" << this->record[i][4] << " 得分：" << this->record[i][5] << endl;
    }
    pauseAndCls();
}
```

### 8.3 测试功能

在main函数分支 2  选项中，调用查看记录的接口

```cpp
case 2:  //查看记录
    sm.showRecord();
    break;
```

**三种状态各跑一次**——这是最容易在不同环境下出现差异的地方：

**① 文件不存在（首次运行）**：

```text
请输入您的选择： 
2
文件不存在！
```

`SpeechManager` 构造时 `loadRecord` 探测到 `speech.csv` 缺失，把 `fileIsEmpty` 置 true。后续 `showRecord` 因 `record.size() == 0` 直接退出循环。

**② 跑完一届比赛后查看（已存 1 行）**：

```text
请输入您的选择： 
2
第1届 冠军编号：10006 得分：81.3125 亚军编号：10010 得分：80.7 季军编号：10008 得分：77.95
```

**③ 多届记录（连续跑了 4 届后）**：

```text
请输入您的选择： 
2
第1届 冠军编号：10006 得分：81.3125 亚军编号：10010 得分：80.7 季军编号：10008 得分：77.95
第2届 冠军编号：10004 得分：82.40 亚军编号：10009 得分：80.13 季军编号：10001 得分：78.55
第3届 冠军编号：10008 得分：83.20 亚军编号：10002 得分：81.10 季军编号：10005 得分：79.95
第4届 冠军编号：10010 得分：85.30 亚军编号：10003 得分：82.50 季军编号：10006 得分：80.40
```

每届一行、6 个字段（冠/亚/季 各 1 个编号 + 1 个分数）——CSV 格式的好处一目了然：**每行格式固定，行数 = 届数，肉眼即可数**。

> ⚠️ **原文打印里"得分： 亚军编号： 得分："字段全空** 是个明显 bug——根因是 7.2 的 `saveRecord` 输出了"10004号选手，分数是：xxx"自然语言行，而 8.1 的 `loadRecord` 用逗号切分根本切不到 6 个字段，导致 `record[i][1] / [3] / [5]` 这些分数位是空字符串。**修复了 7.2 的 CSV 输出后，这里的打印才正常**——这是个典型"bug 跨函数传染"案例：写盘格式不对，读盘和展示都跟着错。

## 09.清空记录

### 9.1 清空记录功能

* 在speechManager.h中添加保存记录的成员函数 `void clearRecord();`

```cpp
//清空记录
void clearRecord();
```

* 在speechManager.cpp中实现成员函数 `void clearRecord();`

```cpp
void SpeechManager::clearRecord() {
    cout << "确认清空？" << endl;
    cout << "1、确认" << endl;
    cout << "2、返回" << endl;
    int select = 0;
    cin >> select;
    if (select == 1) {
        //打开模式 ios::trunc 如果存在删除文件并重新创建
        //（注意：文件名要与 saveRecord / loadRecord 完全一致）
        ofstream ofs("speech.csv", ios::trunc);
        ofs.close();
        //初始化属性
        this->initSpeech();
        //创建选手
        this->createSpeaker();
        //获取往届记录
        this->loadRecord();
        cout << "清空成功！" << endl;
    }
}
```

### 9.2 测试清空功能

在main函数分支 3  选项中，调用清空比赛记录的接口

```cpp
case 3:  //清空记录
    sm.clearRecord();
    break;
```

运行程序，假设当前 `speech.csv` 已经有 4 届记录，测试完整闭环：

```text
请输入您的选择： 
3
确认清空？
1、确认
2、返回
1
文件为空!
清空成功！
```

再选 `2` 查看记录，应当**没有任何输出**——`record` 已经是 0 元素：

```text
请输入您的选择： 
2
```

`clearRecord` 干了 4 件事：① `ios::trunc` 把磁盘文件清零；② `initSpeech` 把 4 个 `vector` / `map` 容器全部 `clear`；③ `createSpeaker` 重建 12 名 0 分初始选手；④ `loadRecord` 重新探测文件——此时文件被清空，`fileIsEmpty=true`、`record` 维持空。**这一连串调用确保"清空"是真正意义上的恢复出厂状态，而不是只删一半**。

> 💡 **"清空 = 磁盘清 + 内存清 + 重建初始数据"三步走**：很多新手只做第一步（删文件）就觉得万事大吉，但内存里 `record` 容器还装着上一届的数据，下次 `showRecord` 会看到鬼影记录。这个三步走模式在 02 银行账号、03 职工管理里都用同样的套路：**重置内存 → 重置磁盘 → 重新加载**。

speech.csv中记录也为空

* 至此本案例结束！ `^_^`

## 10.一些拓展功能
### 10.1 选手信息增强

添加了年龄、院系等详细信息
自动生成合理的随机数据
数据验证功能

### 10.2 比赛系统增强

动画效果支持
音效系统（使用系统铃声）
更详细的比赛过程展示
排名系统
成绩动态计算算法

### 10.3 数据管理增强

完整的CSV导入导出
数据归档功能
自动备份系统
系统配置持久化

### 10.4 记录系统增强

完整的三名选手信息记录
比赛时间戳
多维度统计（选手表现、院系表现）
归档系统防止数据溢出

### 10.5 日志与审计

详细的操作历史记录
自动日志保存
系统事件跟踪

### 10.6 系统管理工具

配置管理（动画、音效、调试模式等）
系统验证工具
数据备份与恢复

### 10.7 用户体验增强

分阶段提示
表格化数据展示
动画效果
响应式按键处理

## 11.一些bug优化
### 11.1 >> 被解析为右移运算符

问题代码如下所示：

```cpp
//往届记录
map<int, vector<string>> record;
```

报错说明：

```bash
 error: a space is required between consecutive right angle brackets (use '> >')
   32 |     map<int, vector<string>> record;
      |                           ^~
      |                           > >
```

这个错误是因为在 C++11 之前的版本中，连续的两个右尖括号 >> 会被解析为右移运算符，而不是模板参数结束符。

在 C++98/C++03 标准中： 在 vector<string>> 之间添加空格，改为 vector<string> >：

```cpp
map<int, vector<string> > record;  // 注意这里的空格
```



## 12.衔接与延伸

### 12.1 与上一案例的差异

| 维度 | 04 简易数据库 | 05 演讲比赛（本篇） |
|------|--------------|---------------------|
| 思维路径 | 通用数据容器 | 业务专用模型 |
| 容器使用 | 自定义 Row/Column | 直接 `vector` + `map` + `multimap` |
| 算法 | 手写增删改查 | `std::sort` + Lambda + `std::shuffle` |
| 持久化 | 字段级序列化 | 整张表一行一条记录 |

### 12.2 与下一案例的递进

[06.机房预约系统](./06.机房预约系统.md) 是 OOP **综合大考**——将把 02/03 的类、05 的 STL、04 的文件三者拧在一起，**按身份（学生/老师/管理员）分三条流程线**。如果说 02-05 是"单点技术练习"，06 就是"中等规模项目的第一次演习"。建议本案例做透后再进 06，否则容易被 1500 行代码的规模劝退。

### 12.3 三个延伸挑战

**A（基础）· 修复 `>>` 右移被误解析**：把所有 `map<int, vector<Player>>` 改写成 C++11 起的合法写法（不加空格），确认你的编译器至少开到 `-std=c++11`；如果必须用旧编译器，保留空格 `vector<Player> >`。

**B（进阶）· 比赛规则可配**：目前是"12 人 → 6 人 → 3 人"固定晋级规则。把它提取为 `struct ContestConfig { int totalPlayers, groupSize, advanceCount; int rounds; }`，菜单增加"配置比赛"选项。这考察**数据与逻辑分离**的能力。

**C（挑战）· 榜单持久化到 SQLite/JSON**：把"查看记录"改为支持多届比赛历史对比——引入 `nlohmann/json` 或 `SQLite3` 第三方库。完成后你就摸到了"**项目依赖第三方库**"这个从入门到工程化的关键一跃。

---

- ⬅ 上一案例：[04.简易的数据库](./04.简易的数据库.md)
- ➡ 下一案例：[06.机房预约系统](./06.机房预约系统.md) —— 1500 行、12 个文件、3 种身份：中等项目第一次演习
