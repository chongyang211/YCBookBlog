# 05.图形和动画
#### 目录介绍
- 5.1 颜色和渐变
  - 5.1.1 颜色
  - 5.1.2 渐变
  - 5.1.3 调色板
- 5.2 图片展示
  - 5.2.1 显示静态图片
  - 5.2.2 带边界的图片
  - 5.2.3 动态图片
- 5.3 Transform变换
  - 5.3.1 缩放（Scale）
  - 5.3.2 旋转（Rotation）
  - 5.3.3 平移（Translation）
- 5.4 状态
  - 5.4.1 创建状态
  - 5.4.2 默认状态（default state）
  - 5.4.3 when属性
- 5.5 动画和过渡
  - 5.5.1 基本动画
  - 5.5.2 并行动画
  - 5.5.3 顺序动画
  - 5.5.4 状态和过渡
  - 5.5.5 行为动画
  - 5.5.6 路径动画
  - 5.5.7 动画总结
- 5.6 图形效果
  - 5.6.1 图形模块介绍
  - 5.6.2 阴影效果
  - 5.6.3 模糊效果
  - 5.6.4 渐变效果
  - 5.6.5 颜色叠加
  - 5.6.6 发光效果
  - 5.6.7 遮罩效果
  - 5.6.8 组合效果
  - 5.6.9 图形效果总结
- 5.7 复杂动画效果
  - 5.7.1 AnimatedImage
  - 5.7.2 序列帧替代
  - 5.7.3 使用Lottie动画
- 5.8 综合案例




## 5.1 颜色和渐变

在 QML 中，你可以使用颜色、渐变和调色板来美化界面、突出显示元素或为用户提供更丰富的视觉体验。下面是关于在 QML 中使用颜色、渐变和调色板的简要介绍：

### 5.1.1 颜色

在 QML 中，颜色可以使用预定义的颜色名称（如 "red"、"blue"）或使用 RGB、RGBA、HSL、HSV 等格式来表示。以下是一些示例：

使用预定义颜色名称：

```qml
Rectangle {
    color: "lightblue"
}
```

使用 RGB 格式：

```qml
Rectangle {
    color: "#ff0000" // 红色
}
```

### 5.1.2 渐变

QML 中的渐变可以用于创建平滑的颜色过渡效果。你可以使用线性渐变（`Gradient`）或辐射渐变（`RadialGradient`）。以下是一个简单的线性渐变示例：

```qml
Rectangle {
    width: 200
    height: 200

    gradient: Gradient {
        GradientStop { position: 0.0; color: "red" }
        GradientStop { position: 1.0; color: "blue" }
    }
}
```

### 5.1.3 调色板

调色板用于定义应用程序的整体颜色主题，包括按钮、文本、背景等元素的颜色。你可以在应用程序中定义自定义的调色板，以便在整个应用程序中保持一致的视觉风格。以下是一个简单的调色板示例：

```qml
ApplicationWindow {
    palette: Palette {
        base: "#f0f0f0" // 基础颜色
        alternateBase: "#e0e0e0" // 替代基础颜色
        text: "#333333" // 文本颜色
        button: "#007bff" // 按钮颜色
    }
}
```

## 5.2 图片展示

在 QML 中，你可以使用 `Image` 控件来显示静态图片，`BorderImage` 控件来显示带边界的图片，以及通过动态改变图片路径或属性来实现动态图片效果。

### 5.2.1 显示静态图片

使用 `Image` 控件可以在界面中显示静态图片。你可以指定图片的路径或 URL。以下是一个简单的示例：

```qml
Image {
    source: "image.png"
    width: 100
    height: 100
}
```

### 5.2.2 带边界的图片

`BorderImage` 控件允许你显示带有边界的图片，边界会根据图片的尺寸进行拉伸。以下是一个简单的示例：

```qml
BorderImage {
    source: "borderimage.png"
    border.left: 10
    border.right: 10
    border.top: 10
    border.bottom: 10
    width: 200
    height: 200
}
```

### 5.2.3 动态图片

你可以通过绑定属性或在运行时改变图片的路径来实现动态图片效果。以下是一个简单的示例，演示如何在按钮点击时切换图片：

```qml
Image {
    id: dynamicImage
    source: "image1.png"
    width: 100
    height: 100
}

Button {
    text: "Change Image"
    onClicked: {
        if (dynamicImage.source === "image1.png") {
            dynamicImage.source = "image2.png"
        } else {
            dynamicImage.source = "image1.png"
        }
    }
}
```

## 5.3 Transform变换

在 QML 中，你可以使用变换（Transform）来实现缩放、旋转和平移等效果，从而改变界面元素的外观和位置。下面是关于在 QML 中实现缩放、旋转和平移变换的简要介绍：

### 5.3.1 缩放（Scale）

使用 `Scale` 变换可以实现对界面元素的缩放效果。你可以设置 `xScale` 和 `yScale` 属性来分别控制水平和垂直方向的缩放比例。以下是一个简单的示例：

```qml
Rectangle {
    width: 100
    height: 100
    color: "red"

    transform: Scale {
        xScale: 2
        yScale: 1.5
    }
}
```

### 5.3.2 旋转（Rotation）

使用 `Rotation` 变换可以实现对界面元素的旋转效果。你可以设置 `angle` 属性来指定旋转角度。以下是一个简单的示例：

```qml
Rectangle {
    width: 100
    height: 100
    color: "blue"

    transform: Rotation {
        origin.x: 50
        origin.y: 50
        angle: 45
    }
}
```

### 5.3.3 平移（Translation）

使用 `Translation` 变换可以实现对界面元素的平移效果。你可以设置 `x` 和 `y` 属性来指定水平和垂直方向的平移距离。以下是一个简单的示例：

```qml
Rectangle {
    width: 100
    height: 100
    color: "green"

    transform: Translation {
        x: 50
        y: 50
    }
}
```

## 5.4 状态

### 5.4.1 创建状态

在 QML 中创建状态需要使用 `State` 和 `StateGroup` 组件。`State` 用于定义单个状态，而 `StateGroup` 用于组织和管理多个状态。

以下是一个简单的示例，展示如何在 QML 中创建状态：

```qml
Rectangle {
    width: 100
    height: 100

    StateGroup {
        id: stateGroup

        State {
            name: "active"
            PropertyChanges { target: rectangle; color: "red" }
        }

        State {
            name: "inactive"
            PropertyChanges { target: rectangle; color: "blue" }
        }
    }

    transitions: Transition {
        NumberAnimation { properties: "color"; duration: 500 }
    }

    MouseArea {
        anchors.fill: parent
        onClicked: {
            if (stateGroup.state === "active") {
                stateGroup.state = "inactive"
            } else {
                stateGroup.state = "active"
            }
        }
    }
}
```

在这个示例中，我们创建了一个矩形，并定义了两个状态：`active` 和 `inactive`。在 `active` 状态下，矩形的颜色为红色；在 `inactive` 状态下，矩形的颜色为蓝色。通过点击矩形，可以在这两个状态之间进行切换。

- `StateGroup` 组件用于组织状态，并且可以在其中定义多个 `State`。
- 在每个 `State` 中，我们使用 `PropertyChanges` 来指定在该状态下要改变的属性，这里是改变矩形的颜色。
- `transitions` 定义了状态切换时的过渡效果，这里使用了 `NumberAnimation` 来实现颜色的平滑过渡。
- 通过 `MouseArea` 的点击事件，我们在不同状态之间进行切换。

创建两个页面：MainPage和PassPage。使用状态（State）和过渡（Transition）来实现页面之间的切换动画。实现思路：

1. 定义一个根Item，包含两个状态：main和pass。
2. 在main状态显示MainPage，在pass状态显示PassPage。
3. 定义从main到pass和从pass到main的过渡动画。


### 5.4.2 默认状态（default state）

在 QML 中，`default` 状态和 `when` 属性是用于定义默认状态和条件状态切换的重要概念。

- 默认状态是在没有明确指定状态时应用的状态。当没有明确设置状态时，QML 会自动将元素置于默认状态。
- 你可以使用 `default` 关键字来指定默认状态。默认状态通常用于定义元素的初始状态。
- 下面是一个简单的示例，展示如何定义默认状态：

```qml
Rectangle {
    width: 100
    height: 100

states: [
    State {
        name: "active"
        PropertyChanges { target: rectangle; color: "red" }
    },
    State {
        name: "inactive"
        PropertyChanges { target: rectangle; color: "blue" }
    }
]

    default: State {
        name: "inactive"
    }

    transitions: Transition {
        NumberAnimation { properties: "color"; duration: 500 }
    }
}
```

在这个示例中，我们将默认状态设置为 `inactive`，这意味着在初始情况下，矩形会处于 `inactive` 状态。

### 5.4.3 when属性

- `when` 属性用于定义状态何时应用的条件。只有当 `when` 属性的条件为真时，相关状态才会被应用。
- 你可以在 `State` 中使用 `when` 属性来指定状态应用的条件。这使得状态切换可以根据特定条件进行。
- 下面是一个简单的示例，展示如何使用 `when` 属性：

```qml
Rectangle {
    width: 100
    height: 100

    states: [
        State {
            name: "active"
            when: mouseArea.containsMouse
            PropertyChanges { target: rectangle; color: "red" }
        },
        State {
            name: "inactive"
            when: !mouseArea.containsMouse
            PropertyChanges { target: rectangle; color: "blue" }
        }
    ]

    MouseArea {
        id: mouseArea
        anchors.fill: parent
    }

    transitions: Transition {
        NumberAnimation { properties: "color"; duration: 500 }
    }
}
```

在这个示例中，我们根据鼠标是否在 `MouseArea` 区域内来切换矩形的状态。当鼠标在区域内时，矩形处于 `active` 状态，颜色为红色；当鼠标不在区域内时，矩形处于 `inactive` 状态，颜色为蓝色。

通过使用 `default` 状态和 `when` 属性，你可以更灵活地控制状态的应用和切换，使界面元素的状态变化更具有交互性和动态性。

## 5.5 动画和过渡

在 QML 中，动画和过渡是用于创建动态用户界面的强大工具。QML 提供了多种动画类型和过渡效果，可以轻松实现平滑的 UI 交互。

1. 淡入淡出效果：使用 OpacityAnimation或 NumberAnimation控制 opacity属性 
2. 缩放效果：使用 ScaleAnimation或 NumberAnimation控制 scale属性 
3. 并行动画：使用 ParallelAnimation同时执行多个动画效果 
4. 顺序动画：使用 SequentialAnimation按顺序执行多个动画效果

### 5.5.1 基本动画

QML 提供了多种动画类型，如 `PropertyAnimation`、`NumberAnimation`、`ColorAnimation` 等，用于改变对象的属性值。**示例：移动一个矩形**

```qml
Rectangle {
    id: rect
    width: 100
    height: 100
    color: "red"

    // 淡入动画
    PropertyAnimation {
        id: fadeInAnimation
        property: "opacity"
        from: 0
        to: 1
        duration: 500
        easing.type: Easing.InOutQuad
        
        onStopped: {
            if (fadeInAnimation.target && fadeInAnimation.target.opacity === 0) {
                fadeInAnimation.target.destroy()
            }
        }
    }

    // 点击矩形开始动画
    MouseArea {
        anchors.fill: parent
        onClicked: fadeInAnimation.start()
    }
}
```

**说明**：

- `PropertyAnimation` 用于改变对象的属性（如 `x`、`y`、`width` 等）。
- `target` 指定动画的目标对象。
- `property` 指定要改变的属性。
- `to` 指定属性的目标值。
- `duration` 指定动画的持续时间（以毫秒为单位）。

1. **使用硬件加速**：
   ```qml
   layer.enabled: true
   ```

2. **避免复杂渲染**： 使用不透明度动画代替模糊效果，限制同时运行的动画数量

3. **使用帧优化**：
   ```qml
   Item {
       // ...
       clip: true
   }
   ```

4. **合理设置持续时间**：200-500ms 适合大多数界面动画，长时间动画应允许用户中断
5. **动画结束后清理资源**：
   ```qml
   PropertyAnimation {
       // ...
       onStopped: {
           if (target.opacity === 0) {
               target.destroy()
           }
       }
   }
   ```



### 5.5.2 并行动画

使用 `ParallelAnimation` 可以同时运行多个动画。**示例：同时改变位置和颜色**

```qml
import QtQuick 2.15

Item {
    width: 400
    height: 400

    Rectangle {
        id: rect
        width: 100
        height: 100
        color: "red"

        // 并行动画
        ParallelAnimation {
            id: parallelAnimation
            PropertyAnimation {
                target: rect
                property: "x"
                to: 300
                duration: 2000
            }
            ColorAnimation {
                target: rect
                property: "color"
                to: "blue"
                duration: 2000
            }
        }

        // 点击矩形开始动画
        MouseArea {
            anchors.fill: parent
            onClicked: parallelAnimation.start()
        }
    }
}
```

**说明**：`ParallelAnimation` 允许多个动画同时运行。

### 5.5.3 顺序动画

使用 `SequentialAnimation` 可以按顺序运行多个动画。**示例：先移动再改变颜色**

```qml
import QtQuick 2.15

Item {
    width: 400
    height: 400

    Rectangle {
        id: rect
        width: 100
        height: 100
        color: "red"

        // 顺序动画
        SequentialAnimation {
            id: sequentialAnimation
            PropertyAnimation {
                target: rect
                property: "x"
                to: 300
                duration: 2000
            }
            ColorAnimation {
                target: rect
                property: "color"
                to: "blue"
                duration: 2000
            }
        }

        // 点击矩形开始动画
        MouseArea {
            anchors.fill: parent
            onClicked: sequentialAnimation.start()
        }
    }
}
```

**说明**：`SequentialAnimation` 按顺序运行多个动画。

### 5.5.4 状态和过渡

使用 `State` 和 `Transition` 可以根据状态变化实现平滑的过渡效果。**示例：按钮点击切换状态**

```qml
import QtQuick 2.15
import QtQuick.Controls 2.15

ApplicationWindow {
    visible: true
    width: 400
    height: 400

    Rectangle {
        id: rect
        width: 100
        height: 100
        color: "red"

        // 定义状态
        states: [
            State {
                name: "moved"
                PropertyChanges {
                    target: rect
                    x: 300
                    color: "blue"
                }
            }
        ]

        // 定义过渡
        transitions: [
            Transition {
                from: "*"
                to: "moved"
                PropertyAnimation {
                    properties: "x,color"
                    duration: 1000
                }
            }
        ]

        // 点击矩形切换状态
        MouseArea {
            anchors.fill: parent
            onClicked: rect.state === "moved" ? rect.state = "" : rect.state = "moved"
        }
    }
}
```

**说明**：

- `State` 定义对象的不同状态。
- `Transition` 定义状态变化时的过渡效果。

### 5.5.5 行为动画

使用 `Behavior` 可以为属性变化自动添加动画。**示例：自动动画**

```qml
import QtQuick 2.15

Item {
    width: 400
    height: 400

    Rectangle {
        id: rect
        width: 100
        height: 100
        color: "red"

        // 行为动画：自动为 x 和 y 属性添加动画
        Behavior on x {
            PropertyAnimation { duration: 1000 }
        }
        Behavior on y {
            PropertyAnimation { duration: 1000 }
        }

        // 点击矩形改变位置
        MouseArea {
            anchors.fill: parent
            onClicked: {
                rect.x = Math.random() * 300
                rect.y = Math.random() * 300
            }
        }
    }
}
```

**说明**：`Behavior` 自动为属性变化添加动画。

### 5.5.6 路径动画

使用 `PathAnimation` 可以让对象沿着路径移动。**示例：沿路径移动**

```qml
import QtQuick 2.15

Item {
    width: 400
    height: 400

    Rectangle {
        id: rect
        width: 50
        height: 50
        color: "red"
    }

    PathAnimation {
        id: pathAnimation
        target: rect
        duration: 3000
        path: Path {
            startX: 0; startY: 0
            PathLine { x: 400; y: 400 }
            PathQuad { x: 200; y: 0; controlX: 400; controlY: 200 }
        }
    }

    // 点击开始动画
    MouseArea {
        anchors.fill: parent
        onClicked: pathAnimation.start()
    }
}
```

**说明**：

- `PathAnimation` 让对象沿着路径移动。
- `Path` 定义路径的形状。

### 5.5.7 动画总结

QML 提供了丰富的动画和过渡功能，可以轻松实现动态用户界面。常用的工具包括：

- **动画**：`PropertyAnimation`、`NumberAnimation`、`ColorAnimation` 等。
- **并行和顺序动画**：`ParallelAnimation`、`SequentialAnimation`。
- **状态和过渡**：`State`、`Transition`。
- **行为动画**：`Behavior`。
- **路径动画**：`PathAnimation`。


## 5.6 图形效果

在 QML 中，图形效果（如阴影、模糊、渐变等）可以通过 `QtGraphicalEffects` 模块实现。这个模块提供了多种视觉效果，可以轻松应用到 QML 元素上。

### 5.6.1 图形模块介绍

**使用 `QtGraphicalEffects` 模块**。首先，需要导入 `QtGraphicalEffects` 模块：

```qml
import QtGraphicalEffects 1.15
```

### 5.6.2 阴影效果

`DropShadow` 可以为元素添加阴影。

```qml
import QtQuick 2.15
import QtGraphicalEffects 1.15

Item {
    width: 400
    height: 400

    Rectangle {
        id: rect
        width: 200
        height: 200
        color: "lightblue"
        anchors.centerIn: parent
    }

    DropShadow {
        anchors.fill: rect
        horizontalOffset: 5
        verticalOffset: 5
        radius: 8
        samples: 16
        color: "#80000000"
        source: rect
    }
}
```

**参数说明**：

- `horizontalOffset` 和 `verticalOffset`：阴影的偏移量。
- `radius`：阴影的模糊半径。
- `samples`：阴影的采样数（值越大，效果越平滑）。
- `color`：阴影的颜色。

### 5.6.3 模糊效果

`GaussianBlur` 可以为元素添加模糊效果。

```qml
import QtQuick 2.15
import QtGraphicalEffects 1.15

Item {
    width: 400
    height: 400

    Image {
        id: image
        source: "https://via.placeholder.com/200"
        width: 200
        height: 200
        anchors.centerIn: parent
    }

    GaussianBlur {
        anchors.fill: image
        radius: 8
        samples: 16
        source: image
    }
}
```

**参数说明**：

- `radius`：模糊半径。
- `samples`：采样数（值越大，效果越平滑）。

### 5.6.4 渐变效果

`LinearGradient` 和 `RadialGradient` 可以为元素添加渐变背景。

```qml
import QtQuick 2.15
import QtGraphicalEffects 1.15

Item {
    width: 400
    height: 400

    Rectangle {
        id: rect
        width: 200
        height: 200
        anchors.centerIn: parent
    }

    LinearGradient {
        anchors.fill: rect
        start: Qt.point(0, 0)
        end: Qt.point(200, 200)
        gradient: Gradient {
            GradientStop { position: 0.0; color: "red" }
            GradientStop { position: 1.0; color: "blue" }
        }
        source: rect
    }
}
```

**参数说明**：

- `start` 和 `end`：渐变的起点和终点。
- `gradient`：定义渐变的颜色和位置。

### 5.6.5 颜色叠加

`ColorOverlay` 可以为元素添加颜色叠加效果。

```qml
import QtQuick 2.15
import QtGraphicalEffects 1.15

Item {
    width: 400
    height: 400

    Image {
        id: image
        source: "https://via.placeholder.com/200"
        width: 200
        height: 200
        anchors.centerIn: parent
    }

    ColorOverlay {
        anchors.fill: image
        source: image
        color: "#80FF0000" // 半透明红色
    }
}
```

**参数说明**：`color`：叠加的颜色。

### 5.6.6 发光效果

`Glow` 可以为元素添加发光效果。

```qml
import QtQuick 2.15
import QtGraphicalEffects 1.15

Item {
    width: 400
    height: 400

    Rectangle {
        id: rect
        width: 200
        height: 200
        color: "lightblue"
        anchors.centerIn: parent
    }

    Glow {
        anchors.fill: rect
        radius: 8
        samples: 16
        color: "yellow"
        source: rect
    }
}
```

**参数说明**：

- `radius`：发光半径。
- `color`：发光的颜色。

### 5.6.7 遮罩效果

`OpacityMask` 可以使用一个元素作为遮罩，控制另一个元素的透明度。

```qml
import QtQuick 2.15
import QtGraphicalEffects 1.15

Item {
    width: 400
    height: 400

    Image {
        id: image
        source: "https://via.placeholder.com/200"
        width: 200
        height: 200
        anchors.centerIn: parent
    }

    Rectangle {
        id: mask
        width: 200
        height: 200
        radius: 100
        visible: false
    }

    OpacityMask {
        anchors.fill: image
        source: image
        maskSource: mask
    }
}
```

**参数说明**：

- `source`：被遮罩的元素。
- `maskSource`：遮罩元素。

### 5.6.8 组合效果

可以将多个效果组合在一起，实现更复杂的视觉效果。**示例：阴影 + 模糊 + 颜色叠加**

```qml
import QtQuick 2.15
import QtGraphicalEffects 1.15

Item {
    width: 400
    height: 400

    Image {
        id: image
        source: "https://via.placeholder.com/200"
        width: 200
        height: 200
        anchors.centerIn: parent
    }

    DropShadow {
        anchors.fill: image
        horizontalOffset: 5
        verticalOffset: 5
        radius: 8
        samples: 16
        color: "#80000000"
        source: image
    }

    GaussianBlur {
        anchors.fill: image
        radius: 4
        samples: 16
        source: image
    }

    ColorOverlay {
        anchors.fill: image
        source: image
        color: "#8000FF00" // 半透明绿色
    }
}
```

### 5.6.9 图形效果总结

`QtGraphicalEffects` 模块提供了丰富的图形效果，包括：

- **阴影**：`DropShadow`
- **模糊**：`GaussianBlur`
- **渐变**：`LinearGradient`、`RadialGradient`
- **颜色叠加**：`ColorOverlay`
- **发光**：`Glow`
- **遮罩**：`OpacityMask`


## 5.7 复杂动画效果
### 5.7.1 AnimatedImage

在 Qt Quick 中，主要通过 `AnimatedImage` 组件来实现 GIF 播放功能。

```qml
import QtQuick 2.15
import QtQuick.Controls 2.15

ApplicationWindow {
    width: 400
    height: 400
    visible: true
    
    AnimatedImage {
        id: gifAnimation
        anchors.centerIn: parent
        source: "qrc:/animations/loading.gif"
        width: 200
        height: 200
        playing: true // 自动播放
        speed: 1.0 // 播放速度，1.0 为原始速度
        
        // 动画状态改变时触发
        onStatusChanged: {
            if (status === AnimatedImage.Ready) {
                console.log("GIF loaded successfully");
            } else if (status === AnimatedImage.Error) {
                console.error("GIF loading error");
            }
        }
        
        // 帧变化时触发
        onCurrentFrameChanged: {
            console.log("Current frame:", currentFrame, "/", frameCount);
        }
    }
    
    // 控制面板
    Column {
        anchors.bottom: parent.bottom
        anchors.horizontalCenter: parent.horizontalCenter
        spacing: 10
        
        Button {
            text: gifAnimation.playing ? "暂停" : "播放"
            onClicked: gifAnimation.playing = !gifAnimation.playing
        }
        
        Slider {
            width: 200
            from: 0.1
            to: 3.0
            value: gifAnimation.speed
            onValueChanged: gifAnimation.speed = value
            ToolTip.text: "播放速度: " + value.toFixed(1) + "x"
        }
    }
}
```

GIF 控制面板组件

```qml
// GifController.qml
Item {
    id: controller
    property alias source: animation.source
    property alias playing: animation.playing
    property alias speed: animation.speed
    
    AnimatedImage {
        id: animation
        anchors.fill: parent
    }
    
    // 动画状态指示器
    Rectangle {
        visible: animation.status === AnimatedImage.Loading
        anchors.fill: parent
        color: "#80000000"
        
        BusyIndicator {
            anchors.centerIn: parent
            running: true
        }
    }
    
    // 帧信息显示
    Text {
        anchors.bottom: parent.bottom
        anchors.right: parent.right
        anchors.margins: 5
        text: animation.playing ? `▶ ${animation.currentFrame + 1}/${animation.frameCount}` : "⏸"
        color: "white"
        style: Text.Outline
        styleColor: "black"
        font.bold: true
    }
    
    // 点击区域控制播放
    MouseArea {
        anchors.fill: parent
        onClicked: animation.playing = !animation.playing
    }
}
```

使用方式

```qml
GifController {
    width: 300
    height: 300
    anchors.centerIn: parent
    source: "qrc:/animations/success.gif"
    speed: 1.5
}
```

### 5.7.2 序列帧替代

```qml
AnimatedSprite {
    id: spriteAnimation
    width: 300
    height: 300
    anchors.centerIn: parent
    source: "qrc:/animations/sequence.png"
    frameCount: 24
    frameDuration: 40 // 每帧40ms
    frameWidth: 300
    frameHeight: 300
    running: true
}
```

### 5.7.3 使用Lottie动画

```qml
import QtLabs.Animation 1.0 // 需安装 Qt Labs Animation

LottieAnimation {
    id: lottieAnim
    anchors.fill: parent
    source: "qrc:/animations/animation.json"
    loops: Animation.Infinite // 无限循环
    running: true
}
```


爸，我跟心怡都累了，她很辛苦，我也很累，心累……
我们两个今年估计吵架近百次，闹过无数次离婚，数不清楚，每次都像是丢了半条命
事情总是有前因后果，但往往总是抓住某一点，让人形成极大的误差

1.就比如：昨天她让我找之前买的手机订单，我这边的确找不到，联系了客服打了电话找不到。她着急，说话气冲冲，我也累了，真的找不到，找不到就算了，她偏偏死咬着不放，说花了冤枉钱，我就说新手机买了就算了，她偏偏要我弄好。
结果是，她说我这个那个，但凡我顶上机具，她说我不是人，骂的比狗都难听。但吐槽说我不上心，没有说前因后果，倒是她极大的委屈。

2.就比如：糖果感受咳嗽，我说去医院开点药就可以，她各种担心拍片子各种检查，说是商量，我多说几句，她就说我不关心，很多事情说是商量，其实就是她做主。

3.就比如：她总是说我没在孩子身上花钱，其实我每年都交了不少，把我几乎所有的钱都上交了。每一次出去，买衣服，逛商场，其实我都是用花呗，我给她透明我每个月开支，几乎都是超支，她说没用这么多，我就说看看账单，我一提这个她就毛了
结果是，她说我爱算账，都没有了解前因后果，事情越说越难。那我为什么要提账单呀，出门几乎大多数消费我都挡了下来。

4.就比如：她总是说丧偶式婚姻，每次工作日，打电话吵架，说跟老公每一样，说婆媳很炸裂，我很多时候是安慰，有时候也会说，尽量不要用自己要求去要求婆婆，在家里闹的死去活来，去年就因这类事请加至少超过10天
结果是，吵架的时候，她就说我们一起欺负她，她这样一个性格，很能欺负她

5.有时候，她会说她前男友怎么样，别人如何接住她的情绪，任何事情都能让她情绪不好。她情绪不好，总会因为一些小事让我难受，她在学校里，跟几个同事闹的不好，后来我了解到别人是故意相互斗争的，其实自己又何尝不是用自己的标准去要求别人，那样自己肯定会很痛苦。

6.总是抱怨说这种生活不是自己想要的，活在梦中，虽然嘴巴上说过简单的生活，但总是渴望惊喜，制造什么浪漫。这种也吵过很多次，每次都说要是当初选择那个买家具的，或者前男友，会生活怎么样怎么样。

7.出去旅游，我买了票，做了安排，但怎么玩，完那里，总是用高标准要求，总是吵架

8.这些年，彩礼都给她了，生育大开支大都我承担，前期各种礼节钱都给她了，我身上钱大都打给了她，她总是说这个老公，那个老公都是全部上交的。我本以为上交之后，生活开支，外出费用她可以承担一部分，结果还是我处处透支，我一说用了超支账单多少，她就火来

9.她总是说，家里开支都是她，我都没出钱，我说不是打钱了吗，她说那个钱存着了，我说拿出来用呀，不能说我没在家里花钱。
聪明的男的，可以不上交，我上交那是实实在在的行为，比任何花言巧语总要强吧。我吵架不是因为花钱，而是花钱了，却总是说我都没有为家买过奶粉，都没在孩子身上花钱。

10.这些年，房子，车子，结婚各种开支，家具家电，学校的家电，生育开支，外出旅游，大额部分几乎都是我淘了，我没有怨言，但是我对说没有对家里小孩买个奶粉尿不湿。
我不是爱算账，爸，妈，你们可以想一想，我这样一个收入，为什么没有钱，为什么还要找她要小额的钱，为什么？
我想着她存着有安全感，但是每次吵架，否定一切，我讨厌这种，说自己为了家庭承担了很多，都是她出得钱，倒显得自己很委屈。

11.事情如果不了解来龙去脉，说我爱算账，那为什么不多问问我为什么要这样，能不能说事情，说一下前因后果，说一下事情的背景。
吵架永远盯着某句话，10句话你一句话没说好，就一定盯着不放。

12.睡眠的事情，她天天说带糖果辛苦。但是爸，我需要跟你说，糖果大多数时候是跟婆婆说，做饭，洗衣服，买菜。只是有时候糖果闹的时候，需要她，带孩子的确辛苦。但每次总是说为了孩子付出了多少多少，你知道生育的代价吗，你会生孩子吗，我无言以对。
沟通很多次，说把糖果带到武汉来。她不愿意。不带过来她说带孩子辛苦。每天都辛苦。其实老师相对其他行业来说相对轻松，时间宽裕，爸，我跟你都在工地做过，还有无数的人都是在基础层劳动，工作起来那根本就很难休息。
还有一点，她避而不说，她每天玩手机，玩到12点，然后凌晨3点后又开始玩手机，每天玩手机痴迷，带糖果累不假，但也不能都找外部原因。

13.爸，妈，你们应该清楚她的消费习惯。虽然说她的收入不低，但是想要过上那种想要的生活，可能也不容易。人，都不容易，都有各自贡献。还有几次亲戚来了说房子太简陋没买好，总是说换个房子，吵架起来那是什么话都说的出来。
因此，我渐渐也增加一些家具。爸，妈，家需要两个人一起齐心协力。

14.糖果住院两次，那两次，我都是找朋友借的，她其实知道，我之前有开口找她要过钱。她每一次给我钱，要把钱的价值最大化，要让爸妈知道是她出得钱，我妈总是问我这样的收入为什么总是没钱。
但其实每个月都开支很大，我这边都是1万以上。注意：不包括：房贷+保险。

15.前段时间糖果去医院，第一次打电话让我支付医疗费，我每个月都会给她发开支记录，她知道我的情况，我每次找别人借钱。第二次她说没钱了，她本来想打电话我，结果我爸妈付了。
还有，总是爱突然叫我回来，父母那一句话说的不好，总是盯着那句话，总是忽略背景和前因后果，爱生气闹情绪，有时候发疯真的很可怕，然后每次就是离家出走
刚开始找不到人，我打电话找邦哥，找伟龙，她总是说的找坏了，后来她拿我手机把伟龙，邦哥，兵哥都删了，说实话她不跑，我干嘛要打电话别人问。

还有一点，我俩吵架100次，也就找了你们两三次，人最痛苦就是无可奈何。

16.总是抱怨原生家庭，没有给自己好的，让自己自卑，留守。说什么要是家庭条件好点，就可以嫁的更好。没错，以前相亲的，她嫁了，可能说过的更好。但能保证婚姻一定就能过程期望的那样吗？

17.爸，我住宿住最便宜的，衣服几乎很少买，商家任何策略都无法让我花费。那为什么这样一个人，总是没钱了，爱算账，是否可以再问问，事情的前因后果，掐头去尾有些话性质就完全变了。

18.去年我姐把孩子送过来，那是每一天都情绪，我想着外甥过来待个一周两周也好。但是她总是说没有尊重她，说我姐把孩子送过来强势，每天闹情绪。后来糖果办周岁，她说暑假过来玩，后来想来，她每天跟我闹情绪，我但凡多说几句，就吵架，闹离婚，我真的是搞怕了。

19.爸妈照顾糖果，虽然不能事事让自己满意，但是他们细心，不想过这样的日子，然后又天天内耗，让父母走其实也可以，但是没人带孩子，吵起来的时候说为什么不是我生孩子，我为什么不带孩子。
举一个例子：做小孩伙食，那是要按照她的要求来，有时候十几种素材，一般人很难搞，当你用高标准要求的时候，这就是她痛苦的根源。这仅仅是一个例子，其他很多场景，累计起来，人怎么不会因为内耗而累。

20.下班了，放假了，我回来总会给她按按摩吧，我自己也很少说累。她每天都很辛苦，我买了按摩床，按摩靠垫，座椅，当你付出很多时，人就容易把它当作应该的，会本能的忽视爱。
这就是为什么有时候父母各种爱总是容易被我们忽视，但是陌生人给我帮一个忙就会感恩戴德，人性就是如此。总是忽略自己拥有的

21.除了闹离婚，一年起码要被删除近100次，要你通过各种方式找她。她拉出黑名单就是一顿痛骂，骂完后就拉黑。然后就说为什么总是不找，我说累了，次数多了累了。

22.因为生育，总是吐槽她自己妈没来看她，说怎么忽略她，骨子里总是埋冤，人到了年龄，也理解做父母的不容易。为什么总是要刀子嘴，为什么。

爸，从实际来说，做永远比说更有说服力。我期望婚姻稳定，但总是力不从心，是非对错很难分。

刀子嘴，从来不是豆腐心。人要对自己行为负责，真正善良的人，说一万句好，不如一次实际行动有用。






