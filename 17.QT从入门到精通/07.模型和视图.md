# 07.模型和视图
- 7.1 模型/视图架构
  - 7.1.1 模型/视图介绍
  - 7.1.2 模型（Model）
  - 7.1.3 视图（View）
  - 7.1.4 委托（Delegate）
- 7.2 数据模型
  - 7.2.1 整数作为模型
  - 7.2.2 ListModel
  - 7.2.3 WorkerScript
  - 7.2.4 ObjectModel
  - 7.2.5 DelegateModel
  - 7.2.6 Package
  - 7.2.7 XmlListModel
  - 7.2.8 LocalStorage
  - 7.2.9 C++扩展QML模型
- 7.3 视图类型
  - 7.3.1 ListView
  - 7.3.2 GridView
  - 7.3.3 视图过渡
  - 7.3.4 PathView

## 7.1 模型/视图架构
### 7.1.1 模型/视图介绍

在 QML 中，**模型/视图（Model/View）架构**是一种用于管理和显示数据的强大模式。

它将数据（模型）与用户界面（视图）分离，使得数据的管理和显示更加灵活和高效。

### 7.1.2 模型（Model）

模型是数据的来源，负责存储和管理数据。

在 QML 中，模型可以是 `ListModel`、`XmlListModel`、`JsonListModel` 或自定义的 C++ 模型。

### 7.1.3 视图（View）

视图是数据的展示方式，负责将模型中的数据渲染到用户界面。

在 QML 中，视图可以是 `ListView`、`GridView`、`TableView` 等。

### 7.1.4 委托（Delegate）

委托定义了如何渲染模型中的每一项数据。

在 QML 中，委托通常是一个 `Component`，用于定义每一项的 UI 和行为。

## 7.2 数据模型
### 7.2.1 整数作为模型


### 7.2.2 ListModel

`ListModel` 是一个简单的列表模型，`ListView` 是一个用于显示列表数据的视图。**案例：`ListModel` 和 `ListView`**

```qml
import QtQuick 2.15
import QtQuick.Controls 2.15

ApplicationWindow {
    visible: true
    width: 800
    height: 600
    title: "ListModel and ListView Example"

    // 定义模型
    ListModel {
        id: fruitModel
        ListElement { name: "Apple"; color: "red" }
        ListElement { name: "Banana"; color: "yellow" }
        ListElement { name: "Grape"; color: "purple" }
    }

    // 定义视图
    ListView {
        anchors.fill: parent
        model: fruitModel // 绑定模型
        delegate: fruitDelegate // 绑定委托
    }

    // 定义委托
    Component {
        id: fruitDelegate
        Rectangle {
            width: ListView.view.width
            height: 50
            color: model.color // 使用模型数据
            Text {
                text: model.name // 使用模型数据
                anchors.centerIn: parent
                font.pixelSize: 20
                color: "white"
            }
        }
    }
}
```



### 7.2.3 WorkerScript
### 7.2.4 ObjectModel
### 7.2.5 DelegateModel
### 7.2.6 Package
### 7.2.7 XmlListModel

`XmlListModel` 是一个用于解析 XML 数据的模型。**案例：`XmlListModel` 和 `ListView`**

```qml
import QtQuick 2.15
import QtQuick.Controls 2.15

ApplicationWindow {
    visible: true
    width: 800
    height: 600
    title: "XmlListModel and ListView Example"

    // 定义 XML 模型
    XmlListModel {
        id: xmlModel
        source: "data.xml" // XML 文件路径
        query: "/items/item" // XPath 查询
        XmlRole { name: "name"; query: "name/string()" }
        XmlRole { name: "color"; query: "color/string()" }
    }

    // 定义视图
    ListView {
        anchors.fill: parent
        model: xmlModel // 绑定模型
        delegate: xmlDelegate // 绑定委托
    }

    // 定义委托
    Component {
        id: xmlDelegate
        Rectangle {
            width: ListView.view.width
            height: 50
            color: model.color // 使用模型数据
            Text {
                text: model.name // 使用模型数据
                anchors.centerIn: parent
                font.pixelSize: 20
                color: "white"
            }
        }
    }
}
```


### 7.2.8 LocalStorage
### 7.2.9 C++扩展QML模型



