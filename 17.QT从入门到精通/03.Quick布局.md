# 03.Quick布局
#### 目录介绍
- 3.1 基础可视项目
  - 3.1.1 Item
  - 3.1.2 Rectangle
  - 3.1.3 Text
  - 3.1.4 TextInput
  - 3.1.5 TextEdit


## 3.1 基础可视项目

### 3.1.1 Item

`Item` 是一个非常重要的基础类型。它是所有可视化元素的基类，但本身并不直接绘制任何内容。`Item` 提供了一个容器和布局的基础，用于组织和管理其他可视化元素。

#### 3.1.1.1 Item是什么

`Item` 是 Qt Quick 中的一个核心类型，位于 `QtQuick` 模块中。它是一个不可见的容器，主要用于：
- 定义一个矩形区域。
- 组织和管理子元素。
- 提供基本的属性（如位置、大小、锚点等）。
- 处理输入事件（如鼠标和触摸事件）。

`Item` 本身不会绘制任何内容，但它可以作为其他可视化元素（如 `Rectangle`、`Image`、`Text` 等）的父级。

#### 3.1.1.2 Item常用属性

几何属性：

- **`x` 和 `y`**: 定义 `Item` 的位置（相对于父级）。
- **`width` 和 `height`**: 定义 `Item` 的宽度和高度。
- **`z`**: 定义 `Item` 的堆叠顺序，值越大，越靠前显示。

锚点属性：

- **`anchors`**，用于将 `Item` 锚定到父级或其他元素。

```qml
Rectangle {
  width: 200; height: 200
  color: "lightblue"

  Rectangle {
      width: 50; height: 50
      color: "red"
      anchors.centerIn: parent
  }
}
```

可见性属性

- **`visible`**: 控制 `Item` 是否可见（`true` 或 `false`）。
- **`opacity`**: 控制 `Item` 的透明度（范围为 0.0 到 1.0）。


### 3.1.2 Rectangle



### 3.1.3 Text


### 3.1.4 TextInput


### 3.1.5 TextEdit











