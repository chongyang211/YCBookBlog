#### 目录说明
- 01.简易线程池设计
  - 1.1 产品需求说明
  - 1.2 程序功能
  - 1.3 开发设计步骤
  - 1.4 技术栈说明
  - 1.5 核心组件说明
- 02.定义线程池类
  - 2.1 包含必要头文件
  - 2.2 声明公有接口
- 03.实现构造函数
  - 3.1 带名称构造
  - 3.2 默认构造函数
  - 3.3 实现析构函数
- 04.实现清理任务
  - 4.1 清空普通任务
  - 4.2 清空优先任务
  - 4.3 清空所有任务
- 05.线程主循环函数
  - 5.1 线程启动时
  - 5.2 线程执行任务
  - 5.3 一些边界条件
- 06.任务提交实现
  - 6.1 提交普通任务
  - 6.2 提交优先任务
  - 6.3 模版函数总结
- 07.获取线程池状态
  - 7.1 普通任务队列大小
  - 7.2 优先任务队列大小
  - 7.3 返回线程数量
- 08.主函数测试
  - 8.1 模拟耗时操作
  - 8.2 创建线程池
  - 8.3 提交普通任务和优先任务
- 09.核心设计思想
  - 9.1 生产者-消费者
  - 9.2 RAII资源管理
  - 9.3 双队列优先级设计
  - 9.4 future/promise
  - 9.5 线程生命周期
  - 9.6 锁粒度优化
  - 9.7 可扩展性设计
- 10.性能与安全平衡
  - 10.1 唤醒策略优化
  - 10.2 异常安全
  - 10.3 资源清理

## 01.简易线程池设计
### 1.1 核心设计思想


### 1.2 核心设计目标

1. 资源管理优化：避免频繁创建/销毁线程的开销；实现线程复用，提高资源利用率；控制并发线程数量，防止资源耗尽。
2. 任务调度机制：支持任务优先级（普通任务 vs 优先任务）；实现公平的任务分配；确保任务执行顺序可控
3. 线程安全保证：确保多线程环境下数据访问安全；避免竞态条件和死锁；提供异常安全保证

### 1.3 开发设计步骤

1. 步骤1：定义线程池类及成员变量 
2. 步骤2：实现构造函数（带名称和最大线程数） 
3. 步骤3：实现构造函数（默认名称） 
4. 步骤4：实现析构函数 
5. 步骤5：实现清理任务队列的函数（普通任务、优先任务、全部任务） 
6. 步骤6：实现线程主循环函数（taskLoop） 
7. 步骤7：实现任务提交接口（普通任务和优先任务） 
8. 步骤8：实现获取任务数量和线程数量的接口 
9. 步骤9：编写示例任务函数和主函数测试

### 1.4 技术栈说明

| 技术类别 | 具体技术 | 应用场景 |
|---------|---------|---------|
| **并发基础** | std::thread, std::mutex | 线程创建和同步 |
| **任务管理** | std::function, std::bind | 任务封装 |
| **结果处理** | std::future, std::promise | 异步结果获取 |
| **容器** | std::vector, std::queue | 线程和任务存储 |
| **模板编程** | 变长模板, 类型推导 | 通用任务接口 |
| **现代特性** | 移动语义, Lambda | 性能优化和简洁代码 |
| **设计模式** | 生产者-消费者, RAII | 架构设计 |
| **同步机制** | 条件变量, 互斥锁 | 线程协调 |


### 1.5 核心组件说明

1. 并发编程组件

- **std::thread**：线程创建和管理
- **std::mutex**：互斥锁（mQueueMutex）
- **std::unique_lock**：RAII风格的锁管理
- **std::condition_variable**：线程同步机制（mCv）
- **std::future/std::promise**：异步结果处理
- **std::packaged_task**：任务封装和结果绑定

2. 条件变量模式

- 等待条件：mCv.wait(lock, predicate)
- 通知机制：mCv.notify_one()/notify_all()


## 02.定义线程池类

实现要点：

1. 定义线程池的核心数据结构 
2. 声明公共接口：任务提交、清理和状态查询 
3. 封装线程主循环和生命周期回调 
4. 使用模板方法支持任意类型的任务

### 2.1 包含必要头文件

```cpp
#include <iostream>
#include <thread>
#include <mutex>
#include <condition_variable>
#include <queue>
#include <functional>
#include <future>
#include <vector>
#include <algorithm>

class ThreadPool {
};
```


### 2.2 声明公有接口

```cpp
class ThreadPool {
public:
    // 初始化线程池，默认线程数为 CPU 核心数加 1。
    ThreadPool(size_t maxNumberOfThreads = (std::thread::hardware_concurrency() + 1));
    // 带名称的构造函数。
    ThreadPool(const std::string &name, size_t maxNumberOfThreads = (std::thread::hardware_concurrency() + 1));
    // 析构函数
    virtual ~ThreadPool();

    // 任务管理接口
    // 提交普通任务。
    template<class F, class... Args>
    auto postTask(F &&f, Args &&... args) -> std::future<typename std::result_of<F(Args...)>::type>;
    // 提交优先任务。
    template<class F, class... Args>
    auto postPreTask(F &&f, Args &&... args) -> std::future<typename std::result_of<F(Args...)>::type>;
    void postPackagedTask(const std::shared_ptr<std::packaged_task<int()> > &task);
    // 清空普通任务队列。
    void clearTasks();
    // 清空所有任务队列。
    void clearAllTasks();
    // 清空优先任务队列。
    void clearPreTasks();

    // 返回普通任务队列的大小。
    std::size_t taskSize() { return mTasks.size(); }
    // 返回优先任务队列的大小。
    std::size_t preTaskSize() { return mPrepTasks.size(); }
    // 返回所有任务队列的大小。
    std::size_t allTaskSize() { return taskSize() + preTaskSize(); }
protected:
    // 线程启动时的回调（空实现）。
    virtual void onThreadAttached() {};
    // 线程退出时的回调（空实现）。
    virtual void onThreadDetached() {}
private:
    void taskLoop(const std::string &name);
    ThreadPool(const ThreadPool &) = delete;
    ThreadPool(ThreadPool &&) = delete;
    ThreadPool &operator=(const ThreadPool &) = delete;
    ThreadPool &operator=(ThreadPool &&) = delete;
private:
    // need to keep track of threads so we can join them
    // 存储线程池中的线程。
    std::vector<std::thread> mThreads;
    // 普通任务队列。
    std::queue<std::shared_ptr<std::packaged_task<int()> > > mTasks;
    // 优先处理的队列
    std::queue<std::shared_ptr<std::packaged_task<int()> > > mPrepTasks;
    // 保护任务队列的互斥锁。
    std::mutex mQueueMutex;
    // 条件变量，用于线程间的同步。
    std::condition_variable mCv;
    // 标志位，指示线程池是否停止。
    volatile bool mStop;
    // 空闲线程的互斥锁（未在代码中使用）。
    std::mutex mIdleMutex;
};
```

## 03.实现构造函数

实现要点：

1. 初始化 mStop为 false，表示线程池运行状态 
2. 使用 emplace_back直接构造线程对象 
3. 每个线程执行 taskLoop函数 
4. 构造函数重载提供默认线程名称

### 3.1 带名称构造

```cpp
ThreadPool::ThreadPool(const std::string &name, size_t maxNumberOfThreads) : mStop(false) {
    // 初始化 mStop 为 false，表示线程池处于运行状态。
    for (size_t i = 0; i < maxNumberOfThreads; ++i) {
        // 使用 emplace_back 创建 maxNumberOfThreads 个线程，每个线程执行 taskLoop 函数。
        // 将线程池的名称传递给 taskLoop，用于设置线程名称（在支持的系统上）。
        mThreads.emplace_back(&ThreadPool::taskLoop, this, name);
    }
}
```

### 3.2 默认构造函数

作用：初始化线程池，使用默认名称 "yt_thread_pool"。

```cpp
// 功能：与带名称的构造函数类似，只是线程名称固定为 "yt_thread_pool"。
ThreadPool::ThreadPool(size_t maxNumberOfThreads) : mStop(false) {
    for (size_t i = 0; i < maxNumberOfThreads; ++i) {
        mThreads.emplace_back(&ThreadPool::taskLoop, this, "yt_thread_pool");
    }
}
```

### 3.3 实现析构函数

作用：销毁线程池，确保所有线程安全退出。

实现要点：

1. 加锁设置 mStop为 true 
2. 使用 notify_all()唤醒所有等待线程 
3. 遍历所有线程并调用 join()等待结束 
4. 确保线程安全退出，避免资源泄漏

```cpp
// 原理：mStop 是一个标志位，用于通知线程退出主循环。mCv.notify_all() 确保所有线程都能检测到 mStop 的变化。join 确保线程池销毁前所有线程都已完成任务。
ThreadPool::~ThreadPool() {
    {
        std::unique_lock<std::mutex> lock(mQueueMutex);
        // 设置 mStop 为 true，通知所有线程停止运行。
        mStop = true;
    }
    // 使用 mCv.notify_all() 唤醒所有等待的线程。
    mCv.notify_all();
    // 使用 std::for_each 和 std::thread::join 等待所有线程执行完毕。
    // std::for_each(mThreads.begin(), mThreads.end(), std::bind(&std::thread::join, std::placeholders::_1));
    // 等待所有线程结束
    for (auto &thread : mThreads) {
        if (thread.joinable()) {
            thread.join();
        }
    }
}
```

## 04.实现清理任务

### 4.1 清空普通任务

作用：清空普通任务队列 mTasks。

```cpp
void ThreadPool::clearTasks() {
    // 功能：使用互斥锁保护队列，逐个移除任务。
    std::unique_lock<std::mutex> lock(this->mQueueMutex);
    while (!this->mTasks.empty()) {
        mTasks.pop();
    }
}
```

### 4.2 清空优先任务

```cpp
void ThreadPool::clearPreTasks() {
    //遍历逐个清除任务
    std::unique_lock<std::mutex> lock(this->mQueueMutex);
    while(!this->mPrepTasks.empty()) {
        mPrepTasks.pop();
    }
}
```

### 4.3 清空所有任务

作用：清空所有任务队列（包括普通任务和优先任务）。

```cpp
// 功能：依次调用 clearTasks 和 clearPreTasks。
void ThreadPool::clearAllTasks() {
    clearTasks();
    clearPreTasks();
}
```

## 05.线程主循环函数

实现要点：

1. 使用条件变量等待任务或停止信号 
2. 优先处理优先任务队列 
3. 任务执行在锁外进行，避免阻塞其他线程 
4. 提供线程生命周期回调点

### 5.1 线程启动时

```cpp
void ThreadPool::taskLoop(const std::string &name) {
    onThreadAttached(); // 线程启动回调
}
```

### 5.2 线程执行任务

```cpp
void ThreadPool::taskLoop(const std::string &name) {
    onThreadAttached(); // 线程启动回调
    while (true) {
        std::shared_ptr<std::packaged_task<int()> > preTask;
        std::shared_ptr<std::packaged_task<int()> > task;
        {
            // 等待条件变量：停止信号或任务可用
            std::unique_lock<std::mutex> lock(this->mQueueMutex);
            //使用 mCv.wait 等待任务队列非空或线程池停止。
            mCv.wait(lock,[this] {
                return this->mStop || !this->mTasks.empty() || !this->mPrepTasks.empty();
            });
            // 检查停止信号
            if (this->mStop) {
                // 如果 mStop 为 true，调用 onThreadDetached() 并退出循环。
                onThreadDetached();
                return;
            }
            // 优先执行优先任务队列 mPrepTasks 中的任务。
            if (!this->mPrepTasks.empty()) {
                preTask = this->mPrepTasks.front();
                this->mPrepTasks.pop();
            }
            //执行普通任务队列 mTasks 中的任务。
            if (!this->mTasks.empty()) {
                task = this->mTasks.front();
                this->mTasks.pop();
            }
        }
        // 执行任务（在锁外执行，避免阻塞其他线程）
        if (preTask) {
            //std::cout << "preTask begin" << std::endl;
            (*preTask)();
            //std::cout << "preTask end" << std::endl;
        }
        if (task) {
            //std::cout << "task begin" << std::endl;
            (*task)();
            //std::cout << "task end" << std::endl;
        }
    }
}
```

### 5.3 一些边界条件

```cpp

```

## 06.任务提交实现

实现要点：

1. 使用模板支持任意类型任务 
2. 使用 std::packaged_task封装任务 
3. 返回 std::future用于获取结果 
4. 任务加入队列后唤醒一个线程 
5. 检查线程池状态，避免已停止时提交任务

### 6.1 提交普通任务

```cpp
template<class F, class... Args>
auto ThreadPool::postTask(F &&f, Args &&... args) -> std::future<typename std::result_of<F(Args...)>::type> {

    using return_type = typename std::result_of<F(Args...)>::type;
    // 封装任务为 packaged_task
    auto task = std::shared_ptr<std::packaged_task<return_type()> >(
        new std::packaged_task<return_type()>(std::bind(std::forward<F>(f), std::forward<Args>(args)...)));
    std::future<return_type> res = task->get_future(); {
        std::unique_lock<std::mutex> lock(mQueueMutex);
        // don't allow enqueueing after stopping the pool
        if (mStop) {
            throw std::runtime_error("enqueue on stopped ThreadPool");
        }
        // push 一个 task任务
        mTasks.push(task);
    }
    mCv.notify_one();
    return res;
}
```

### 6.2 提交优先任务

```cpp
template<class F, class... Args>
auto ThreadPool::postPreTask(F &&f, Args &&... args) -> std::future<typename std::result_of<F(Args...)>::type> {
    using return_type = typename std::result_of<F(Args...)>::type;
    auto task = std::shared_ptr<std::packaged_task<return_type()> >(
        new std::packaged_task<return_type()>(std::bind(std::forward<F>(f), std::forward<Args>(args)...)));
    std::future<return_type> res = task->get_future(); {
        std::unique_lock<std::mutex> lock(mQueueMutex);
        if (mPrepTasks.size() > 500) {
            std::future<int> empFuture = std::async(std::launch::async, []() -> int { return 0; });
            return empFuture;
        }

        // don't allow enqueueing after stopping the pool
        if (mStop) throw std::runtime_error("enqueue on stopped ThreadPool");

        mPrepTasks.push(task);
    }
    mCv.notify_one();
    return res;
}
```

### 6.3 模版函数总结

- **模板函数**：支持任意任务类型
  ```cpp
  template<class F, class... Args>
  auto submit(F&& f, Args&&... args)
  ```
- **类型推导**：auto和decltype
- **完美转发**：std::forward


## 07.获取线程池状态

实现要点：

1. 加锁访问共享数据 
2. 提供实时任务队列状态 
3. 线程数量直接返回容器大小

### 7.1 普通任务队列大小

```cpp
std::size_t ThreadPool::taskSize() {
    std::unique_lock<std::mutex> lock(mQueueMutex);
    return mTasks.size();
}
```

### 7.2 优先任务队列大小

```cpp
std::size_t ThreadPool::preTaskSize() {
    std::unique_lock<std::mutex> lock(mQueueMutex);
    return mPrepTasks.size();
}
```

### 7.3 返回线程数量

```cpp
std::size_t ThreadPool::allTaskSize() {
    return taskSize() + preTaskSize();
}
```

## 08.主函数测试

测试要点：

1. 验证线程池创建和销毁 
2. 测试普通任务和优先任务的执行顺序 
3. 验证任务结果获取 
4. 测试任务清理功能 
5. 验证批量任务处理能力

### 8.1 模拟耗时操作

```cpp
int test1(std::string name, int age) {
    std::cout << "打印1：" << name << " , age " << age << std::endl;
    std::this_thread::sleep_for(std::chrono::milliseconds(100));
    std::cout << "打印1：等待了100ms" << std::endl;
    return 1;
}

int test2(std::string name, int age) {
    std::cout << "打印2：" << name << " , age " << age << std::endl;
    return 2;
}
```

### 8.2 创建线程池

```cpp
TaskThreadPtr m_callback_thread;
m_callback_thread = std::make_shared<TaskThread>();
auto future1 = m_callback_thread->postTask(&test1, "yangchong", 30);
auto future2 = m_callback_thread->postTask(&test2, "tangxinyi", 29);
```

### 8.3 提交普通任务和优先任务

```cpp
// 提交普通任务
auto future3 = m_callback_thread->postTask([]() {
    std::cout << "打印3：提交普通任务" << std::endl;
    std::this_thread::sleep_for(std::chrono::seconds(1));
    std::cout << "打印3：提交普通任务执行结束" << std::endl;
    return 42;
});
// 提交优先任务
auto future4 = m_callback_thread->postPreTask([]() {
    std::cout << "打印4：提交优先任务" << std::endl;
    std::this_thread::sleep_for(std::chrono::seconds(1));
    std::cout << "打印4：提交优先任务执行结束" << std::endl;
    return 100;
});
```

## 09.核心设计思想

### 9.1 生产者-消费者

- **生产者**：调用 postTask/postPreTask 提交任务的线程
- **消费者**：线程池中的工作线程
- **缓冲区**：任务队列(mTasks/mPrepTasks)
- **同步机制**：互斥锁(mQueueMutex)+条件变量(mCv)

这种模式解耦了任务提交和执行，提高了系统的吞吐量和响应能力。

### 9.2 RAII资源管理

- **构造函数**：创建线程资源
- **析构函数**：自动释放资源（通知停止+等待线程结束）
- **异常安全**：确保异常情况下资源正确释放

```cpp
ThreadPool::~ThreadPool() {
    {
        std::unique_lock<std::mutex> lock(mQueueMutex);
        mStop = true;
    }
    mCv.notify_all();
    for (auto &thread : mThreads) {
        if (thread.joinable()) thread.join();
    }
}
```

### 9.3 双队列优先级设计

- **普通任务队列(mTasks)**：先进先出(FIFO)
- **优先任务队列(mPrepTasks)**：高优先级任务
- **调度策略**：

```cpp
if (!mPrepTasks.empty()) { // 优先处理优先任务
    preTask = std::move(mPrepTasks.front());
    mPrepTasks.pop();
}
if (!mTasks.empty()) { // 再处理普通任务
    task = std::move(mTasks.front());
    mTasks.pop();
}
```

这种设计实现了： 1.紧急任务优先处理；2.普通任务公平调度；3.避免优先级反转问题

### 9.4 future/promise

基于 `future/promise` 的任务结果处理

```cpp
template<class F, class... Args>
auto ThreadPool::postTask(F &&f, Args &&... args) -> std::future<typename std::result_of<F(Args...)>::type> {

    using return_type = typename std::result_of<F(Args...)>::type;
    // 封装任务为 packaged_task
    auto task = std::shared_ptr<std::packaged_task<return_type()> >(
        new std::packaged_task<return_type()>(std::bind(std::forward<F>(f), std::forward<Args>(args)...)));
    // 获取future用于获取结果
    std::future<return_type> res = task->get_future();
    // 任务加入队列
    {
        std::unique_lock<std::mutex> lock(mQueueMutex);
        // push 一个 task任务
        mTasks.push(task);
    }
    mCv.notify_one();
    return res;
}
```

这种设计实现了：1.异步任务结果获取；2.任务执行状态跟踪；3.异常传播机制

### 9.5 线程生命周期

- **线程创建**：构造函数中创建固定数量线程
- **线程回收**：析构函数中安全停止并回收
- **线程状态回调**：

```cpp
void onThreadAttached() { /* 线程启动时调用 */ }
void onThreadDetached() { /* 线程退出时调用 */ }
```

提供扩展点用于线程本地存储(TLS)初始化等操作

### 9.6 锁粒度优化

- **细粒度锁**：只在访问共享数据时加锁
- **锁外执行**：任务执行在锁范围外进行

```cpp
{
    std::unique_lock<std::mutex> lock(mQueueMutex);
    // 访问共享数据
} // 锁自动释放

// 在锁外执行任务
if (task) task(); 
```

这种设计：1.减少锁竞争；2.提高并发性能；3.避免任务执行阻塞其他线程

### 9.7 可扩展性设计

- **模板化任务提交**：支持任意类型任务
- **生命周期回调**：提供扩展点
- **状态查询接口**：便于监控

```cpp
size_t getTaskCount() const;
size_t getPriorityTaskCount() const;
size_t getThreadCount() const;
```

### 9.8 潜在改进方向

1. **动态线程调整**：根据负载自动增减线程
2. **任务窃取机制**：提高负载均衡
3. **任务超时处理**：防止任务长时间阻塞
4. **任务依赖管理**：支持有依赖关系的任务
5. **线程本地队列**：减少锁竞争


## 10.性能与安全平衡
### 10.1 唤醒策略优化

**唤醒策略优化**

- `notify_one()`：提交单个任务时只唤醒一个线程
- `notify_all()`：停止时唤醒所有线程

### 10.2 异常安全

**异常安全**。任务提交时检查线程池状态

```cpp
if (mStop) {
   throw std::runtime_error("线程池已停止，无法提交任务");
}
```

任务执行异常通过future传播

### 10.3 资源清理

```cpp
void clearTasks();
void clearPreTasks();
void clearAllTasks();
```

提供任务队列清理机制，防止无效任务堆积

